# CS018 — 1.0.1: the ten coverage gaps, the VOICE row

Per-changeset narrative (`CLAUDE.md`: reasoning, measured-against-predicted and
mutation records here; hazards and what the next phase acts on in `STATUS.md`).
Plan: `archive/PLANNED-FEATURES-CS018.md`, `archive/IMPLEMENTATION-PHASES-CS018.md`, planned at
`35a7dde` from the CS017 close `43e07f2`.

## P1 — the ten coverage gaps, each with a carrier (2026-09-23)

**Calls taken as they stand (plan §0, no answer from Paul):** Q1, Q4, Q5, Q6,
Q7, Q8, Q9. None re-opened.

### `build.js` (Q6)

- `injectScript(shell, script)` is the one `shell.replace(MARKER, () => script)`;
  `build()` calls it; `kitBlock` and `injectScript` are exported (6 → 8 names).
- ⛔ **`dist/vector-vortex.html` byte-identical across the fix**, MEASURED:
  sha256 `702d98989a56b57cf45eef579a2ebde16f629260fb83d122769fbc71c6487455`,
  856,874 bytes, before (`node build.js` at `35a7dde`) and after. PREDICTED
  identical; no finding.
- **The string form's `$` demonstration**, MEASURED (one line of Node):
  `` "a<!--M-->b".replace("<!--M-->", "x$&y$$z$`w$'v") `` is
  `"ax<!--M-->y$zawbvb"` — `$&` became the marker, `$$` one `$`, `` $` `` the
  text before it, `$'` the text after. `test-cs018-p1.js` G5 asserts the
  function form carries the same probe verbatim and that the string form does
  not (the probe's non-vacuity).

### `scratchpad/test-cs018-p1.js`

80 assertions, one section per gap. ⛔ **Timed alone: 1.97–2.68 s over four
runs** (`/usr/bin/time`), against PREDICTED < 6 s. Deterministic: G7's clock
is a fixed base plus the frame clock and its jitter the test's own stream, so
every run prints the same numbers.

- **G1** — 200 comparisons counted, every one strict; `heat(201)` finite.
- **G2** — the bassline's per-bar figure (step positions, `drive`'s method)
  changes at exactly `[0, 12, 24]`; the heart is 2 notes a bar in all twelve A
  bars and 4 in all twelve B bars. ⚠ **Two assertions beyond the plan's G2**,
  taken because §11.7's restated B names them and a restated clause should
  have a carrier: the bassline's first leap is 7 semitones in every A bar and
  12 in every B bar (MEASURED: C's is 0, a repeated root), and the melody's
  mean sits higher in B (MEASURED 71.8 → 77.3 MIDI, C 75.2; bar 32's tune
  equals bar 0's).
- **G3** — 36 deflections (0.15 … 1.00 by 0.05, both signs) exact to the bit
  against `x × GAMEPAD_SENS × FIXED_DT`; every doubling that stays ≤ 1.0
  exactly doubles; just inside the deadzone is 0 (non-vacuity).
- **G4** — eight depths through `drawShot()` against a recording `stroke`,
  both passes' `globalAlpha` to 1e-12; one real `Game.draw()` with a shot at
  depth ~0.125 finds the faded `SKIMMER_COLOR` pair. ⚠ `HUD_COLOR` is also
  `#FFFFFF`, so the board check matches by colour AND the exact faded alpha
  pair, not colour alone. Mutation `shotAlpha(depth)` → `1` on `drawShot()`'s
  `glowStroke` line: red on both (the line asserted once in the build first).
- **G5** — banners found by `test-cs012-p1.js`'s regex, never constructed;
  each `src/` file (`trimEnd()`) is its slice's prefix and in the script once;
  `kitBlock()` once, preceded only by whitespace after `20-achievements.js`'s
  body; the residue `"use strict";`. ⚠ **First run red, a defect in the
  CHECKER**: `trimStart()` before `startsWith(KIT)` ate the kit block's own
  leading newline; now the kit is found by `indexOf` and what precedes it must
  be whitespace. Non-vacuity: one byte flipped mid-`14-render-entities.js` and
  `01-rng.js` / `02-state.js` swapped, both red. The page equals
  `injectScript(shell, "<script>" + script + "</script>")`.
- **G6** — three children, MEASURED offsets 0 / −840 / 660 at 2026-01-15 12:00
  UTC. Shipped keys right in all three. The local mutant: 0/9 wrong under UTC,
  **2/9 Kiritimati** (the two Sunday-night instants read as W39), **4/9 Pago
  Pago** (every New-Year boundary a day early). America/Chicago, run by hand:
  4/9 (offset 360 at that instant). Matches plan §1.2 G6.
- **G7** — MEASURED, 64 runs: active first kill **2.15–2.52 s Overdrive,
  2.15–2.58 s Classic** (medians 2.33 / 2.40); passive first craft lost
  **6.900–10.88 s / 6.900–10.33 s** (medians 8.68 / 8.68). Against the plan's
  probe (active 1.65–2.32, median 2.20; passive 6.883–10.87): the bounds hold;
  the active numbers are slower than the plan's probe and close to CS016's
  (median 2.38, worst 2.68) — a different driver (this one steers toward the
  deepest enemy by `laneDelta`, capped per step, and never taps), not the
  game. The passive floor reads 6.900 rather than 6.883 because `state.time`
  is read after the step the craft is lost on — one tick, the bound's slack.
  The run is left by `Game.quitToTitle()`, the pause row's own action: the
  front door is the entry, and what follows the measurement is not measured.
- **G8 / G9** — the four `C` lines by literal; each `mutate` string asserted
  once in the build, then its value moved by one unit in the last place shown
  (0.41, 2.51, 601, 18001) and red.
- **G10** — list, addition and exception read from `test-cs008-p6.js` by
  regex (9 words, 1 addition, 1 exception); `LIB_FILES` read from
  `package-for-itch.sh` (2 files); the markup is the page with the game's tag
  cut out (the module bridge kept). Clean. Non-vacuity: five probes built from
  the list; the markup's vendor prefix is the exception's own case. ⛔ The file
  scans ITSELF last: it writes no banned word.

### Documents

- GDD §19: ten "⛔ At 1.0.1 (CS018 P1)" lines, each under its row's at-ship
  block (Core 1, Audio 3, Meta 1, Onboarding 1, Quality 4); the at-ship blocks
  untouched. G8, G9 and G6 were ✅ with a ⚠, so their lines say "the ⚠
  closed" rather than ◐ → ✅. The stick's feel, the traverse by hand and the
  repository's documents (S10-B) stay where they were.
- GDD §11.7's `pulse` row: B restated to the table (Q8).
- `CLAUDE.md`'s `tools/` line (Q9): 38,741 → 38,748 bytes.

### Closed files

⛔ **None edited** — plan §8 predicted none. No finding.

### The suite

`node scratchpad/run-all.js` → **86 files passed, zero skips, exit 0, 391.06 s
wall** (`/usr/bin/time`, `../coinless-kit` present). Planning measured 308.8 s
for 85; the new file costs ~2 s alone, so the rest is machine load (no file
near the 120 s per-file timeout was reported). `P1_DETERMINISM_HASH`,
`GOLDEN_LANES` and all fifteen soaks green, unedited.

---

## P2 — the VOICE row, 1.0.1, the review and the close (2026-09-23)

**Calls taken as they stand (plan §0, no answer from Paul):** Q2 (A), Q3,
Q10. None re-opened. ⚠ **This is the one CS018 session that read `log/`** —
this file, whole, because the close reviews it; plus `log/CS017.md`'s P4
header, review headings, "moved out" and version-history sections, for FORM
only. Nothing else from `log/` or `archive/`.

### The cut (Q2-A)

V1's seven lines in `src/23-main.js` — `SOUND_ROWS.voice`, its place in
OPTIONS' items, `"voice"` out of `VOL_BUSES`, `sound`, `ADJUST`,
`settingsSnapshot()` and `applySettings()`' field list — and the A3 comment
restated above `VOL_BUSES` (the bus is kit-audio's, built at unity by
`19-sfx.js`, fed and moved by nothing; no row, no `sound` field, no stored
value). ⛔ kit-audio, `19-sfx.js`, both labs and `update()` untouched (nothing
re-indented; `SEAT_EDGE` green). `settings` stays v1 with no `migrate`.
`C.GAME_VERSION`'s comment gained "CS018, the first patch, 1.0.1".

`dist/` **856,874 → 856,880 bytes** (sha256 `57c3b3e0…`): the cut alone is
856,849 against V1's 856,656 — the difference is the restated comment (~190
bytes, three lines where there was one) — then +31 for the version comment.
The plan's PREDICTED ~856,650 assumed a one-line comment.

### The red set, measured against predicted

Before any repair, with the cut built: `test-cs008-p6.js` 1/260,
`test-cs008-p7.js` 1/172, `test-cs011-p2.js` 45/135, `test-cs012-p1.js` 6/55,
`test-cs009-p3.js` THROWS at `:227` — V1's five, to the assertion. After the
repairs, each file's assertion count equals HEAD's 1.0.0 run of it
(MEASURED in a `git worktree` of `f4267bc`): **184 / 135 / 260 / 172 / 55** —
no assertion deleted.

- `test-cs008-p6.js` — the sound-row label list without VOICE; "ten rows
  outgrow" → "the rows outgrow". The three forms G10 reads by text untouched.
- `test-cs008-p7.js` — RESET TO DEFAULTS' stored shape without `voice`.
- `test-cs012-p1.js` — `toTrack()`'s `right(8)` → `right(7)`.
- `test-cs011-p2.js` — `DEF` / `GOOD` without `voice`; VOICE's navigation step
  out; the reload's "VOICE VOLUME 20%" row rewritten to "no VOICE VOLUME between
  SFX VOLUME and MUSIC TRACK" (both drawn, so the absence is not the window's);
  the two `sound.voice` CASES rows rewritten to a 1.0.0 row's VALID (2) and
  INVALID (2.5) `voice`, each ignored with every other field loading — a
  `voice` branch in the loop, since the default-per-field `want` has no
  `voice` to restore.
- `test-cs009-p3.js` — read past `:227` first, as the plan said. `:194`'s
  `right(8)` → `right(7)`; ⚠ **FINDING, not in plan §8's list: `:308`'s
  `right(3)` from MUSIC VOLUME aimed at MUSIC TRACK (5 + 3 = 8) and would now
  open ACHIEVEMENTS** — repaired to `right(2)`, restoring the precondition
  (the MUSIC TRACK clamps); PREDICTED territory the plan had marked, so a
  finding of its prediction, not of the plan's shape. `:324` / `:338`'s
  `right(8)` → `right(7)`, re-aimed at MUSIC TRACK as they were at 1.0.0 (both
  scroll the window onto every sound row either way). **The VOICE block,
  REPLACED behaviour, rewritten assertion by assertion**: `ROWS` (the three
  volume rows) now drives the per-row pass and `BUSES` stays the engine's four,
  so every "untouched" and "unity" check still reads the voice bus; VOICE's own
  pass became "its old row, 4 + 3, is MUSIC TRACK: Fire arms it, three taps
  clamp it at AUTO, the voice bus holds unity, no ramp reaches it — nor ever
  has, this session — no bare set, the other three untouched, Fire exits"; A3's
  "nothing connected into the voice bus" kept; "and VOICE 70%" → "no VOICE row
  to keep: the voice bus is at unity"; `Game.reset()`'s "restores voice to
  unity / ramps the voice bus back" → "leaves voice at unity / never ramps it";
  "after Game.reset(), VOICE VOLUME reads 100%" → "still no VOICE VOLUME row".
  Header restated.
- `test-cs016-p1.js:121` — `"1.0.1"`, with the cause (V2's one red).

⛔ **The vacuity grep** (`STATUS.md`: a closed file can go vacuous without going
red): every closed file that opens OPTIONS and steps `right(n)` or
`ArrowRight` — seventeen by grep — read at its presses. Beyond the five, none
passes index 6: `test-cs011-p6.js` stops at MUSIC VOLUME (5), `-cs009-p5.js`
at MASTER (4), `-cs011-p2.js`'s profile switch at MUSIC VOLUME (5),
`-cs011-p4.js` and `-cs008-p7.js` at CONTROLS (2), `-cs011-p2.js`'s telemetry
at EXPORT (1), `-cs015-p3.js` walks to ACHIEVEMENTS BY LABEL and its row count;
the rest open OPTIONS and leave. No `tools/` script steps OPTIONS.

### `scratchpad/test-cs018-p2.js` — 30 assertions, 0.36–0.59 s alone

Four sections; the cursor moves BY LABEL (the `drawMenu` spy's view), so the
file cannot be the next one a cut re-indexes.
- **A** — OPTIONS' items are exactly the ten labels, ACHIEVEMENTS then BACK
  last, TELEMETRY first; a traversal of the seven-row window draws all ten and
  never VOICE VOLUME, ending clamped on BACK.
- **B** — a planted 1.0.0 row (`voice: 2`, every other field off its default,
  FIRE bound to Q) loads whole but for `voice`; one MASTER step saves `v: 1`
  with no `voice`, exactly the loaded row with MASTER 6.
- **C** — on the recording context, after unlock the voice bus has its ONE
  build-time `.value = 1`; after each of the four sound rows armed and moved,
  BACK, RESET TO DEFAULTS and `Game.reset()` it has no automation, no second
  set and `vol` 1; nothing feeds it. Non-vacuity: MASTER ramps to 0.8 and
  `Game.reset()` ramps it back to 1.
- **D** — ⛔ the 1.0.0 build as a mutant (`V100`: the seven cut lines put back,
  each asserted in the build exactly once first) is red on A (VOICE is a row),
  B (it loads and stores `voice`) and C (`Game.reset()` ramps the voice bus).
  This is the plan's "a mutant restoring `SOUND_ROWS.voice` is red", widened to
  the whole cut so each section has its red.

### The version (Q3) and the package

`C.GAME_VERSION` `"1.0.1"`, `package.json` `"1.0.1"`, `test-cs016-p1.js:121`
rewritten in place. `bash package-for-itch.sh` → `dist/vector-vortex-itch.zip`, **287,515 bytes**, three files (`index.html` 856,880 — the build, sha256 `57c3b3e0…`, `GAME_VERSION` "1.0.1" inside — `lib/kit-leaderboard/kit-leaderboard.js` 9,533, `lib/kit-names/kit-names.js` 1,363). ⚠ It replaced
the untracked 1.0.0 zip; the zip stays untracked (not staged).

### Documents

GDD §10.5: the Options row lists ten rows and says VOICE was cut; "the five
rows" → four, "Ten rows outgrow" → "OPTIONS' ten rows outgrow
`MENU_VISIBLE_ROWS` (7)"; the VOICE sentence restated (cut at 1.0.1, why, and
the v1 row loading). §11.1's bus sentence: `voice` fed by nothing and, since
1.0.1, moved by nothing, no OPTIONS row. §19 Audio: one "At 1.0.1 (CS018 P2)"
line for "volume sliders persist" under the P1 lines; the at-ship "Met, since
CS011" line (which names VOICE) stays as history.

### The suite

`node scratchpad/run-all.js` → **87 files passed, zero skips, exit 0, 377.8 s
wall** (`/usr/bin/time`, `../coinless-kit` present), run after every `src/` and
test edit and before any document of the close was touched by a test (none
reads one). `P1_DETERMINISM_HASH`, `GOLDEN_LANES` and all fifteen soaks green,
unedited. No `mutate` threw (the nine CS011 strings, the seven seat strings and
P1's pins all sit off the edited lines).

---

## ⛔ THE CS018 REVIEW — the one pass that reads every phase together

Both phases' entries above, read together at the close.

### 1. Plan §7's baseline ledger, row by row (MEASURED at the close)

| Baseline | PREDICTED | At the close |
|---|---|---|
| `P1_DETERMINISM_HASH` 1229033515 | unmoved | ✅ unmoved (both determinism files green) |
| `GOLDEN_LANES` | unmoved | ✅ unmoved |
| The fifteen soaks | green, unedited | ✅ green; no soak file in either commit |
| `state` 28 / `tally` 23; kill sites 4 / lines 5; `C.SFX` 28 / pitch 11 | unmoved | ✅ `test-registry.js` and the kill-line pins green |
| Achievement ids 23 / 19 / 5 | frozen | ✅ unmoved |
| Save keys and versions | `settings` v1, no `migrate` | ✅ v1; a 1.0.0 row loads (`test-cs018-p2.js` B) |
| OPTIONS rows | 11 → 10 | ✅ 10 (P2) |
| `C.GAME_VERSION` / `package.json` | `"1.0.1"` | ✅ both, P2 |
| `dist/` bytes | P1 identical; P2 ~856,650 | P1 ✅ identical (sha logged); P2 **856,880** — the plan assumed a one-line restated comment (P2, "The cut") |
| `build.js` exports | 8 | ✅ 8 |
| `_harness.js` `EXPORTS` | 218 | ✅ 218 (regex over the array, comments stripped) |
| The suite | 87 files, 0 skips, ~+10 s | ✅ 87, 0 skips, exit 0; **377.8 s** (P1's 86 took 391 s — the wall is the machine's load, not the files: the two new ones cost ~2.5 s together) |
| `CLAUDE.md` | < 50 KB, ~±50 bytes | ✅ 38,748 bytes (P1's +7; P2 did not touch it) |
| `STATUS.md` | < ~400; reset post-ship | ✅ reset (287 lines pre-close, moved out below) |

### 2. Closed-file edits — plan §8 against what happened

P1: none, as predicted. P2: exactly §8's six files, each repaired in place with
its cause at the edit, and **one edit §8 did not list** — `test-cs009-p3.js:308`'s
`right(3)` → `right(2)` (P2's finding: a navigation past V1's throw that the
plan had marked PREDICTED as a block). No fixture relaxed; every assertion count
equal to 1.0.0's.

### 3. What phases said differently — stale claims, fixed

- Pre-close `STATUS.md` pinned the version literal at `test-cs016-p1.js:120`;
  the plan said `:121`, which is right — fixed in the reset.
- Pre-close `STATUS.md` and GDD §10.5 said OPTIONS has eleven rows / "five
  rows" / "ten rows outgrow" — all restated to 1.0.1's ten and four.
- The "ten coverage gaps" were nine at the CS017 close plus the markup (P1,
  plan §0.1) — already fixed by P1 in `STATUS.md` and GDD §19.
- The ROADMAP's CS017 "deliberately left" and pre-close `STATUS.md`'s unowned
  list both carried "the VOICE bus"; the first is a closed record and stands,
  the second is gone from the reset file.

### 4. GDD §19 at the close

Eleven "At 1.0.1" lines: P1's ten (Core 1, Audio 3, Meta 1, Onboarding 1,
Quality 4) and P2's one (Audio, volume sliders persist). The at-ship blocks
stand as history. A ◐ left at the close names a skipped playtest.

### 5. The changeset, compressed

CS018 is coverage and one row. **P1** gave each of the ten §19 gaps a carrier
in one file (80 assertions, ~2 s, three `TZ` children) and made `build.js`'s
injection a function replacement, with `dist/` byte-identical across it.
**P2** cut OPTIONS' VOICE VOLUME row — the slider that moved a bus nothing
feeds — leaving kit-audio's bus built at unity and moved by nothing, `settings`
at v1 loading a 1.0.0 row whole but for `voice`; repaired the five closed files
V1 named plus one navigation V1 never reached; proved the cut in a new file
whose mutant is the 1.0.0 build; set 1.0.1 and packaged it. ⛔ **No simulation
line moved in either phase.**

## What moved out of `STATUS.md` at the close

⛔ **NOTHING WAS DELETED.** `STATUS.md` is reset to the post-ship form at 1.0.1.
Its pre-close text follows **verbatim** (as committed at `f4267bc`); what a
patch must still obey was carried forward into the new file, with the CS018
planning and P1 entries compressed to one ledger paragraph and P1's new pins
moved under "Pinned text".

```text
# Vector Vortex — STATUS
Version: **1.0.0 — SHIPPED** · CS017 closed 2026-09-23 · **CS018 (1.0.1) — P1 landed, P2 next** ·
Wells: 16/16 · Enemies: 6/6 Classic, 3/3 Overdrive · Tracks: 3/5 · Tokens: 5/5 effects ·
Achievements: 23 lifetime + 19 weekly (**frozen**) · Prompts: 12

## What this file is now

⛔ **A POST-SHIP FILE.** CS001–CS017 are closed, each with a `log/CS0##.md`;
CS017's ledger, phase entries, review and **this file's pre-close text,
verbatim**, are in `log/CS017.md`. ⛔ **Post-ship work is a PATCH, and a patch
is a changeset**: a planning session first, then build phases, then a close —
`CLAUDE.md`'s three session kinds, unchanged. **CS018 is planned**
(`PLANNED-FEATURES-CS018.md` + `IMPLEMENTATION-PHASES-CS018.md`); its ledger
starts under "Phase ledger".

⚠ **PAUL'S, OUTSIDE THE REPO, BEFORE THE ITCH PAGE GOES LIVE** (S10, S12):
make `github.com/freakingid/vector-vortex` **private** (it is PUBLIC, and 8 live
documents name the original on ~38 lines); write the itch page's copy (no file
here can scan it — GDD §18.6); run `bash package-for-itch.sh` and upload
`dist/vector-vortex-itch.zip` (untracked; ~287 KB; three files, scanned clean).

## Phase ledger

| Changeset | Landed |
|---|---|
| CS018 planning | 2026-09-23 — 1.0.1: the ten §19 coverage gaps, the VOICE row, the version; two phases |
| CS018 P1 | 2026-09-23 — the ten gaps in `test-cs018-p1.js`; `build.js`'s `injectScript()`; no `dist/` byte moved |

**CS018 planning (2026-09-23).** Planned at `43e07f2` (85 files, zero skips,
308.8 s). Ten calls, Q1–Q10, each with one recommendation that STANDS unless
Paul answers (plan §0). **P1** closes the ten gaps in `test-cs018-p1.js` and
moves no `dist/` byte; **P2** cuts OPTIONS' VOICE VOLUME row (V1 MEASURED: five
closed files red, all label lists, settings shapes or row-index navigation;
`test-cs009-p3.js` throws at `:227`, so its VOICE block is PREDICTED), sets
`C.GAME_VERSION` 1.0.1 (V2: one red, `test-cs016-p1.js:121`) and closes.
✅ **Hazards found (plan §0.1) — all four FIXED at P1**: `build.js` injected
the script with a STRING replacement, so a `$&`, `$$`, `` $` `` or `$'` in any
module would have been rewritten in `dist/` (none was; Q6); the carried "ten
coverage gaps" were nine — Q1's tenth is the page's markup (all ten carried);
GDD §11.7's `pulse` "an octave up" did not match the table (restated, Q8);
`CLAUDE.md` named a `glow-lab.html` that was never built (corrected, Q9).

**CS018 P1 (2026-09-23).** Q1, Q4–Q9 taken as they stand.
`scratchpad/test-cs018-p1.js`: G1–G10, 80 assertions, **1.97–2.68 s alone** (PREDICTED < 6 s). `build.js`
injects through `injectScript()`, a FUNCTION replacement, and exports it and
`kitBlock` (8 names); ⛔ `dist/` sha256 `702d9898…` identical before and after.
No closed file edited (plan §8). Suite: **86 files, zero skips, exit 0, 391 s**
(`/usr/bin/time`; +82 s over planning's run, a load effect — the new file costs
~2 s). G7 MEASURED: first kill 2.15–2.58 s, first craft lost 6.900–10.88 s.
⚠ **For P2 — `test-cs018-p1.js` READS `test-cs008-p6.js` BY TEXT** (G10): its
`const WORDS = [ … ];` line, `WORDS.concat("…")` and the `w === "…" ? "…" : w`
exception. P2's label-list repair there must leave those three forms intact.
⚠ **New `mutate` pins**: `drawShot()`'s `glowStroke` line; `isoDayIndex()`'s
and `isoWeekKey()`'s UTC lines; the `INT_ATTACK`, `INT_RELEASE`,
`FILTER_MIN_HZ` and `FILTER_MAX_HZ` lines of `C`. G5 fixes `MANIFEST` at 26;
G7 enters START DEPTH's first row. How each call was reasoned, and the one
checker defect: `log/CS018.md`.

## Working / verified (MEASURED at the CS017 close)

- `node build.js` → `dist/vector-vortex.html`, 26 modules + 3 inlined kit,
  **836.8 KB**; `MANIFEST` checked both ways; a missing `KIT_INLINE` file fails
  the build.
- `node scratchpad/run-all.js` → **85 files, all green, zero skips**
  (319 s, `/usr/bin/time`). ⚠ **`run-all.js` has a 120 s PER-FILE timeout and machine load
  can trip it.** ⛔ A timeout is not a red — re-run the file alone first — but
  ⛔ a close cannot carry one. The slowest file is `test-cs014-p3.js` (37.6 s at planning, `867ebd1`),
  then the fifteenth soak (~35 s alone).
- ⛔ **Zero skips needs `../coinless-kit`**: `test-cs011-p5.js` (at `f0b0eb2`)
  and `test-cs012-p3.js` (at `e2efed5`, ⛔ never `f8d34f3`) read its
  `registry.js` and SKIP LOUDLY without it.
- ⛔ **`P1_DETERMINISM_HASH` 1229033515** (cross-file: `test-cs006-p2.js` runs
  `test-cs005-p5.js` in a child process) — unmoved CS009–CS017; re-record once
  per change, one named cause at the assertion. ⛔ **`GOLDEN_LANES`' first
  SIXTEEN entries are the `9ebd27b` recording** (+`2, 5`, CS008); any other move
  is a defect.
- ⛔ **FIFTEEN SOAKS, EACH PROVING A DIFFERENT THING** — `-cs003-p5` …
  `-cs007-p5`, `-cs008-p8`, `-cs009-p6`, `-cs010-p5`, `-cs011-p6`, `-cs012-p6`,
  `-cs013-p5`, `-cs014-p3`, `-cs015-p4`, `-cs016-p4`, **`-cs017-p4`** (§17 item
  12's hundred runs, 50 per mode, all sixteen wells). ⛔ **A new changeset's is a
  SIXTEENTH file, never a widened one.**
- ✅ **GDD §19: every row carries an "at ship" verdict block (CS017 P3, P4) and
  no clause is ✗.** The ◐ clauses name their missing half: a skipped playtest
  (`SKIPPED-PLAYTESTS.md`, CS009–CS017) or a coverage gap — ✅ all ten carried
  since CS018 P1 (GDD §19's "At 1.0.1" lines).
- `_harness.js` `EXPORTS` **218**; `test-registry.js` `enemies` 9,
  `enemyKinds` 13, `state` **28** keys, `tally` **23** counters inside one.
  `C.SFX` 28 events, `C.SFX_KILL_PITCH` 11 voices; kill sites / lines 4 / 5.
- ✅ **`CLAUDE.md` is 38,748 bytes** (CS018 P1) against its 50 KB ceiling; the
  valve and the ban on standing sweeps stand.

## ⛔ What a patch must obey

The rules are `CLAUDE.md`'s; these are the facts a patch trips over.

### Frozen at ship

- ⛔ **THE ACHIEVEMENT IDS AND THE POOL'S LENGTH**: 23 lifetime and 19 weekly
  ids, `perWeek` 5, never renamed; the rotation walks the pool BY LENGTH, so a
  row added or cut reshuffles every week's five against live saves. A
  threshold, a `name` and a `note` are not save data. ⚠ Real totals stay
  possible as an ADDITIVE v2, new ids beside these. ⛔ **`REACH` is measured by
  `tools/reach-probe.js`** (~30 s): replace it WHOLE from its output; an
  unreached row is reported to Paul, never lowered.
- ⛔ **The save keyspace** (`CLAUDE.md`'s table): `settings` v1, `progress` v2,
  `achievements` v1, `scores` (root), `telemetry` v1 (`TELEMETRY_FIELDS` 29,
  moving with `TELEMETRY_KINDS`, `telemetryRow()` and the version),
  `onboarding` v1. ⛔ A row-shape change bumps the version and supplies a pure
  `migrate` — `migrateProgress` is the worked example — never a new key name.
- ⛔ **The boards**: `C.LEADERBOARD_GAME_IDS` `vector-vortex` /
  `vector-vortex-overdrive`, registered with seven `statsFields`. ⛔ Read the
  Worker's registered `statsFields` before sending a stats key. ⛔ `C.GAME_ID` is
  the SAVE keyspace and nothing else.
- ⛔ **`C.GAME_VERSION` is `"1.0.0"` AND IS PINNED BY LITERAL in
  `test-cs016-p1.js:120`**: a patch's bump rewrites that assertion in place;
  `package.json`'s `version` moves with it.

### The build and its switches

- ⛔ **`C.DEBUG_KEYS` ships `false`**: the eight bench bindings (`1`–`6`, `0`,
  `w`) exist only in a flagged build, so no key a player can press voids a
  run's eligibility; `t`, `e`, `p`, Escape are bound in both. ⛔ **A FLAGGED
  BUILD IS A `mutate` of `  DEBUG_KEYS:           false,`, by literal, in 18
  files** (MEASURED at the close, grep) — re-spacing that line rewrites all
  eighteen in place. ⚠ **SETTLED — `w` is bindable in the shipped build**
  (Paul, S7 addendum); a profile that binds it loads its KEYBOARD page as
  defaults in a flagged dev build (dev only).
- ⛔ **A CLOSED FILE CAN GO VACUOUS WITHOUT GOING RED** (CS017 P3,
  `test-cs003-p2.js`): a change that unbinds, stubs or gates something a closed
  file DRIVES needs a grep for the file's presses, not only the red set.
- ⛔ **`test-cs017-p1.js` PINS THE BUDGET BOARD AT 156 STROKES AND 8 TEXT CALLS
  a `draw()`**: a renderer change that draws more rewrites `BUDGET_*` in place
  with the cause named — a counter, never a clock. Its nine mutants pin
  `const out = points._screen;`, `drawPoly(ctx, _shotPair, false);`,
  `ctx.font = textFont(size);`, ` : promptFadeColor(a);`, `wellBandColor()`'s
  indexed `for`, the three HUD cache lines and game over's
  `if (screen === SCREENS.gameover &&` guard. ⛔ **No allocating expression on
  the DRAW path** (GDD §17); the step path's `.filter()` is the invariant.
- ⛔ **THE PROMPT BAND IS ARITHMETIC**: `PROMPT_Y` 636 sits 6 px under the
  lowest rim; a change to `HUD_MARGIN`, `HUD_ICON_SIZE`, `WELL_RADIUS`,
  `PROMPT_SIZE` or a prompt's length re-derives it (`test-cs016-p1.js`).
- ⛔ **THE MENU SHAPES**: the title has five rows, game over three lines,
  OPTIONS eleven rows in a seven-row window (a row goes before BACK, never above
  TELEMETRY); a label ≤ 20 characters beside a four-character detail, TWO info
  lines a screen; the credits ≤ 7 lines of ≤ 66 characters, each passing the
  SUBSTRING vocabulary scan.
- ⛔ **`attractDrive()` IS A PORT OF THE SOAKS' HUNTER**: a roster or rule
  change the soaks' driver learns is taught to it too. ⚠ Its Mimic dodge — the
  soaks' too — MEASURED worse than no dodge for a bot (0.274 against 0.125
  kills per reflection); no call was made.
- ⚠ **SETTLED — a prompt can be marked seen and never drawn** (Paul,
  2026-09-23): `startGame()` empties the queue. Do not "fix" it.
- ⚠ **A mouse move during a demo DEATH FREEZE is drained and lost**; the
  shipped demo dies nowhere (MEASURED, ten seeds).

### Pinned text — grep before editing

- ⛔ **A PHASE THAT EDITS EITHER ACHIEVEMENT SEAT MOVES SEVEN `mutate`
  STRINGS**: `test-cs015-p2.js`'s `SEAT_EDGE` / `SEAT_END`, `-p3.js`'s
  `SEAT_CLEAR` / `SEAT_END`, `-p4.js`'s `SEATS_OUT` (two), `test-cs016-p3.js`'s
  `SEAT_CLEAR` — ⚠ and `tools/reach-probe.js` mutates the line above each seat.
  ⛔ **`SEAT_EDGE` MATCHES BY INDENTATION LUCK**: `Meta.clearEdge();` sits at
  eight spaces inside the demo's `if (!attract)` block.
- ⛔ **A PHASE THAT EDITS A KILL LINE** repairs `test-cs012-p4.js`'s six
  `COMBO_OUT` strings and two `mutantRed` shot-kill strings, `-p6.js`'s, and
  `test-cs014-p1.js`'s count of four (`09-collision.js:160, 286, 498, 512,
  565`). ⛔ **ITEM 8 PRICES A RING AND A BOUNTY PER STEP** in five files: a new
  payout owes all five a term. ⛔ **`gddPoints()` has seven copies**; order a
  price table's branches subclass-first.
- ⛔ **NINE TEXTS IN `22-meta.js`, `23-main.js` AND `lib/kit-leaderboard/` ARE
  PINNED BY A CLOSED CS011 `mutate`** — grep `test-cs011-*.js` for `mutate:`.
- ⛔ **AN EDIT TO `16-audio-engine.js`, `17-audio-tracks.js`, `C.MUSIC_LIMIT`,
  `C.LAYER_THRESHOLD`, `C.LAYER_CROSSFADE` OR `C.FILTER_*` IS A THREE-FILE
  EDIT** (both labs). ⛔ **A new `SFX_KILL_PITCH` voice is too** (`C`, BLOCK
  SFX, the `PITCH_*` tables); a new `C.SFX` event owes the lab a brief, an A
  label, 1–2 alternates and an in-context sequence. ⚠ **Every kit `VERSION`
  bump is a closed-file edit** — four files pin `AUDIO_VERSION`.

### Test hazards

- ⛔ **A CLOSED OVERDRIVE PLAY AT L11+ OWES ITS DRIVER FOUR THINGS**: jump at
  anything `aloft`; never target a `MimicShot`; go to a hovering token; steer to
  a ring in reach during a dive. ⛔ Repairs are the DRIVER or the FIXTURE, never
  the build, a lowered level or a relaxed assertion. ⚠ That driver holds fire
  and spends the Purge.
- ⛔ **A CLOCK READ IS A MOVER.** `Date.now` / `performance.now` have four
  readers (the time seed, `nowMs()`, a score row's `ts`, the achievements
  clock). Closed soaks before `-cs015-p4` fake `Date.now` per CALL;
  `-cs015-p4`, `-cs016-p4` and `-cs017-p4` fake WALL time.
- ⛔ **A TEST THAT IDLES 20 s AT REST ON THE TITLE ENTERS THE DEMO.**
  ⛔ **A STAGED RECORD CAN MASK A WRITE**: stage Start Depth records after a
  no-write window and assert the precondition. ⚠ **Start Depth options are
  ODD** (1, 3 … 29 at a record of 30): choose by ROW; an even value is row −1.
- ⛔ **MODE IS `OVERDRIVE`, THEN `CLASSIC`**; START DEPTH is built for
  `pendingMode`; a driver starting at the title spends two live steps first; a
  menu press is an EDGE — release every held key first; a replay that outlives
  its game over meets a live menu. ⛔ A fire-holder has no death path on levels
  1–4 and never spends a Ward.
- ⛔ **A DIVE'S LENGTH IS A PROPERTY, NEVER A STEP COUNT**; a run ending inside
  a dive leaves `dive.active` true until the RESTART press. ⛔ **Three Overdrive
  fixtures are seed-fragile** (`-cs012-p2`'s `SOAK_SEED`, `-cs012-p4`'s seed
  59, `-cs012-p6`'s `OD_CLOCK` 7927); anything that changes an Overdrive BEAT's
  length is a mover.
- ⛔ **A TEST THAT BOUNDS `state.shots` READS THE CAP IN FORCE** off
  `state.powers` (24 is reached). ⚠ **A `Shot`'s `depth` is a METHOD**
  (`depth()`); an entity's is a field. ⛔ A staged death in an Overdrive fixture
  clears `state.powers.ward` or sets up two hits.
- ⛔ **A MUTATION RUN THAT THROWS IS A DEFECT IN THE TEST**: assert the string
  is in the build exactly once before asserting red; pin only the ARGUMENT a
  claim is about.
- ⛔ **`test-cs008-p4.js` and `-p6.js` scan the WHOLE built file, comments
  included** (no `fillRect` / `strokeRect`, one text site; vocabulary by
  SUBSTRING, only `webkit` excepted from "web"); `test-cs002-p1.js` bans raw
  input reads outside `04-input.js`. ⛔ **Audio code**: no "atari", no platform
  RNG, never `audioFrame()` inside `frame()`; a new `heat(` call is red (use
  `heatT()`); `setState()` before the first gesture is dropped.
- ⛔ **`AudioSys.ctx` is null until a key, click or lifted touch**; a gesture is
  a DOM event, not `G.input.keyDown()`. ⛔ **The boot block runs inside the
  harness**; a `Store.set` spy sees only `p0`'s writes, and `p0`'s scope IS the
  root store. ⛔ A `Profiles.select()` staged on a play screen looks like a
  play-step write.
- ⚠ **`_harness.js`**: `{ stub }`, `{ spy }` (`.before` / `.after`),
  `{ mutate }` (throws unless its string is in the build exactly once); a
  function inside `Game`'s closure cannot be reached this way. An added
  `EXPORTS` row is a design decision a plan owes a line.

### The entities and the Dive, as shipped

- ⛔ **`startDive()` DOES FIVE THINGS** — `resetDive()`, the `anchored` filter,
  `resetJump()`, `resetTokens()`, `layRings()` last; a repeated dive re-lays
  the set; a dive pays exactly one thing, a ring, and spends zero draws.
- ⛔ **AN `aloft` ENTITY IS INVISIBLE TO THE SHOT PASS AND VISIBLE TO EVERYTHING
  ELSE.** ⛔ The Reaver is a `Vaulter` subclass and the `MimicShot` a
  `WeaverBolt` one. ✅ The Mimic is KEPT (S6) and still cuts in ONE ROW.
- ⚠ **`RING_POINTS`, `RING_ARC_LANES`, `RING_LANE_STEP` and the five dive
  visual values are PROVISIONAL**; `DIVE_TIME_OD` 4.0 and `DIVE_RINGS_MAX` 6
  are not (a ceiling may only come DOWN).
- ⛔ **Read GDD §6.5 before adding an entity** — nine contract fields, the
  wiring points, the Dive; neither a token nor a ring is on that table.

## Carried tasks — none of them ship's (S13)

- ⛔ **ONE LAB SESSION, when Paul wants it**: `drive`'s tiers, gains and tempo
  (PASS marks) and the FIVE unauditioned cues — `ringTake`, `ringMiss`, the
  Surger tone, the Warden's fuse and `unlock` (candidate A). Its own commit; it
  rewrites `test-cs012-p1.js`'s "no tier" and "no audition mark" assertions in
  place. ⛔ A re-pick is ported verbatim from the lab, never hand-tuned in `C`.
- **Backports**, each a separate manual step: kit-input 0.8.0, kit-menu 0.1.0,
  kit-fx, kit-audio 0.4.0, kit-leaderboard 0.2.1 (`lib/`); `createScores` is
  kit-scores' draft, `createAchievements` kit-achievements' (⚠ its screen is
  the game's wrapper).
- **The unowned list** (plan §0.2's table, carried with its reasons):

| Item | Why it was left |
|---|---|
| The pad-only silence (a pad press is not a user activation) | a platform rule; a line on the itch page is Paul's |
| The VOICE bus — an OPTIONS slider no recipe routes to | removing the row changes OPTIONS' pinned eleven-row shape: a design call for a patch |
| The Surger tone's 1.106 sample peak | a recipe changes only by a port from `tools/sfx-lab.html` — the lab session |
| The enemy / menu palette and the HUD sizes (⚠ provisional) | tuning, in `SKIPPED-PLAYTESTS.md` |
| `glow-lab.html` — never built (`CLAUDE.md`'s tools list says so since CS018 P1) | the audition is Paul's |
| F1 — the combo readout covers 10 of 233 rim lane-centres | accepted (CS013) |
| F2 — the lifted rim point off-screen on five lanes at the Jump's apex | accepted (CS013); a `JUMP_LIFT` or well change moves `PROMPT_Y`'s arithmetic |
| The four unreachable entity cases (a second Purge on a bolt or a reflection above 0.95; a run starting past 99; a rim Vaulter / aloft Warden on the continuous lane; a biggest-Purge of six) | no board reaches them; a patch that makes one reachable owns it |

- ⚠ **`CLAUDE.md` carries no telemetry rule**; whether it earns one is Paul's.

## The tools

- **`tools/perf-probe.js`** — Node + headless Chromium over the shipped
  `file://` build (`CHROME_BIN`, else Playwright's cache, else exit 2 with the
  reason): boot health, bytes per `draw()` (after 300 draws and at steady
  state) and uncapped frame cost at CPU throttle. DATA, never a gate;
  `run-all.js` never runs it; ~27 s. At the CS017 close: budget board **6,001 /
  4,979 B**, empty **1,228 / 131 B**; **4.3 ms p50 at 1×, ~18 ms at 4×**
  (software raster; raster-bound). ⚠ Its bytes are the JIT's as much as the
  source's: quote a reading with its warm-up; each board needs its own page.
- **`tools/reach-probe.js`** — the measurement behind `test-cs015-p3.js`'s
  `REACH` (above).
- `tools/music-lab.html`, `sfx-lab.html` (the porting sources, pinned by text
  identity), `well-lab.html` (⚠ its visual audition has not happened),
  `feel-lab.html`, `serve-lan.js`.

## Open questions (not blocking)

- ⚠ **Should the headroom gate (`test-cs009-p5.js`, D16's limiter-curve model,
  ⚠ provisional) also bound the limiter's INPUT?** Paul's call since CS012; it
  cannot catch a louder track (red needs an input of 152.8). Unanswered at ship.
```

## Version history

| Version | Date | What landed |
|---|---|---|
| **1.0.1** | 2026-09-23 | CS018 P1–P2: **the first post-ship patch.** GDD §19's ten coverage gaps each carried in `test-cs018-p1.js` (§17 item 7's full 1..200, `pulse`'s A→B→C, the stick at 36 deflections, the shot's throat fade, `dist/` slice-identical to `src/`, the week key under three pinned time zones, level 1's first seconds re-measured over 64 runs, `INT_ATTACK` / `INT_RELEASE` / `FILTER_MIN_HZ` / `FILTER_MAX_HZ` pinned, the page's markup and the package's `lib/` files in the vocabulary scan); `build.js` injects its script through `injectScript()`, a function replacement, `$`-safe, moving no byte; GDD §11.7's `pulse` B restated to the table; `CLAUDE.md`'s `glow-lab` line corrected. **OPTIONS' VOICE VOLUME row cut** (Q2-A): ten rows, kit-audio's `voice` bus kept at unity and moved by nothing, `settings` v1 with no `migrate` — a 1.0.0 row loads whole but for its ignored `voice` (`test-cs018-p2.js`, whose mutant is the 1.0.0 build); five closed files repaired in place. `C.GAME_VERSION` "1.0.0" → **"1.0.1"**, `package.json` aligned, the itch zip rebuilt (287,515 bytes). ⛔ No simulation baseline moved in either phase. |

## Carried forward (not CS018's to fix)

The post-ship `STATUS.md` carries each with its reason: Paul's actions outside
the repo (the repository private, the itch page's copy, uploading the 1.0.1
zip); Paul's one lab session (`drive` and five cues); the kit backports; the
unowned list (the pad-only silence, the Surger tone's 1.106 peak, the palette
and HUD sizes, `glow-lab`, F1, F2, the four unreachable entity cases); §19's
other at-ship ⚠ notes (plan §0.2); whether `CLAUDE.md` earns a telemetry rule;
and whether the headroom gate should bound the limiter's input.
