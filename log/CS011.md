# log/CS011.md — Meta: profiles, persistence, local scores, the leaderboard

Narrative build log for CS011, **closed 2026-09-16**: the kit inlined at build,
the store and the silent profile (P1), what a profile keeps (P2), the local top
10 and the run's end (P3), the profile screens and NAME (P4), the online board
(P5), and the ninth soak plus the close (P6). Planned at `52ba439`; Paul answered
M1–M9 in the planning session. Each phase appended its reasoning here as it
went. At the close the phases were read together; two stale method names, one
missing method and two stale status lines were corrected in `CLAUDE.md`, the GDD
and `ROADMAP.md` (P6, review notes).

---

## Phase ledger (moved from `STATUS.md` at the close)

| Phase | Commit | One line |
|---|---|---|
| Planning | `8355b77` | Measured at `52ba439`. M1–M9 answered by Paul; R1–R22 flagged; every claim MEASURED or PREDICTED |
| P1 | `6fefcd1`, `6cda16c` | `KIT_INLINE` + `wrapKitModule()`, `Store` / `Profiles` / `Meta.boot()`, the harness's `store` / `storage` / `crypto` / `mutate` / `storageReads`, the CS015 renumber. `test-cs011-p1.js` (62). Paul accepted the `lastUsed` finding. 8 of 8 red |
| P2 | `640a288` | `settings` / `progress` / `telemetry` per profile, `Meta.boot(Game.settingsHooks)`, `Profiles.select()`, `Telemetry.snapshot()` / `restore()`. `test-cs011-p2.js` (135). 13 of 13 red |
| P3 | `3409a9c` | `createScores` + `src/22-meta.NOTES.md`, the run's three seats, the bench flag, `Meta.eligible()`, SCORES, game over's third line. `test-cs011-p3.js` (83). 19 of 19 red |
| P4 | `179592c`, `4ebaf47` | kit-input 0.8.0's `captureText`, `Profiles.create` / `rename` / `remove`, PROFILE, a profile's page, DELETE, NAME. `test-cs011-p4.js` (103). Kit-first delete kept by Paul. 22 of 22 red, plus five in-file |
| P5 | `82df3ea` | kit-leaderboard 0.2.1, `Leaderboard`, `Profiles.player()`, the submits, SCORES' VIEW and ONLINE, the queued line. `test-cs011-p5.js` (89). 11 of 12 red (M8 unobservable) |
| P6 | this commit | The ninth soak (working vs blocked storage, one hash; a reload), the review, the docs, the close. 10 of 10 red |

---

## P1 — the kit inlined at build, the store, the silent profile

**What shipped.** `build.js`'s `KIT_INLINE` (kit-names, kit-storage,
kit-profile, after `20-achievements.js`), `wrapKitModule()` and a
requirable `build.js`. `22-meta.js`'s `Store`, `Profiles` (`scope()`,
`current()`, `list()`) and `Meta.boot()`, called from the boot block before
`Game.start()`. `C.PROFILE_MAX` 8. The harness's `store`, `storage: "blocked"`,
`crypto` and `mutate` options, `_env.storageReads`, and six new exports.
`test-cs011-p1.js` (62 assertions). Three closed files rewritten in place, as
the plan measured. The CS015 renumber, the Save-data rule rewording, and CS010's
plan docs moved to `archive/`. Build: 24 modules + 3 kit, 547.5 KB (variant B
predicted 544.4; the difference is `22-meta.js`'s comments and code).

**The wrapper keeps line identity.** Each rewrite is one line for one line: an
`export const|function` loses its keyword, an `import { … } from` becomes
`const { … } = KitNames;`, and an `export { … } from` becomes a comment, with the
names read off the namespace in the `return`. So the test compares the inlined
body with its `lib/` file line by line, and every difference must be one of the
three rewrites. `KitProfile.NAME_CHANGE_NOTICE` is `KitNames.NAME_CHANGE_NOTICE`
itself, which is the reference identity the kit promises. The wrapper also
refuses a namespace import, a multi-line export, `export default`, a dynamic
`import(`, and an import from a module not inlined above it. Each is a fixture
in the test.

**The banner is a dash rule, not the module banner.** Seven closed tests slice
the built file on `// ={74} / // <file> / // ={74}`, and `test-cs002-p1.js`
checks that slicing both ways against `src/`. An equals-sign banner for
`lib/kit-names/kit-names.js` would read as a module that is not in `src/`. With
the dash rule the three bodies fall inside `20-achievements.js`'s slice. None of
those scans' banned tokens is in them (the plan's variant A, and green here).

**Store and profile are made in `Meta.boot()`, not when the script is
evaluated.** Evaluating the script touches no storage. `Store` is a `let`, null
until boot. A second `boot()` returns early, because two `KitStorage.create()`
calls over one keyspace have separate declaration tables (the kit calls that a
footgun).

**FINDING: `select()` after `createAnonymous()` is a no-op, and the plan
predicted otherwise.** The prompt says `createAnonymous()` then `select()` leaves
`lastUsed` naming the profile and a `playerId` minted. MEASURED: on an empty
install kit-profile's boot leaves `activeId` at the legacy id `p0`, and
`createAnonymous()` creates `p0`. `select("p0")` then hits `if (id ===
this.activeId) return true;` (`kit-profile.js:550`), which saves nothing and
mints nothing. The stored roster after boot had `"lastUsed":""` and
`"playerId":null`. The same holds for the non-ANONYMOUS id, because seq starts
at 0.
- **What the build does:** it keeps the `select()` as the prompt says, then calls
  `kit.current()`. The kit documents that call as a mint path, so the stored
  `playerId` is a v4 UUID at first boot.
- **What it cannot do:** store `lastUsed` without editing `lib/`, which is
  forbidden. Renaming `legacyProfileId` would make `select()` a real switch, but
  it would also move `p0` off the root store, which R12 and the GDD rely on.
- **Why the behaviour is unchanged:** a reload's `activateLoaded()` falls back
  to `roster[0]`, which is the same profile, and the test asserts that reload.
  The test asserts "selected" through `current()` and the reload, never through
  the `lastUsed` byte. Recorded for a kit reviewer in `kit-profile.NOTES.md`.

**The blocked harness.** `window.localStorage` gets a throwing getter, and
kit-storage's probe wraps exactly that read. The Function parameter
`localStorage` is passed `undefined`, because evaluating the getter to pass it
would throw in the harness. Nothing outside the kit names it
(`test-cs011-p1.js` scans for that).

**The `mutate` option.** The `legacyRosterKey: ''` mutation has to run against
the real boot. The alternatives were calling `KitProfile.create` directly, which
proves the kit and not `Meta`, or writing a mutated `dist/`, which a crash would
leave behind for the next test. `buildGame({ mutate })` replaces text in the
evaluated script, and each `from` must be found exactly once or it throws.

**The closed-file rewrites (plan §1.2 B), exactly the three.**
- `test-cs008-p6.js:375`: the store's full key/value snapshot before the first
  `t` press equals the one after. The snapshot is one fixture line added above.
- `test-cs008-p7.js:436`: `Profiles.scope().has("settings")` is false.
- `test-cs007-p3.js:382`: the message string CS016 → CS017.
No other closed assertion went red.

**The renumber sweep.** Re-grepped live files for `CS015` / `CS016`: STATUS ×3,
ROADMAP ×5 (the two rows, `:128`, `:335` twice, `:407`), `DECISIONS.md:81`, GDD
`:1383` and `:1634`, `src/23-main.js:462`, and the test string. ROADMAP's CS011
row and §21 #4 no longer claim achievements. GDD §17 item 10 and §19's Meta line
name CS015. ROADMAP gained a third-shift note beside its two earlier renumber
notes. §19's Meta line also said "`keyFor` routing", the same stale rule R3
replaces, so it now says `scope()`.

**Left for P2, not touched here.** `src/21-telemetry.js:22` and
`src/00-config.js`'s Telemetry group comment still say `Profiles.keyFor` and "no
Profiles in the build". P2 owns telemetry persistence and rewrites both.

**Mutations, by hand, each restored (8 of 8 red).**

| # | Mutation | Red |
|---|---|---|
| M1 | `legacyRosterKey: ''` in `22-meta.js` | `test-cs011-p1.js`, through its `mutate` fixture's throw (the text is already `''`). The in-file mutant proves the planted-roster assertion goes red |
| M2 | no `kit.current()` at first boot | the v4 `playerId` assertion (`got null`), which confirms the `select()` no-op |
| M3 | no `Meta.boot()` in the boot block | `Store` is null |
| M4 | no `createAnonymous()` / `select()` | `current()` is null, so the test throws |
| M5 | `22-meta.js` names `sessionStorage` | the outside-the-kit scan |
| M6 | `KIT_INLINE_AFTER = "21-telemetry.js"` | the order assertion, and `test-cs007-p4.js`'s telemetry slice |
| M7 | the wrapper's catch-all removed | the `export default` refusal |
| M8 | `Store.usage()` at boot | `_env.storageReads` (got 3) |

**Baselines.** `test-cs006-p2.js` is green at **1229033515**, and `GOLDEN_LANES`
is green too. Suite: 55 files, zero skips, 74 s.

**After the commit: Paul accepted the `lastUsed` finding** (2026-09-16). The
stored byte stays `""`, `lib/` stays unedited, and `legacyProfileId` is
unchanged.

## P2 — what a profile keeps

**Built.** `settings` v1 (`{ controls: { mouse, touch, autofire, mirror, keys,
pad }, sound: { master, music, sfx, voice, track } }`, `track` by name),
`progress` `{ highestCleared }` and `telemetry` (`{ rows, wrapped }`, rows as
arrays), all through `Profiles.scope()` in `22-meta.js`. `23-main.js` hands
`Meta.boot()` `Game.settingsHooks`: `resetSettings()` (`resetControls()` +
`resetSound()`, writing nothing), `applySettings(data)` and `settingsSnapshot()`.
`Meta.saveSettings()` is called at five sites: the adjust step when the clamped
value moved, the mirror and auto-fire toggles, a capture that bound, and RESET TO
DEFAULTS. `21-telemetry.js` gains `snapshot()` / `restore()` and still names no
storage. `Meta.runEnded(outcome)` is a stub with no caller; its body is the
telemetry write.

**How the load is made per field.** `applySettings()` skips a field that is
missing or invalid, and Meta's `activateSettings()` calls `resetSettings()` first.
So "else default" is the reset's job, and the mutation that drops the reset is
exactly a bleed. Boot uses the same function: the reset there is a no-op over the
evaluation-time defaults. A binding page is checked whole: two slots per action,
not both null, a key a non-empty lowercase string and not `reservedKey`, a button
a whole number ≥ 0 and not Start, nothing seen twice. The kit's own setter then
runs inside a `try`. It refuses what the page check does not name (`gamepadleft`),
and a refused page stays at its defaults. MEASURED: dropping the game's duplicate
check leaves the duplicate cases green, because the kit refuses them too.

**Readings taken (Claude's; flagged in `STATUS.md`).**
- **`t` in play.** The prompt says both "write when capture turns off" and
  "never inside a play step". `t` in play is both. The play-step rule wins, so
  the toggle writes only off play, and the rows wait for the next seat
  (`autoPause`, a switch, the run's end). `autoPause` writes after `pauseRun()`,
  so its step is already a pause step. The mutant that writes at once is red.
- **The ring on a switch.** `beforeChange` writes the outgoing rows. `change`
  empties the ring, and with capture on it reads the incoming profile's rows.
  Without that, the next seat would write profile A's rows under profile B.
- **`Profiles.select(id)`** is added to the wrapper. It is the switch R11 wires,
  and the test's two-profile cases need it. `create` / `rename` / `remove` stay
  P4's, so the test plants a two-profile roster.
- **The telemetry read in play.** `t` turning capture on over an empty ring reads
  inside a play step. The rule forbids writes, and deferring the read would let
  the ring fill and then overwrite the stored rows.

**The closed file, in place.** `test-cs008-p7.js:436` now compares the stored
`settings` to the shipped defaults. Two comment phrases in that file said
"nothing is stored" and were corrected with it. No other closed assertion went
red. GDD §11.7's "Session-only until CS011" pointer was also corrected, beside
the §4.6, §10.5 and §15.6 edits.

**Mutations (13 of 13 red).** Two are in-file (`mutate`): load without the reset
(p1 keeps p0's MUSIC 4) and `t` off writing at once (a write on `play`). By hand,
each restored:

| # | Mutation | Red |
|---|---|---|
| H1 | no save after a capture binds | "a capture that bound saves at once", key and pad |
| H2 | no save on RESET TO DEFAULTS | `test-cs008-p7.js:436` |
| H3 | no save on the adjust step | "an adjust step that moved a value saves" |
| H4 | no save on LEFT-HANDED TOUCH | "LEFT-HANDED TOUCH saves at once" |
| H5 | `restore()` without the row-length check | "one row a column short restores empty" (got 37) |
| H6 | no read when capture turns on | the capture-on read |
| H7 | no read on EXPORT | the round trip and the fresh-store control |
| H8 | no `beforeChange` write | the switch writes outgoing rows first |
| H9 | `noteCleared` writes `Store`, not the scope | p1's record at `p1.progress` (throws) |
| H10 | `Game.reset()` saves | "Game.reset() changes no stored byte", and 19 more |
| H11 | `mirror` loaded when not `undefined` | the planted `1` throws in `configure()` |

The first pass had two survivors, H1 and H8. Later saves hid H1, and nothing
switched with rows held for H8. The test gained an assertion right after each
capture and toggle, and a switch case with capture on.

**Baselines.** `P1_DETERMINISM_HASH` **1229033515** and `GOLDEN_LANES` are
green. Nothing moved. Suite: 56 files, zero skips, 73 s. `SKIPPED-PLAYTESTS.md`:
none, since there is nothing to hear or see.

## P3 — the local top 10 and the run's end

**Built.** `createScores({ store, key, perMode, modes })` in `22-meta.js`, which
returns `qualifies` / `add` (rank or 0) / `list`, and its draft doc
`src/22-meta.NOTES.md`. It is created once, in `Meta.boot()`, over the root store
with `C.SCORES_PER_MODE` 10. `Meta.runStarted` / `benchUsed` / `eligible` /
`runOpen` / `runEnded` / `scores` / `lastPlace` are added; the run's record is
`Meta`'s closure (`run`, `placed`). The three seats are the last line of
`startGame()`, the first line of `quitToTitle()` (on pause), and `frame()` after
the step loop. The bench flag is at the top of `runAction()`. The title gets
SCORES as its third row, there is a new `SCREENS.scores` screen with
`buildScoreRows()`, and game over gets a third line. The harness exports
`Scores` and `createScores`. `test-cs011-p3.js` has 83 assertions. Build: 565.6
KB.

**Readings taken (Claude's).**
- **`runEnded()` does nothing when no run is open, the telemetry write
  included.** Without that guard, the game-over seat would write telemetry on
  every frame. The cost: a board started with `Game.reset()` and no
  `startGame()` opens no run, so quitting it from pause writes no rows. Every
  run a player can start comes through `startGame()`.
- **`Game.reset()` leaves the open run alone.** The plan gives the drop to
  `startGame()` only.
- **`modes` is the literal `["classic", "overdrive"]` in `Meta.boot()`**, not a
  `C` entry. It lists `state.mode`'s two values and is not a tunable.
- **`createScores` throws on an undeclared mode**, as kit-storage does on an
  undeclared key, and sorts on read with a stable sort, so a planted unsorted
  table loads in order.
- **A SCORES row has `action: null`.** Fire on it returns nothing, so it makes
  no sound and does nothing. Rotate scrolls.
- **Game over's third line is written in `draw()`** like its other two. It
  reads `Meta.lastPlace()`, a closure value, and does no storage read. SCORES'
  rows are built on entry, as the prompt says.

**MEASURED hazard: `frame()`'s text.** A comment saying "BEFORE audioFrame()"
inside `frame()` turned `test-cs009-p3.js` red ("frame() calls audioFrame()
exactly once"), because it counts that text. The comment now says "the audio
frame".

**The staged fixtures.** The last-life clear is `test-cs008-p2.js`'s staging (a
Vaulter shot on the fire tick plus a Weaver bolt, lives 1, level 7), driven
through `Game.frame()` so the seat runs. The Dive death is every lane thorned
plus a cleared well at level 9. Its strike is the only death, and the test
counts `runEnded` calls across 120 frames after the freeze. Quit, game over's
QUIT, RESTART and the bench keys all run through the front door.
`state` is hashed, and `state.rng` wrapped to count draws, around every `Meta`
method. The wrapper sits on the object, so every internal `Meta.x()` call
reaches it: 7,209 calls, none moved `state`.

**Mutations (19 of 19 red).** Two use `mutate` in the test file itself: the
record moved into `killSkimmer()` (the row reads 150, the final score is 1,350),
and the gate ignoring the flag (the bench-touched run records). The rest were
done by hand, each restored:

| # | Mutation | Red |
|---|---|---|
| M1 | the `'died'` seat disabled | "the frame that ended the run recorded one row" |
| M2 | no `'quit'` seat | the `'quit'` row |
| M3 | the `'quit'` check after the overwrite | the `'quit'` row |
| M4 | `runEnded` does not close the run | "the frames after it record nothing more" (10 rows) |
| M5 | no `runStarted()` | the last-life row |
| M6 | no `benchUsed()` | all eight bench keys |
| M7 | `t` flags | "`t` and `e` leave the run eligible" |
| M8 | a tie goes above | the tie rule |
| M9 | no `score > 0` | zero and negatives |
| M10 | no cap | the 10th-place tie, and game over's line |
| M11 | `placed` not reset on a run start | RESTART's `lastPlace` 0 |
| M12 | `runEnded` writes `state.purgeLatched` | the `state` hash |
| M13 | `buildScoreRows()` in `draw()` | "rebuilt on entry, never in draw()" |
| M14 | the line shown whatever the placing | the bench runs' empty line |
| M15 | no SCORES row | the title's rows |
| M16 | `profileName` not stamped | the name at the time |
| M17 | `Store.usage()` in `scores()` | `_env.storageReads` (13) |

The first pass of M1, M2, M3 and M5 went red by throwing (a missing row was
dereferenced). The test now guards those reads, so each fails on a named
assertion.

**Baselines.** `P1_DETERMINISM_HASH` **1229033515** and `GOLDEN_LANES` are green.
Nothing moved, and no closed file was edited (plan §9: P3 predicted none).
Suite: 57 files, zero skips, 76 s. `SKIPPED-PLAYTESTS.md`: none. SCORES'
layout was measured by the plan (R15) and has nothing to judge by ear.

## P4 — the profile screens and name entry

**Built.** kit-input **0.8.0**: `captureText(cb)` and its `.NOTES.md` entry.
`Profiles.create` / `rename` / `remove` and the kit's notice and name length on
`Profiles`. `C.NAME_WHEEL` and `C.NOTICE_WRAP` 60. In `23-main.js`: the title's
PROFILE row, `SCREENS.profile` / `profilePage` / `profileDelete` /
`profileName`, `openProfiles()`, `stepName()`, one edit path for the wheel and
the keyboard, and the text mode armed in `syncScreen()`. `test-cs011-p4.js` has
103 assertions. Build: 578.7 KB.

**⚠ FINDING, AND A READING THAT REVERSES R12's WORDING: the kit's `remove(id)`
goes first, then the three `remove(key)` calls.** R12 and the prompt say keys
first. MEASURED with the order as written, as an in-file mutation:
- the last profile's DELETE is refused (`last_profile`), but only after its
  `settings` and `progress` are gone;
- deleting the active `p0` with capture on runs the kit's switch, whose
  `beforeChange` writes the outgoing telemetry rows back into the root store
  after they were removed. They stay.

The plan's outcomes, a refused delete that deletes nothing and all three keys
removed, hold only with the kit first. kit-profile's `scope(id)` answers for a
removed id (no membership check), so the order costs nothing. Paul can reverse
it if he disagrees. **Paul went ahead with the built order, 2026-09-16.**

**Readings taken (Claude's), none player-facing beyond presentation.**
- **Flow.** SELECT and a committed NEW both return to PROFILE. A committed
  RENAME returns to the page. A cancel returns to where NAME was opened from.
  A deleted profile returns to PROFILE. A refused delete stays on DELETE,
  with the reason as its second line.
- **Titles.** The page is titled with the profile's name. DELETE's first line
  names the profile. NAME's title is NEW PROFILE or RENAME.
- **A reason line is the kit's reason uppercased with `_` as a space**, one
  rule for all four: INVALID NAME, NAME TAKEN, ROSTER FULL, LAST PROFILE.
- **NAME starts empty on RENAME too**, and the wheel starts on A, wraps, and
  stays on its entry after a Fire.
- **The buffer caps at kit-names' `MAX_NAME_LENGTH` (12).** Uncapped, a typed
  line outgrows `WORLD_W` at 69 characters. Validation is still the kit's.
- **The cursor mark is `|`**, because `_` is a name character. Space shows as
  SPACE on the wheel. The notice is shown verbatim, mixed case, wrapped once.
- **Sounds:** a wheel step plays `menuMove`; an append or commit `menuConfirm`;
  a delete or cancel `menuBack`. A typed key sounds as its wheel twin, and
  nothing sounds when nothing changed.
- **The text mode arms at the first step on NAME** (`syncScreen()`, the one
  place a screen change is noticed), not inside `menuAction()`.
- **In the kit:** a typed key's auto-repeat is not typed, and a key held when
  the mode arms is not a press, both by the capture path's rule. Events go out
  in `sample()`. The later of a capture and a text mode wins, and every
  `captureText()` call empties the queue.
- **SELECT stays enabled on the active profile** (a kit no-op).

**MEASURED consequence (in GDD §9.5 and STATUS):** on NAME, Space, Z, Shift and X
type or do nothing. A keyboard player types; the test's keyboard wheel rebinds
Fire to ArrowDown.

**Mutations.** Five are in the file: the text mode never armed (the kit's
named actions and all four fields; in the game the struct, a spawn, the well
and telemetry), R12's written order twice, and `clear()` in place of the removes
(scores and profiles gone, `storageReads` > 0). By hand, 22 of 22 went red, each
restored:

| # | Mutation | Red |
|---|---|---|
| M1 | Shift not swallowed | "Shift holds no purge" |
| M2 | typed key not swallowed | auto-repeat typed twice |
| M3 | `captureText` keeps a capture | "disarms a capture" |
| M4 | `captureNext` keeps the text mode | "z fires after it" |
| M5 | a re-arm keeps queued keys | "re-arming drops a key" (first pass survived; assertion added) |
| M6 | `reset()` ends the mode | "survives reset()" |
| M7 | `captureText(null)` does not end it | "z fires again", and section 9's `t` |
| M8 | NEW PROFILE never disabled | "disabled at C.PROFILE_MAX" |
| M9 | a new profile not selected | Fire on END commits |
| M10 | no buffer cap | "the buffer stops" |
| M11 | the wheel clamps | "left of A is END" |
| M12 | Purge never cancels | the pad's cancel |
| M13 | Escape ignored on NAME | "Escape cancels NEW" (first pass crashed; driver guarded) |
| M14 | `telemetry` not an own key | p0's root telemetry |
| M15 | no title detail | the PROFILE row |
| M16 | the notice unwrapped | the wrap, and "spans -1.7..1281.7" |
| M17 | no entry guard on NAME | "a Purge on NAME's entry step" (first pass survived; assertion added) |
| M18 | SELECT does not select | SELECT's activation |
| M19 | reasons verbatim | INVALID NAME |
| M20 | YES first | "NO comes first" |
| M21 | `OWN_KEYS` empty | "the deleted profile's keys are removed" |
| M22 | the notice on NEW too | NAME's empty lines |

**Closed-file edit, in place:** `test-cs011-p3.js:380`, the title's rows gain
PROFILE, as `STATUS.md` said P4 would (plan §9 predicted none besides; MEASURED
none). **Baselines.** `P1_DETERMINISM_HASH` **1229033515** and `GOLDEN_LANES`
green; nothing moved. Suite: 58 files, zero skips, 76 s.
`SKIPPED-PLAYTESTS.md`: one entry, NAME on hardware.

## P5 — the online leaderboard

**Built.** kit-leaderboard **0.2.1** (`mintRunId()`, kit-profile's
`mintPlayerId` body line for line; `.NOTES.md` entry, `endpoint` in the
example, the vendoring line corrected to `main` @ `f0b0eb2`; `EXTERNAL-FILES.md`;
a `DECISIONS.md` pointer). `C.LEADERBOARD_ENDPOINT`, `C.LEADERBOARD_BOARD_LIMIT`
10 and `C.PROFILE_ANONYMOUS_NAME`. `Leaderboard` in `22-meta.js`,
`Profiles.player()`, the two Meta seats. SCORES' VIEW row and ONLINE view,
`onBoard()`, the title's `refreshTitleLine()`. `Leaderboard` in `_harness.js`'s
`EXPORTS`. `test-cs011-p5.js`: 89 assertions. Build: 586.6 KB.

**Readings (Claude's, flagged; none changes what a run does).**
- **`C.PROFILE_ANONYMOUS_NAME` "ANONYMOUS" is passed to kit-profile as
  `anonymousName`.** That is the kit's default value, so nothing moves. The
  kit exports no such constant, and M9's hint must compare against the name the
  kit wrote. One source beats a string typed twice.
- **VIEW is the FIRST row, and every entry from the title opens LOCAL.** The plan
  names the switch, not where it sits. First keeps it reachable without
  scrolling past ten rows, and opening LOCAL means no network read unless one is
  asked for. With no module SCORES is P3's screen exactly (`test-cs011-p3.js`
  untouched).
- **The `*` is on the score (the detail)**, so it ends the drawn row. PREDICTED
  width: P3's measured 409 px worst row plus one glyph (18.6 px) leaves 32 px
  of `MENU_COL_W` 460. The hint is 41 characters, about 763 px.
- **`Leaderboard` is lazy on EVERY call until it finds the global**, never
  caching an absence: the bridge is an async module and can land after boot.
  A throwing `create()` is not retried.
- **`beginRun()` runs for bench runs too** ("one per `startGame()`"); a bench
  run just never submits.

**MEASURED.** The registry at coinless-kit `f0b0eb2` (`git show`, not the
working tree) lists all seven keys; the test evaluates that file and compares
the key sets, so no list is copied. Node 24 `require`s the ES module (with a
typeless-package warning, silenced in the test). With `randomUUID` hidden, 0.2.0
throws `crypto.randomUUID is not a function`.

**Mutations.** Two in the file: the stale token removed (the older answer is
drawn) and 0.2.0's `beginRun` in a copy of the module (throws, nothing posts).
Against `lib/` itself, 0.2.0's body turned 7 assertions red. By hand, 11 of 12
went red, each restored:

| # | Mutation | Red |
|---|---|---|
| M1 | submit without the gate | "a bench run begins and submits nothing" |
| M2 | VIEW row without the module | "SCORES has no VIEW row" |
| M3 | no title refresh | section 2 crashes: the client never made |
| M4 | an eighth stats key | "exactly the registry's statsFields" |
| M5 | `durationS` unrounded | "durationS is an integer" |
| M6 | no M9 hint | "LOADING…, and M9's hint" |
| M7 | no `beginRun()` | "one beginRun per startGame()" |
| M8 | `onBoard()` ignores the view | ⚠ **survives** |
| M9 | no `*` | "a flagged row ends in `*`" |
| M10 | a rejection never answers | "COULD NOT REACH THE BOARD" |
| M11 | no singular | "one queued: singular" |
| M12 | the client never cached | "a late module is created on the next title step" |

⚠ **M8 survives, and it cannot be seen.** SCORES rebuilds on entry and on VIEW,
so an answer rebuilt while the view is LOCAL or the screen is elsewhere draws
nothing different. The guard stays as a guard; no assertion claims it.

**Closed files:** none edited (plan §9 predicted none, MEASURED none).
**Baselines.** `P1_DETERMINISM_HASH` **1229033515** and `GOLDEN_LANES` green;
nothing moved. Suite: 59 files, zero skips. `SKIPPED-PLAYTESTS.md`: one entry.

## P6 — the ninth soak, the review, and the close

### `scratchpad/test-cs011-p6.js` (50 assertions)

⛔ **A ninth file; `test-cs010-p5.js` is untouched.** It takes `test-cs009-p6.js`'s
drive and whole-board hasher unchanged, and `test-cs011-p5.js`'s recording fake
leaderboard, set on `X._env.win` before the first frame. Every press goes through
`Game.input` and `Game.frame()`, from the boot title after two live steps. The
script, in both sessions:
- PROFILE → NEW PROFILE, `soak` typed, Enter (SOAK is created and selected as
  `p1`), Escape;
- OPTIONS → MUSIC VOLUME two steps down, Fire; CONTROLS → MOUSE SENSITIVITY two
  steps up, Fire; Escape twice;
- `noteCleared(23)` staged on SOAK, so the list reaches Start Depth 23;
- Start Depths 1, 13 and 23: a run to game over, RESTART, a run to game over,
  QUIT TO TITLE. Run 0 of 13 gets a `1` at step 600. Run 1 of 23 is quit from
  pause at step 900 (Space released first, Escape, two rights, Fire).

**Reading: the profile and the settings come first.** A switch resets the
settings, so settings moved before SOAK existed would stay on ANONYMOUS and the
reload could not show them on SOAK. The staged record is written after the switch
for the same reason.

**Reading: "the Start Depth list reaching the highest level the session
cleared".** A run clears its levels in order from its Start Depth, so its highest
cleared is `startDepth + wellsCleared − 1`, read off `state` at each run end, and
the session's is the largest of those and the staged 23. The first draft
observed `noteCleared()` calls instead. Deleting the clear edge's write then
removed the observations too, and only a non-vacuity line went red (M8 below),
so the board count replaced it.

**Reading: "one local row per eligible run that placed".** Every run end meets a
table with room (asserted), so an eligible run with a score above 0 places. At
each `runEnded` the wrapper records whether the run was open and eligible, the
score, the table length and the submit count. After the call it records the
same counts and the row at `lastPlace()`. That row must carry the score, the
outcome and SOAK.

**Measured (seed 20260922).** Each session ran **97,322 frames** and 46,668 play
steps. Levels 1–8, 13–20 and 23–29. The whole file takes about 11 s.

| Claim | Working session |
|---|---|
| Frames whose hash differs, blocked vs working | **0** of 97,322 |
| Blocked session's Map | empty |
| `_env.storageReads` (session; reload) | **0**; 0 |
| Stored keys | `profiles`, `scores`, `p1.settings`, `p1.progress` |
| `Meta` calls, `state` moved | 2,153, **0** |
| Run ends (outcome, eligible, score) | died ✓ 21,675; died ✓ 25,090; died ✗ 69,255 (bench); died ✓ 81,390; died ✓ 155,635; quit ✓ 3,455 |
| Local rows, submits | 5, 5 (all under SOAK) |
| Highest cleared; reload's list top | 28; 27 |
| MUSIC VOLUME, MOUSE SENSITIVITY after reload | 8 of 10, ×1.2 |

**Mutation record — 10 of 10 red.** Each applied to `src/`, built, run against
`test-cs011-p6.js` alone, and restored.

| # | Mutation | Red |
|---|---|---|
| M1 | `startGame()` adds a life when `!Store.available` | 5: frame counts, the hash (first at frame 180), steps, run ends, the table |
| M2 | submit without the gate | 4: one submit per eligible end (5 of 6), none elsewhere, outcomes, scores |
| M3 | no `Meta.benchUsed()` | 3: the one bench run, its non-vacuity, the seats called |
| M4 | `runEnded` flips `state.purgeLatched` | 1: `state` moved on 6 calls |
| M5 | no save on the adjust step | 3: `p1.settings` absent, the save not called, the reload's settings `[10,10]` |
| M6 | `Store.usage()` at a run end | 1: `storageReads` 54 |
| M7 | no `'quit'` seat | 3: five ends, no quit, the outcomes |
| M8 | no `noteCleared` at the clear edge | 2: the reload's record 23 (want 28), the list 23 (want 27). **Green but a non-vacuity line on the first draft**, which observed the calls |
| M9 | a new profile not selected | 12: SOAK not `p1`, rows stamped ANONYMOUS, … |
| M10 | a `coinless.lb.queue` write at a run end | 2: the declared-key scan, the `coinless.lb.*` check |

Not a mutant: a `set()` on an undeclared key **throws inside kit-storage**, so no
stray key can come through `Store`. A declaration nobody writes stores nothing.
The declared-key scan exists to catch writes that go around the store (M10).

### ⛔ The baseline ledger — plan §9, measured at the close

`git diff --stat 52ba439..HEAD -- scratchpad/test-cs006-p2.js
scratchpad/test-cs004-p1.js scratchpad/test-registry.js` is empty.
`P1_DETERMINISM_HASH` stands at **1229033515**, `GOLDEN_LANES` is untouched, and
`STATE_FIELDS`, `tracks` 2, `enemies` 6 and `enemyKinds` 9 did not move. All
three files run green. kit-input moved 0.7.0 → **0.8.0** (P4), kit-leaderboard
0.2.0 → **0.2.1** (P5). `lib/kit-storage`, `lib/kit-profile` and `lib/kit-names`
code is unmodified (`git diff --stat 52ba439..HEAD -- lib/` touches only
kit-leaderboard's two files and `kit-profile.NOTES.md`). **No baseline moved in
any phase**, as the ledger predicted.

### ⛔ The files CS011 edited but did not create

| File | Phase | Cause |
|---|---|---|
| `test-cs008-p6.js:375` | P1 | the telemetry toggle stores nothing: a store snapshot before and after |
| `test-cs008-p7.js:436` | P1, P2 | RESET TO DEFAULTS: no stored `settings` (P1), then the stored settings equal the defaults (P2) |
| `test-cs007-p3.js:382` | P1 | the renumber's message string, CS016 → CS017 |
| `test-cs011-p3.js:380` | P4 | the title's rows gain PROFILE. ⚠ Plan §9 predicted no closed-file edit in P3–P5; P3's STATUS entry named this one |
| `_harness.js` | P1, P3, P5 | the storage options and `storageReads`; EXPORTS +6, +2, +1 |

⛔ No assertion was relaxed or deleted.

### ⛔ Acceptance criteria — plan §10

1. ✅ The kit carried line for line but for the three rewrites; an unhandled form
   fails the build (`test-cs011-p1.js`, M7).
2. ✅ One selected ANONYMOUS profile at first launch, the same after a reload,
   and blocked storage plays (`-p1.js`; played, `-p6.js`).
3. ✅ No `length` or `key(i)` read at boot, over every CS011 test's builds, and
   across a played session (`-p1.js` to `-p4.js`, `-p6.js`).
4. ✅ Every settings page and the record survive a reload per profile,
   known-value-else-default, and the telemetry switch never does (`-p2.js`; the
   played reload, `-p6.js`).
5. ✅ Reset before load, with the mutation (`-p2.js`).
6. ✅ A top 10 per mode, both outcomes, a last-step clear's final score, nothing
   from a bench run (`-p3.js`; played, `-p6.js`).
7. ✅ One gate for both, never two submits (`-p3.js`, `-p5.js`; played, `-p6.js`).
8. ✅ SCORES LOCAL and ONLINE, game over's placing, the queued line (`-p3.js`, `-p5.js`).
9. ✅ PROFILE's four operations; the wheel on keys and pad, typing, no action
   from a typed key (`-p4.js`).
10. ✅ A v4 run id without `randomUUID`; the game plays with no module (`-p5.js`).
11. ✅ Rows as arrays across a reload, rejected on version or length (`-p2.js`).
12. ✅ `Meta` writes no `state` (`-p3.js`, `-p6.js`), and blocked matches working
    on every frame (`-p6.js`).
13. ✅ Achievements are CS015's in `ROADMAP.md`, the GDD and `STATUS.md`;
    `src/20-achievements.js` is still its one-line placeholder.

### Plan §11–§13 at the close

- §11 held: no achievements, no Overdrive board, no registry change, no
  time-window switch, profile filter or erase, no save-and-resume, no change to
  kit-storage, kit-profile or kit-names code. R2's pad-only silence, the VOICE
  bus, the Dive's visual and the tone's 1.106 are still unowned.
- K1 held (P1's fixtures). K2: the `clear()` mutation and `storageReads` in P1,
  P2, P3, P4 and P6. K3 and K4 are unmeasured: K4 is a new skipped-playtest
  entry. K5: P5 asserts against the repo's registry. K6: M7 is still outstanding.
  K7: P4 did not split. K8: the wrap reads the kit's string.
- §13: the scripted driver is still the only measured board.

### Review notes — where two phases said different things

- **`Leaderboard.eligible()`.** Plan R19, `CLAUDE.md` (Leaderboard) and GDD §15.4
  named it. P3 built the gate as `Meta.eligible()`, and P5 read it once in
  `runEnded()` for both boards without adding a second. Both documents now say
  `Meta.eligible()`, and §15.4 says what the old line named.
- **`activate(id)`.** `CLAUDE.md` (Save data) and GDD §15.2 kept Orbital
  Overhaul's name after P2 built the switch as `Profiles.select(id)` off
  kit-profile's `change` event. Both corrected.
- **`Profiles`' method list.** GDD §15.2's P4 note listed seven methods; P5 added
  `player()`. Added.
- **R12's order.** The plan and P4's prompt said keys first; P4 measured and
  built kit first, and Paul kept it. GDD §15.2 and `STATUS.md` already carry both;
  nothing to reconcile.
- **P2's `runEnded` stub and P3's.** P2 described the stub's body as the
  telemetry write; P3 made the whole call a no-op when no run is open, telemetry
  included, so a board started by `Game.reset()` alone and quit from pause writes
  no rows. P3 recorded it; GDD §15.6's "a run's end" is still true of every run a
  player can start.
- **P1's `keyFor` leftovers.** P1 left `src/21-telemetry.js` and the Telemetry
  group comment saying `Profiles.keyFor` for P2; P2's entry does not mention it,
  but no live `src/` file names `keyFor` now. ⚠ One comment in the closed
  `test-cs007-p4.js:479` still does; left, because a comment is not a replaced
  assertion.
- **GDD §15.4's "Registration required".** Still read as pending after P1's
  `STATUS.md` confirmed the deployed Worker lists `vector-vortex`. Annotated.
- **`ROADMAP.md`'s "CS001 through CS009 are closed"** survived CS010's close. Now
  CS011.

---

## Version history

| Version | Date | What landed |
|---|---|---|
| 0.0.8 | 2026-09-16 | CS011 P1–P6: **meta.** kit-names, kit-storage and kit-profile inlined at build; one declared `Store`; a silent ANONYMOUS first profile; settings, the Start Depth record and telemetry rows per profile, reset before load; the run's three seats and one gate, closed by a bench key; the local top 10 per mode (`createScores`); kit-input 0.8.0's text mode, PROFILE and NAME; kit-leaderboard 0.2.1 and `Leaderboard`, one submit per eligible run end, SCORES' ONLINE view and the queued line. The ninth soak: blocked storage matches a working store on every frame, and a reload brings the session back. ⛔ No baseline moved. |

## Carried forward (not CS011's to fix)

- ⛔ **Paul, outside this repo (M7):** `maxMetricPerSecond` 1,200 → about
  100,000 in coinless-kit's `registry.js`, and a redeploy.
- ⛔ **CS012:** Overdrive's own game id and board, SCORES' OVERDRIVE view, and
  `max_combo`'s real source; plus CS010's carried `drive`, combo input and
  intensity re-measure.
- ⛔ **CS015:** achievements, and `achievements` joining `Store` and
  `Profiles.remove()`'s `OWN_KEYS`.
- ⚠ Unowned: the pad-only silence (R2), the VOICE bus, the Dive's visual, the
  Surger tone's 1.106 peak; NAME's typing Space, Z and X.
- ⚠ Backport kit-input 0.8.0, kit-menu 0.1.0, kit-fx, kit-audio 0.3.0 and
  kit-leaderboard 0.2.1 to coinless-kit.
