# CS017 — ship: the budget measured, the verdicts, the sweeps, the hundred-run soak

Per-changeset narrative log and version history. ⛔ Not session context: a later
phase pulls this file in only when a question genuinely needs project history.

`STATUS.md` carries what the NEXT phase must act on; this file carries how a call
was reasoned, what was measured against what was predicted, and mutation records.

---

## P1 — the budget, measured (2026-09-23)

Built S1a, S2, S3-A and S4-A as answered at `ed8474d`. Nothing re-opened.

### §0.1, checked first

`STATUS.md` already carried all seven §0.1 findings (the planning session's
edit, "Found by the planning session"); nothing added.

### The tool — `tools/perf-probe.js`

Rebuilt from the planning session's throwaway probes (its scratchpad survived:
`cdp.js`, `browser-probe.js`, `fps-probe.js`), as one Node file with no npm
dependency: Chromium from `CHROME_BIN` or Playwright's cache (else exit 2 with
the reason — both paths run: `HOME=/nonexistent` and `CHROME_BIN=/nope`), the
DevTools protocol over Node 24's own `WebSocket`, a throwaway `--user-data-dir`
per launch so no stored profile leaks between runs. ~27 s a run.

Four things changed from the throwaway, each MEASURED first:

- **Bytes per `draw()` are the JIT's as much as the source's.** The same
  board read 8,196 B after 300 warm draws, 7,738 after 1,000 and 7,076 after
  5,000: TurboFan inlines `projectPoly()` into `drawWell()` (its bytes move
  names) and unboxes some doubles. So the tool reports TWO readings per board
  — after 300 draws (plan §1.3's method) and after 5,000 more ("steady").
- **Each board gets its own page.** Measured in one page, the second board's
  first reading inherits the first's warm-up: the budget board read 7,363 B
  after the empty board and 8,196 before it. This also explains the plan's
  empty-board figure: its 1,819 B was measured after three other boards, and
  matches this tool's STEADY reading (1,856), not its first (3,017).
- **The frame row is S2's board exactly, and it is a death freeze.** The
  fragments exist only on a dead craft, and a dead craft's frames run no step,
  so the row times `draw()` + raster with the freeze held open by a rAF hook
  (`Game.hitStop` only raises). A live-loop row that re-staged whenever the
  board thinned re-staged on nearly every step (240 in 4 s) — it measured the
  staging — so the step's script cost is timed apart instead: 10 stagings × 12
  steps, the staging untimed (one staging stepped 120 times thins to 9
  enemies and 0 shots).
- ⚠ **One boot in five lost the first Space** (title → title → mode → depth),
  pressed before the title had run live steps. The probe now waits for 30
  ticks, waits for each screen change, retries a press once and COUNTS it
  (`boot.retries`; 0 in the five runs after).

### The counter gate's numbers

Read off the build at `ed8474d` BEFORE the fixes, on the test's `stage()`:
**156 strokes, 8 text calls (4 `drawText()`)** per `draw()`. The plan's 144 / 4
was a different board: with a live craft and the prompt the count is 146 / 8,
without the prompt 146 / 6, at 8 shots 114 / 6 (plan: 112) — the +2 is the
enemy set (the test's board holds every one of the 13 `ENEMY_KINDS` rows once;
the plan's had two Surgers and no Drifter Carrier), the fragments add 10
strokes over a live craft, and the plan's "4" counted `drawText()` calls. After
the fixes: 156 / 8, unmoved, and the HEAD twin (all four old lines) 156 / 8.

### The four fixes — V4's edits, with two choices recorded

`projectPoly()` onto a non-enumerable `_screen` scratch on the input array;
`drawShot()` hands `drawPoly` one module `_shotPair`; `drawText()`'s font from
`textFont(size)`, a per-size cache; `drawPrompt()`'s fade from
`promptFadeColor(a)`, one string per 1/`C.PROMPT_FADE_STEPS` (100).
- **`C.PROMPT_FADE_STEPS` is new** (the plan said "per 1/100"): `CLAUDE.md`
  allows no magic number outside `C`. It is a draw-path granularity, 5 ms of
  a 0.5 s fade, finer than a frame.
- **The string keeps CS016's `toFixed(3)`**, so a cached colour reads exactly
  as the shipped one at every step (V4 used `toFixed(2)`); it stays correct for
  any step count up to 1,000.

### The test — `test-cs017-p1.js`, 41 assertions, ~1 s

A string has no identity in JavaScript, so "by identity" is by an observable
the old line fails:

| Site | Probe | Fixed | Old line put back |
|---|---|---|---|
| `projectPoly` | drawWell's first two `drawPoly` arrays, three frames | same arrays | fresh each frame — RED |
| `drawShot` | arrays `drawPoly` gets inside `drawShot`, two frames | 1 over 48 | 48 over 48 — RED |
| `drawText` | change `C.TEXT_FONT_FAMILY` after a size's first draw | `28px ui-monospace…` | `28px probe-family` — RED |
| `drawPrompt` | `Number#toFixed` calls on a warm alpha and a second alpha in its step | 0, one colour | 2, `0.500` / `0.503` — RED |

Each mutant string is asserted in the build exactly once before it is built;
no mutant threw. The played session (Overdrive L1, `Game.frame()`, 3,600
frames, levels 1–3, a dive, a death, 150 faded-prompt frames, 22,409 shot
draws) hashes identically frame by frame against the HEAD twin; the cached rim
projects to exactly the HEAD's screen points.

### Measured against predicted

| | Plan (PREDICTED / its MEASURED) | P1 MEASURED (`perf-probe`, Chromium 153) |
|---|---|---|
| Budget board, bytes / `draw()`, after 300 | 8,200 → ~6,200 (V4 6,196) | **8,200 → 6,184** |
| Budget board, steady | — | 6,929 → 5,059 |
| Empty board, after 300 / steady | 1,819 → 184 | 3,017 → 1,381 / **1,856 → 213** |
| Uncapped p50, 1× / 4× | 4.4–4.7 / 18.4–20.1 ms | 4.3–4.4 / 18.1 ms, before and after alike |
| Script per `draw()` / per step, 1× / 4× | 0.80 / 3.25 ms (step + draw, re-staged) | 0.15 / 0.66 and 0.05 / 0.10 ms |
| Closed files edited | none (§11) | **none**; `EXPORTS` unchanged (218) |

After the fixes the empty board's steady residue is `craftPoints` (~97 B), the
HUD (~66 B) and `wellBandColor` (~42 B); the budget board's is `entityPoints`
under every silhouette (~3.3 KB) and `Game.draw` itself (~0.6 KB). Not
chased (S3-B rejected).

### ⚠ Finding — S3's restated rule is not met by the four fixes alone

S3-A restates §17 to "no allocating expression on the draw path" and names
four sites. A scan of the draw modules after the fixes (every `" +`, `String(`,
`.toFixed`, `.map(`, `for … of`, array or object literal outside a
module-level constant) finds three more, each on every frame that draws it:
- **the HUD's text** — `hudScoreText()` (`String(score)`) and `hudLevelText()`
  (`"LEVEL " + level`), each called twice a play frame (layout and draw), and
  `hudComboText()` at ×2 and up (`15-render-hud.js`) — the "HUD" residue above,
  ~70 B a frame;
- **`wellBandColor()`'s `for (const band of C.BAND_COLORS)`** — an iterator a
  call, twice a frame (`13-render-well.js`), ~40 B MEASURED at steady state;
- **the game-over screen's three lines**, rebuilt every frame in `Game.draw()`
  (`23-main.js`).
The prompt forbids moving anything else in the renderer, so none is touched;
GDD §17 says so, and `STATUS.md` carries it as Paul's call: cache these too, or
narrow the rule's wording to the four.

### The suite

`node scratchpad/run-all.js` at the commit: **83 files green, zero skips, 288 s**
(82 + `test-cs017-p1.js`). No closed file edited; `EXPORTS` 218; `dist` 834.0 KB.

### S3 addendum — `wellBandColor()` by index (Paul, 2026-09-23, after P1)

Paul answered one of the three found allocators: `wellBandColor()`'s
`for (const band of C.BAND_COLORS)` becomes an indexed loop, a rewrite and not
a cache (the function is pure). Recorded in the plan under S3. The test gains a
fifth probe: over levels 1 … `BAND_RNG_LEVEL` + 1, the fixed build takes 0
array iterators (the probe counts `Array.prototype[Symbol.iterator]`), the
`for…of` line put back takes 99 — RED — and the colours are identical to the
HEAD twin's at every level. 46 assertions. `perf-probe` alloc, re-run:
budget board 6,184 → **6,055** B after 300 draws, 5,059 → **5,014** steady;
empty board 1,381 → **1,247**, 213 → **170**; `wellBandColor` is gone from the
attributions. The HUD text and the game-over lines stay open, Paul's.

## P2 — the bench behind a flag, and traverse-and-stop per device (2026-09-23)

S1 (c) and S7-B, built as answered. Read: the plan's §0 (S1, S7), §1.8, §1.9,
§2, §4, §8, §10, §11; GDD §0, §1, §9, §19 Core; the named source and tests.

### The flag

`C.DEBUG_KEYS: false` under Build / debug, beside `GAME_VERSION`. The eight
bench rows moved out of `ACTION_KEYS` into `BENCH_ACTION_KEYS`, merged by one
`if (C.DEBUG_KEYS) Object.assign(...)` (the build uses no object spread; its
idiom is `Object.assign`). `runAction()`, `DEBUG_SPAWN_ACTIONS`, `spawnRow()`,
the bench flag and `Meta.benchUsed()` are untouched; `update()`'s demo block is
not re-indented. One stale comment in the bench header ("they ship until CS017
decides") was rewritten to say what CS017 decided. The flag's mutate string,
`  DEBUG_KEYS:           false,`, is in the build exactly once.

### The red set, measured against predicted

Flag off, file by file: **17 red — exactly V3's set** (15 in one sweep; the
sweep raced the repairs from `-cs012-p3` on, so `-cs015-p2` and `-cs016-p2`
were confirmed red by running their HEAD text against the new build: 3 and 9
failures). No file outside §11's P2 rows went red.

**`test-cs008-p7.js`'s red, read first (the plan had not read it).** It IS the
flag, but not through a press: `reservedKey()` refuses a key because it is a
named action, and `w` stops being one. The refusal list (`Escape`, `p`, `w`,
`7`, `1`, `t`) presses `w`, which the unflagged build CAPTURES as FIRE 1, and
the page's later assertions cascade (37 of 172 red). The repair is the flip, as
§11 says — the claim was "a named-action key is refused". ⚠ **The consequence
is a finding, not a repair**: in the shipped build `w` is bindable on the
KEYBOARD page (the digits stay refused by `reservedKey()`'s own regex). Not
answered here; `STATUS.md` carries it for Paul.

**`test-cs002-p1.js`.** Unflagged, its determinism case fails in ONE process
(1575515118 against 1060872585, two runs): without the `w` press every 401
ticks the recorded run reaches a game over, and the list's Fire confirms
RESTART, which seeds from `Date.now`. The flip restores it: `--hash-only` reads
**1764508517 before and after** (the unflagged build reads 101408965). The list
is not edited.

**The two repair forms.** A `mutate` flipping the flag in the file's build, in
13 files (`-cs002-p1` both builds; `-cs004-p1` … `-p4`, `-cs005-p2`, `-p3`,
`-cs006-p1`, `-cs007-p3`, `-cs008-p7` their one build; `-cs011-p3`, `-p4`,
`-p5`, `-cs012-p3` their `build()` wrapper, with the caller's own `mutate`
concatenated after it; `-cs011-p6` its session build, not the reload, which
presses nothing; `-cs015-p2` ahead of `TABLE`). `test-cs011-p4.js` presses no
bench key itself: its text-mode mutant proves that NAME's typed `1` and `w`
would otherwise spawn and cycle, so "a bench exists" is its precondition too.
`test-cs016-p2.js`: `1`, `0` and `w` leave the ender table and are asserted
in place as unbound — each pressed in a running demo leaves it running, marks
no bench, toggles and saves nothing, pauses nothing and moves no stored byte
(8 assertions each, 239 in the file against 251).

### The suite

`node scratchpad/run-all.js` at the commit: **84 files green, zero skips, 288 s**
(`/usr/bin/time`). `EXPORTS` 218, no harness edit; `dist` 835.5 KB.

### The test — `test-cs017-p2.js`, 125 assertions, ~0.1 s

1. The shipped build: each of the eight `isBound` false, pressed in play for
   three steps it leaves a twin digest (enemies, lane, score, well, level, and
   one draw from the run's stream) identical and the run eligible; `t`, `e`,
   `p`, Escape bound. The flagged build: all eight bound, each moves the digest
   and makes the run ineligible, `1` spawns once; the untouched runs of the two
   builds digest identically (the flag alone moves nothing).
2. Traverse-and-stop, Ring (closed, leftward across the seam, 5 lanes) and Vee
   (open, rightward off the wall, 4 lanes), seven sequences each (mouse and
   touch aimed ±0.3 lane, hold and stick released within 0.5, taps exact),
   through `G.input`'s sink and the pad through `sample()`'s own poll of the
   harness window's `navigator`. Every case rests in 10–20 steps after release,
   within `SNAP_EPSILON` of the target's centre (off ≤ 1.3e-14). A 0.7-lane miss
   rests a whole lane off (the check can fail); with no pad the stick moves
   nothing.

**Mutants (each one build line, every build of the test):** the flag's `if`
dropped — 24 red; snap disabled — 12 red, the ±0.3 aims, hold and stick (taps
stay green, being exact); tap balance dropped — 2; stick delta 0 — 4; touch 0
— 4; mouse 0 — 8; hold 0 — 4; a closed well clamped like an open one — 16.
⚠ Snap's claim needed its own assertion: at `HIT_LANE_TOL` 0.5 a 0.3-lane aim
passes with no snap at all, so "snap done" is asserted as snap's own stop,
inside `SNAP_EPSILON`.

### GDD

§9.5 gains the paragraph "the debug bench is a dev build's"; §10.5's refused
keys are "every named-action key in force", with the `w` finding marked ⚠; §19
Core's traverse-and-stop clause names the test and says its hardware half is a
skipped playtest (P3 writes the entry). ⚠ Not edited, outside the prompt's
sections: §15.6's "the bench keys stay until CS017's debug-key decision" (now
decided — `t` and `e` stay), and §3.7 / §6.1 / §15.3's bench sentences, which
stay true of a flagged build.

## After P2 — two answers, and Paul's standing rule (2026-09-23)

Paul took both recommendations and set a standing rule: **a call that
carries a recommendation is taken and recorded for him to reverse; only one
with none stops.** (Recorded in `STATUS.md`; `CLAUDE.md` rule 3 amended at Paul's word, same
day, with a `RATIONALE.md#session-rules` bullet and a `DECISIONS.md` row.)

**S7 addendum — `w` stays bindable.** No code; GDD §10.5's ⚠ is SETTLED.

**S3 second addendum — the HUD's three strings and game over's lines are
cached per value.** `15-render-hud.js`: `hudScoreText()`, `hudLevelText()` and
`hudComboText()` each keep the value they last showed and its string, rebuilt
only when `view.<field> !== key` (identity, no allocation). `23-main.js`'s
`draw()`: the three game-over lines are rebuilt only when the score, the level
or `Meta.lastPlace()` moves (`_overScore` / `_overLevel` / `_overPlace`
beside `_menuView`); `lastPlace()` is read once a frame now, not twice. No
closed file pins any of these lines (grep).

`test-cs017-p1.js` gains four probes, 46 → 62 assertions. The observable is a
COUNTING value (an object whose `valueOf` / `toString` count): a string built
per frame converts it every call, a cache keyed by identity converts it on the
first only. MEASURED: hudLayout() 1 read then 0 over three warm calls (old line:
3); combo 2 then 0 (old: 6); game over, 2 on the first frame then 0 over two
(old: 2 more). The two HUD strings are not exported, so their probe reads the
rectangle's width in characters (1, 7); no `EXPORTS` row. All four old lines
join `HEAD_MUTATE`, so the played-session hash compares against every old line
— identical over 3,600 frames. perf-probe (alloc, one run): budget board
6,055 → **6,001** B after 300 draws, 5,014 → **4,979** steady; empty 1,247 →
**1,228**, 170 → **131**; no `hud*Text` site in the attribution. The game-over
lines are on no perf-probe board; the counter probe is their proof.

---

## P3 — the verdicts and the sweeps (2026-09-23)

S6-A, S9, S10-B, S11-A, S12-A and S13 built as answered; S1 (d) and (e) written.
No closed test edited; no simulation line moved.

### S6 — the Mimic: KEPT

The verdict is recorded with plan §1.7's table in GDD §14.6 (a new paragraph
after the cut paragraph), and in §21 #6 and §19 Overdrive's criterion. The
`⚠ ON PROBATION` wording became the verdict in `CLAUDE.md` (now a `⚠ SETTLED`,
still "cuts in ONE ROW"), `00-config.js`'s two comments, and the GDD's §6.4
head, §7 price table, §12 row 11, §14.9 summary, §15.5 `mimic_kill` note and
§20 #13. ⚠ **One site the prompt did not name**: `07-enemies-overdrive.js`'s
Mimic banner also said `⚠ ON PROBATION` — a comment, no pin (grep), rewritten
with the rest. ⚠ **Three comments in the suite still say "on probation"** —
`test-cs012-p4.js:50`, `-p6.js:61` (closed) and `test-registry.js:20` — left:
plan §11 lists no P3 closed-file edit, and none changes an assertion. The
close's doc pass owns them (`STATUS.md`). The row, `C.MIMIC_*`, prompt row 11
and `mimic_kill` do not move (`git diff` on `src/`: comments only).

### S9 — the credits: nothing moves

Paul kept the two lines. MEASURED against §1.9's limits: 2 lines + VERSION
(≤ 7), 13 and 14 characters (≤ 66); `test-cs008-p6.js` asserts each is drawn
and none trips the substring scan (green in this phase's run).

### S10 — the legal sweep: B, nothing in the repo moves

Paul makes the repository private before the itch page goes live. MEASURED
this session, the package's three files through `test-cs008-p6.js`'s own
`WORDS` plus the three names plan §1.9 grepped for (the maker, the sequel's
designer, his port), substring, `webkit` excepted: `index.html` — 8 hits of
the designer's surname, all inside `vaultRimInterval` (not on the list); both
`lib/` files clean; `README.md` — one hit of the list's last word, inside the
name of the browser's audio API. Same as planning's §1.9. Plan §13's note to Paul stands: the itch page's copy is
outside the repo and no file here can scan it.

### S11 — §19 Audio's sweep clause, restated

The criterion now reads "the filter sweep mapped end to end — the director's
`[0, 1]` onto 600 Hz → 18 kHz, with its played maximum recorded", with the
restatement marked in place (0.6860, D6, nothing rescaled; "heard as a sweep"
a skipped playtest, pointed at CS013 P5). The ✗ the CS012 block records stays
in its block as history; the at-ship block carries the ✅.

### S12 — the package

`package-for-itch.sh`: `zip` when present, else `python3 -m zipfile -c`, from
the same staging directory. MEASURED: `zip` absent here, `unzip` present; the
script built, staged and packaged, exit 0 — `dist/vector-vortex-itch.zip`
**287,446 bytes** (Python 3.12's CLI deflates), six entries: `index.html`
856,813, `lib/`, `lib/kit-leaderboard/`, `kit-leaderboard.js` 9,533,
`lib/kit-names/`, `kit-names.js` 1,363. Planning's hand-made zip was 285,575;
the difference is the build (+4.4 KB since `867ebd1`) and the three directory
entries. The zip is untracked (`dist/` holds only the build in git) and is not
committed. `EXTERNAL-FILES.md` unchanged — no runtime file moved.

### SKIPPED-PLAYTESTS — three entries and a pointer paragraph

S1's right-hand column, one entry per half no existing entry carries: the two
devices at 60 fps (CS017 P1), traverse-and-stop and the proportional rim by
hand (CS017 P2), the whole game from `file://` in Firefox and Safari (CS017
P3). The airborne "unmistakable" (CS012 P5), the tone by ear (CS009 P5, CS010
P5), the sweep by ear (CS013 P5, CS012 P4), storage in the two browsers (CS011
P6) and the limiter (CS010 P3) are POINTED AT in the section's head, not
repeated. Two stale lines in the file fixed in the same edit: its head's bench
keys (bound only in a flagged build since P2) and the CS013 P4 entry, which
now carries the verdict beside its ask. The frame numbers are this phase's
`perf-probe` run (`PARTS=boot,frame`): 4.3 ms p50 at 1×, 17.8 / 34.2 ms p50 /
p95 at 4×, boot healthy, zero exceptions, zero console errors, two bridge
refusals.

### The §19 sweep — how it was written

Six blocks, one per row, each "⛔ <Row> at ship (CS017 P3)"; Core and Quality
their first. Three read-only agents located each clause's asserting line in
parallel; **every cited `file:line` was then printed from the file in this
session** (three scripts, ~180 lines) and five agent citations corrected where
they pointed one or two lines off (a blank line, a `}`, a `for` head, a
fixture). The legend: ✅ asserted as the clause reads; ◐ part asserted, the
rest named; ✗ no carrier. The perf numbers are this phase's own run.

**Tally: 45 clauses — 31 ✅, 14 ◐**, and one ✗ inside Quality's first ◐
(§17 item 12, the hundred-run soak: P4's). Each ◐ names its missing half:
seven carry a hardware half pointing at a `SKIPPED-PLAYTESTS.md` entry, and
the rest are the gaps below.

### Findings from the sweep (each recorded in `STATUS.md`)

1. **`test-cs003-p2.js` passed VACUOUSLY since P2.** Its well-cycling trap
   presses `w`; P2 unbound `w`, the file stayed green, and V3's red set
   (measured by "goes red") could not see it. Repaired in place in P2's form —
   `mutate` flipping `C.DEBUG_KEYS` — plus a fixture that every press changed
   the well. MUTATION: without the flag the fixture is red, "got 0, want 18";
   with it, 73 assertions green. A grep of every file pressing a digit or `w`
   without the flag found no other (the rest are binding-validity data and
   achievement ids). ⚠ Not in plan §11's table: recorded as the finding the
   table's rule asks for; the edit restores the claim the file always made.
2. **§17 item 7's loop stops one short**: `for (l = 1; l < 200; l++)` checks
   `heat(l+1) > heat(l)` to n = 199; the message says 1..200.
3. **No played session is asserted to visit all sixteen wells** (Core, "render
   and play"). Recommendation for P4, TAKEN per rule 3 and recorded in the plan
   §6: the fifteenth soak's non-vacuity asserts every well was played.
4. **`pulse`'s A→B→C is never asserted** (only `drive`'s sections are).
5. **The stick's proportionality is asserted at one deflection** and its mirror.
6. **The shot's fade below `READABILITY_DEPTH` is a recorded known gap**
   (`test-cs002-p3.js`'s own comment).
7. **Nothing compares `dist/` with `src/`** beyond the banner scan's module
   names; the claim holds by construction (build.js, the harness's staleness
   rebuild).
8. **No test pins `TZ`**: at UTC the week assertions cannot catch a local-time
   bug.
9. **Level 1's first-seconds promise was measured once** (CS016 planning, 64
   runs) and nothing re-measures it; the determinism hash and `GOLDEN_LANES`
   hold it indirectly.
10. **Two CS016 Onboarding claims are stale**: "fifteen enders" is twelve since
    P2, and "mutation-checked in both files" is `test-cs016-p2.js` alone.
    Corrected in the at-ship block; the CS016 block is history and not edited.
11. **Values the clauses name are `C`'s and unpinned** — `INT_ATTACK` 0.40,
    `INT_RELEASE` 2.50, `FILTER_MIN_HZ` 600, `FILTER_MAX_HZ` 18,000. The
    mechanism is asserted against the constant, never the number.

None is fixed beyond (1): each is coverage a closed file would gain, and
`CLAUDE.md` puts new coverage in a new changeset's own file, never a closed one.

### The drift half

Plan S1's table gives Audio's whole row a "by ear" half; the Surger tone and
the sweep have entries, and "no audible drift" had none. A fourth CS017 entry
(ten minutes of music, by ear) covers it; its one knob is `C.MUSIC_LOOKAHEAD`.

### The suite

`node scratchpad/run-all.js` twice: before the `test-cs003-p2.js` repair, 84
green, zero skips, 275 s; after it, **84 green, zero skips, 273 s**. `dist/`
836.7 KB (comments only moved in `src/`).

---

## P4 — the fifteenth soak, the version, the review, and the close (2026-09-23)

S5-A, S8 and S14 built as answered, with P3's taken addendum (all sixteen
wells). Read: `CLAUDE.md`, `STATUS.md`, the plan's §0, §1.5, §2, §6, §8,
§10–§15, GDD §0, §1, §17 item 12 and §19 whole, `test-cs016-p4.js` whole and
this file whole. ⚠ **One further history read, for FORM only**:
`log/CS016.md`'s review, "moved out" and version-history sections, so this
close's entry has the same shape.

### The soak — `scratchpad/test-cs017-p4.js`, 35 assertions

One build, one front-door session (a hundred builds would be a hundred boots
for nothing a soak owns). Run k plays OVERDRIVE when k is even, CLASSIC when
odd: PLAY → the mode → START DEPTH → a run to game over → QUIT TO TITLE, then
the next. Everything the soak chooses comes from ITS stream, `mulberry32(SEED ^
0x5eed)` off `_seeded-random.js`, never the game's: the fire-hold (600 + ⌊u ·
6,000⌋ ticks) and the Start Depth's ROW in the live list (trap 5 — the list is
odd, so a value is never asked for). The game seeds each run from `Date.now`,
faked as WALL time (the frame clock plus a Monday-noon base, `-cs015-p4`'s
form), so every start differs: 100 distinct seeds, asserted.

- **The no-write window, then the records.** After the title's entry step the
  soak rests 600 steps (10 s, under `ATTRACT_IDLE`) and asserts the store's
  bytes unmoved and both records 0 with both lists `START_DEPTH_FIRST`; only
  then stages 30 per mode and asserts both lists are 1, 3 … 29. The live list
  then grows with the runs' own clears (33 was drawn twice).
- **The demo.** Every title visit is counted in live steps: the longest was 3,
  and no start was an attract start (`startGame` spied).
- **Per played step**: `enemies` ≤ `ENEMY_CAP`, `shots` ≤ `SPREAD_SHOT_MAX` while
  Spread is on else `SHOT_MAX`, `tokens` ≤ `MAX_TOKENS`, the score, the craft's
  lane and the dive's depth finite, and every lane and depth in `enemies`,
  `shots`, `tokens` and the dive's rings finite. Counted, the first failure kept
  — one assertion per invariant, not 391,000.
- **The driver** is `test-cs016-p4.js`'s four-clause hunter, unchanged but for
  the hold, which is the run's argument.

⚠ **Found on the first run: a `Shot`'s `depth` is a METHOD** (`06-shots.js:49`,
`depth()`), and the first draft's `Number.isFinite(e.depth)` failed on every
shot of every step (2,017,080). The check reads `depth()` where it is a
function. A finding for the next soak's author, not a defect in the build;
`STATUS.md` carries it among the test hazards.

### MEASURED

`node scratchpad/test-cs017-p4.js`, alone, `/usr/bin/time`: **34.5 s** (35.6 on
the measuring run) against S5's 60 and the plan's PREDICTED 45–70 — faster
because a drawn hold averages ~3,600 ticks against §1.5's 9,000-tick fixtures.

| | Classic | Overdrive |
|---|---|---|
| Runs | 50 | 50 |
| Distinct death levels | **27** | **25** |
| Highest level played | 35 | 33 |
| Dives begun | 153 | 117 |
| Distinct Start Depths | 1 … 33 drawn | 1 … 31 drawn |

391,046 played steps; all sixteen wells; a Mimic on the board for 13,892
entity-steps; high-water marks enemies 10, shots **24** (Spread's cap, reached),
tokens **2** (reached). ⛔ **No exception, no non-finite value, no array past
its cap.** `ENEMY_CAP` 16 is never approached (10), as every soak since CS007
has found. PREDICTED against MEASURED: §1.5 said "distinct death levels" would
need the per-run hold; one hold gave five Classic runs the same level 9, and
fifty drawn holds gave 27 levels.

**Floors.** The non-vacuity floors are set well under the measurement:
≥ 10 distinct death levels and ≥ 8 distinct Start Depths per mode, > 100,000
played steps, both modes past the Mimic's level 16 (read off
`C.SPAWN_SCHEDULE_OVERDRIVE`, not typed), a dive in each, a live `Mimic` seen,
the shot rack past `SHOT_MAX` and tokens at `MAX_TOKENS`.

### Mutation record — throwaway copies in the session scratchpad

- The shot check reading `C.SHOT_MAX` alone and the token check at
  `MAX_TOKENS − 1`: **both red** (4,409 steps at 10 shots against 8; 2,351 at 2
  tokens) — the invariants can fire and the caps are reached.
- The NaN check: proved able to fire by the finding above (a non-number depth
  read as NaN on every shot step).

### S8 — 1.0.0

`C.GAME_VERSION` `"0.0.13"` → **`"1.0.0"`** (its comment names S8);
`test-cs016-p1.js:120` rewritten in place to `"1.0.0"` (159 assertions, green);
`package.json` `"0.0.1"` → `"1.0.0"`. V5's prediction held: one file, one
assertion. Nothing else in the suite, `tools/` or the docs pins the literal
(grep); the credits' VERSION line reads it at draw time.

### Closed-file edits

- `test-cs016-p1.js:120` — plan §11's P4 row, as predicted.
- ⚠ **Not in §11's table — three COMMENTS, the close's doc pass that P3
  deferred to it** (`STATUS.md`): `test-cs012-p4.js:50` and `-p6.js:61`'s
  `gddPoints()` Mimic line, "⚠ on probation" → "KEPT (CS017 S6)", and
  `test-registry.js`'s `enemies` comment. No assertion, string or count moved;
  no `mutate` pins either line (grep).

### The doc pass

GDD §15.6's "the bench keys stay until CS017's debug-key decision" now says
`t` and `e` are bound in the shipped build and the bench is `C.DEBUG_KEYS`'s;
§16.4's `tools/` line lists `perf-probe`; §17 item 12 says where it shipped;
§19 Core's wells clause ◐ → ✅ and Quality's item 12 ✗ → ✅, each edited in
place with the phase named. ⚠ **Found, not fixed (rule 8)**: `CLAUDE.md`'s
design-instruments list names `glow-lab.html`, which is not in `tools/`
(MEASURED, `ls`; ROADMAP #9 already says it was never built) — carried in the
post-ship `STATUS.md`'s unowned table. ⚠ **And a stale count**: the pre-close
`STATUS.md` said the flag's `mutate` literal is in 14 files; it is in **18**
(MEASURED, grep at the close): P2's entry says "13 files" and lists 16,
the old count was that 13 plus `test-cs017-p2.js`, and the 18 are P2's 16,
`test-cs017-p2.js` and P3's `test-cs003-p2.js`.

---

## ⛔ THE CS017 REVIEW — the one pass that reads every phase together

### 1. Plan §10's baseline ledger, row by row (MEASURED at the close)

| Baseline | Predicted | At the close |
|---|---|---|
| `P1_DETERMINISM_HASH` | ⛔ unmoved P1–P4 | ✅ **1229033515** (`test-cs005-p5.js --hash-only`) |
| `GOLDEN_LANES` | ⛔ unmoved | ✅ `test-cs004-p1.js` green, never edited in CS017 |
| The fourteen closed soaks' hashes | ⛔ unmoved | ✅ all green; no driver or hash edited (P2's flag `mutate` reached one soak's build, `-cs011-p6`'s session, which presses a bench key) |
| `COUNTS` enemies / enemyKinds / wells / openWells / tracks | 9 / 13 / 16 / 6 / 3 | ✅ unmoved (the registry's comment edited, not its numbers) |
| `state` keys / `tally` fields | 28 / 23 | ✅ 28 / 23 |
| `C.SPAWN_SCHEDULE` / `_OVERDRIVE` | 7 / 3 (S6-A) | ✅ 7 / 3 |
| `C.SFX` / `SFX_KILL_PITCH` | 28 / 11 | ✅ 28 / 11 |
| Kill sites / kill lines | ⛔ 4 / 5 | ✅ 4 / 5 |
| `C.ACHIEVEMENTS` | ⛔ frozen 23 / 19 / 5 | ✅ 23 / 19 / 5 — **frozen from here** |
| `C.PROMPTS` | 12 | ✅ 12 |
| `ACTION_KEYS` in the shipped build | 4 | ✅ 4 (`back`, `pause`, `telemetryToggle`, `telemetryExport`) |
| `C.GAME_VERSION` | `"1.0.0"`, P4 | ✅ `"1.0.0"`; `package.json` too |
| `MANIFEST` | 26 + 3 | ✅ 26 + 3 |
| `_harness.js` `EXPORTS` | +0–2, P1 | ✅ **+0** — 218 throughout |
| `tools/` | +`perf-probe.js` | ✅ |
| The suite | 85 files, 0 skips, +~60 s | ✅ **85 files, zero skips**, 319 s, `/usr/bin/time` (the soak ~35 s) |
| Bytes per `draw()`, budget board | ~6,200 | ✅ **6,001** after 300 draws (4,979 steady) — below V4's by the two addenda |
| `dist` bytes | ~ +1 KB | 852,420 → **856,874** (+4.4 KB: the caches, the flag, the comments) |
| `CLAUDE.md` | ⛔ < 50 KB | ✅ **38,741** (rule 3's amendment, +430 B) |
| `STATUS.md` | < ~400; reset at P4 | 404 at P3's end; reset here to a post-ship file |

⛔ **No simulation baseline moved in any CS017 phase.** Plan §8's claim held
phase by phase: P1 drew the same board (hash identical against the old lines),
P2 moved which keys are bound (the flag alone moves nothing), P3 moved comments
and data, P4 added a test and a version string.

### 2. Plan §11's closed-file edits — predicted, and what else happened

⛔ Checked against `git diff --stat 867ebd1 -- scratchpad/ tools/ lib/`.

| Phase | Predicted | What happened |
|---|---|---|
| P1 | none | ✅ none; `EXPORTS` unchanged |
| P2 | V3's 17 | ✅ exactly those 17 — 16 by the flag's `mutate`, `test-cs016-p2.js` by the ender table rewritten |
| P3 | none (S6-A) | ⚠ **one**: `test-cs003-p2.js`, found VACUOUS by the §19 sweep and repaired in P2's form — the finding §11's rule asks for |
| P4 | `test-cs016-p1.js:120` | ✅ that, plus ⚠ three comment-only edits (above) |

### 3. What phases said differently — stale claims, fixed

- **The plan's budget board (144 strokes, 4 text calls) was not the test's**
  (156 / 8): a different enemy set, a live craft against fragments, and
  `drawText()` calls against text calls. P1 pinned what it measured.
- **The plan's empty-board 1,819 B was a steady-state reading**, taken after
  three other boards; P1's tool reads each board on its own page.
- **"The four fixes meet S3"** was false (P1): three more allocators, two
  answers from Paul, then met whole.
- **V3's red set was complete by "goes red" and still missed a file** — the one
  that went VACUOUS (P3). The hazard is now `STATUS.md`'s.
- **The flag's literal is in 18 files, not 14** (P4, grep); P2's "13 files"
  listed 16.
- **CS016's Onboarding block** said fifteen enders and "mutation-checked in both
  files"; P3's at-ship block corrects both, the CS016 block left as history.

### 4. GDD §19 at the close

Six rows, each with an at-ship block (P3), two lines moved in place at P4.
**45 clauses: 32 ✅, 13 ◐, no ✗.** Every ◐ names its missing half — a
hardware or by-ear judgment pointing at a `SKIPPED-PLAYTESTS.md` entry, or one
of the ten coverage gaps carried in `STATUS.md`. The document's one standing ✗
since CS010 (Audio's "audible end to end") closed at P3 by restatement (S11),
and the last (§17 item 12) at P4 by a carrier.

### 5. The changeset, compressed

**P1** built the tool (`tools/perf-probe.js`), removed every allocating
expression from the draw path in three passes (four sites; `wellBandColor()`'s
loop; the HUD and game-over strings), pinned the canvas work at 156 strokes /
8 text calls, and restated GDD §17's budget: bytes and milliseconds are data,
the work is a counter. **P2** put the bench's eight bindings behind
`C.DEBUG_KEYS` (off at ship), repaired V3's seventeen exactly, and asserted
traverse-and-stop on five device paths, two wells, eight mutants red. **P3**
recorded the verdicts — the Mimic KEPT, the credits kept, the repo private,
the sweep clause restated, the zip fallback — wrote §19's six at-ship blocks
from the suite with every citation printed from its file, and found a vacuous
closed file. **P4** met §17 item 12 in one file, all sixteen wells, and set
1.0.0. Across all four: **no simulation line, baseline, hash, schedule, kill
line, tally field, telemetry column, save key or achievement id moved.**

---

## What moved out of `STATUS.md` at the close

⛔ **NOTHING WAS DELETED.** `STATUS.md` is reset to a POST-SHIP file (S14).
Its pre-close text follows **verbatim**; what a patch must still obey was
carried forward, compressed, into the new file.

```text
# Vector Vortex — STATUS
Version: 0.0.13 · Changeset: **CS017 P3 landed 2026-09-23** · next: **P4** (§0 answered) · Wells: 16/16 · Enemies: 6/6 Classic, 3/3 Overdrive ·
Tracks: 3/5 · Tokens: 5/5 effects · Achievements: 23 lifetime + 19 weekly ·
Prompts: 12

## Phase ledger

⛔ **CS016's ledger, reasoning, mutation records, the fourteenth soak and the
CLOSE REVIEW moved to `log/CS016.md`.** This file is reset for CS017 and carries
only what a CS017 session must act on. ⛔ `log/` is not session context: pull one
file in only when a question genuinely needs project history, and say that you
did.

| Phase | Landed |
|---|---|
| Planning | ✅ 2026-09-23 — `PLANNED-FEATURES-CS017.md` + `IMPLEMENTATION-PHASES-CS017.md`: four phases (the budget; the bench and the devices; the verdicts and the sweeps; the fifteenth soak and the close), fourteen calls S1–S14; ✅ **§0 answered 2026-09-23 — every recommendation**; S9 keeps the current credits |
| P1 | ✅ 2026-09-23 — the budget, measured: `tools/perf-probe.js`; the four draw-path allocators cached (+`C.PROMPT_FADE_STEPS`); `test-cs017-p1.js` (counter gate 156 strokes / 8 text calls, five sites mutation-checked, played-session hash); GDD §17's budget restated; ⚠ three more draw-path allocators found — `wellBandColor()`'s loop rewritten (Paul's S3 addendum), the HUD text and game-over lines cached (second addendum, after P2) |
| P2 | ✅ 2026-09-23 — the bench behind `C.DEBUG_KEYS` (`false` ships; the eight bindings only in a flagged build, the bench's functions kept); V3's 17 closed repairs, exactly; `test-cs017-p2.js` (the eight unbound and inert in the shipped build, bound in a flagged one; traverse-and-stop per device on a closed and an open well); GDD §9.5, §10.5, §19 Core; `w` stays bindable (S7 addendum, Paul) |
| P3 | ✅ 2026-09-23 — the verdicts and the sweeps: the Mimic KEPT (S6); credits unchanged (S9); S10-B, nothing in the repo; §19 Audio's sweep clause restated (S11); the itch script's zip fallback, packaged (S12); four `SKIPPED-PLAYTESTS.md` entries; **GDD §19's six at-ship blocks, 45 clauses: 31 ✅, 14 ◐, one ✗ (item 12, P4's)**; `test-cs003-p2.js`'s vacuous pass repaired |

**Planning (2026-09-23).** Measured at `867ebd1`: suite 82 green, 0 skips,
282 s; every number the phases build on is in the plan's §1. Findings below,
under "What CS017 must act on".

**P1 (2026-09-23).** S1a, S2, S3-A, S4-A built as answered; no closed file
edited, `EXPORTS` 218. `tools/perf-probe.js` (Node + headless Chromium, no
npm) reads boot health, bytes per `draw()` and uncapped frame cost; the
counter gate (156 strokes, 8 text calls) did not move with the fixes, and a
played session hashes identically against the old lines. S3's rule is met
whole after two addenda (below). Reasoning, numbers and mutants: `log/CS017.md`.

**P2 (2026-09-23).** S7-B and S1 (c) built as answered. ⛔ **The bench is no
longer a shipped surface**: `C.DEBUG_KEYS` `false` leaves the eight bench keys
unbound (they reach neither the struct nor a named action), so no key a player
can press voids a run's eligibility; `t`, `e`, `p`, Escape are bound in both
builds. Flag off, **17 closed files red — V3's set exactly**; each repaired by
a `mutate` flipping the flag (13 files) or, in `test-cs016-p2.js`, the three
bench enders rewritten in place as "unbound, the demo runs on".
`test-cs002-p1.js`'s list still presses `w`; its hash reads **1764508517**
before and after. `test-cs008-p7.js`'s unread red was the flag, through
`reservedKey()` — and a finding (below). `test-cs017-p2.js`, 125 assertions,
~0.1 s, eight mutants red. `EXPORTS` 218, no harness edit. Reasoning, the red
set and the mutants: `log/CS017.md`.

**P3 (2026-09-23).** S6-A, S9, S10-B, S11-A, S12-A built as answered; `src/`
moved by comments only. GDD §19's sweep was written from the suite, every
cited line printed from its file: **31 ✅, 14 ◐, one ✗** (§17 item 12, P4's
soak). ⚠ **Found: `test-cs003-p2.js` passed VACUOUSLY since P2** — its trap
presses `w`, which P2 unbound, and a repair set measured by "goes red" cannot
see a file that stays green. Repaired in P2's form (the flag by `mutate`) with
a fixture that every press changed the well (red without it: 0 of 18); no
other file presses a bench key unflagged (grep). ⚠ Ten coverage gaps recorded,
not fixed (new coverage belongs in a new file): below. ✅ **Taken for P4
(rule 3)**: the fifteenth soak also asserts all sixteen wells played (plan §6).
The package: 287,446 B, three files, scanned clean. perf-probe this phase:
4.3 ms p50 at 1×, 17.8 / 34.2 ms p50 / p95 at 4×, boot healthy. Reasoning and
the findings in full: `log/CS017.md`.

## Working / verified

- `node build.js` produces `dist/vector-vortex.html` (26 modules + 3 inlined kit,
  **836.7 KB**); `MANIFEST` is checked both ways and a missing `KIT_INLINE` file
  fails the build.
- `node scratchpad/run-all.js`: **84 files, all green, zero skips** (273 s, P3).
  ⚠ **`run-all.js` has a 120 s PER-FILE timeout and machine load can trip it.**
  ⛔ A timeout is not a red — re-run the file alone before treating it as one.
- **CS001–CS016 are closed; each has a `log/CS0##.md`.** ⛔ **The Classic roster
  is complete at six and GDD §6.2's variants at three**; ⛔ **all five of GDD
  §4.5's death conditions are live in BOTH modes.**
- **CS012–CS014 shipped OVERDRIVE WHOLE, CS015 ACHIEVEMENTS WHOLE and CS016
  ONBOARDING WHOLE** (2026-09-17 … 09-23). What a phase must still obey is below
  and in `CLAUDE.md`; the narrative is `log/CS012–016.md`.
- ✅ **GDD §19's OVERDRIVE, META AND ONBOARDING ROWS ARE MET, every item**
  (Onboarding's, added by CS016 P1, closed at the CS016 close). ⚠ **Audio's "filter sweep audible end to end" is
  the document's one remaining ✗** and it is Paul's D6: the highest reading ever
  measured is **0.6860** (CS013 P5).
- ⛔ **FOURTEEN SOAKS, EACH PROVING A DIFFERENT THING** — `-cs003-p5` …
  `-cs007-p5`, then `-cs008-p8`, `-cs009-p6`, `-cs010-p5`, `-cs011-p6`,
  `-cs012-p6`, `-cs013-p5`, `-cs014-p3`, `-cs015-p4`, `-cs016-p4`. ⛔ **A new
  changeset's is a FIFTEENTH file, never a widened one.**
- ⛔ **`test-cs006-p5.js` carries the count-based no-draw rule**, a function of
  the LEVEL: `spawnEnemy`'s 1 plus `pickSpawnLane`'s `[1, C.SPAWN_LANE_TRIES]`.
- ⛔ **`GOLDEN_LANES`' first SIXTEEN entries are the ORIGINAL `9ebd27b`
  recording** (`test-cs004-p1.js`), held by a prefix assertion; CS008 P1
  appended `2, 5`. ⛔ **Any other move is a defect, not a baseline.**
- ⛔ **`P1_DETERMINISM_HASH` is the one baseline that moves, and it is
  CROSS-FILE** (`test-cs006-p2.js` runs `test-cs005-p5.js` in a child process).
  It stands at **1229033515**, unmoved through CS009–CS016. ⛔ Re-record once
  per change, one named cause at the assertion.
- ⛔ **On a boundary rider the LATTICE is where §17 item 3 stands, not the speed
  bound** (`RATIONALE.md#boundary-lattice`).
- ⛔ **`AudioSys.ctx` is null until a key, click or lifted touch**, and every
  audio entry point returns early on it. `buildGame({ audio: true })` installs
  the recording fake. ⛔ **A gesture is a DOM event**: `G.input.keyDown()` is not
  one, so a test wanting audio attaches `fakeTarget()`s and fires `keydown`.
  ⚠ So a fresh load's demo is SILENT: the gesture that opens audio ends it.
- ⛔ **A seat writes no `state` and draws nothing.** ⛔ The headroom gate
  (`test-cs009-p5.js`, ⚠ provisional) is D16's limiter-curve model — 0.450
  against `pulse` 0.3512, `title` 0.3408, `drive` 0.3505 — and holds only at the
  rendered settings, so `test-cs010-p1.js` pins `C.MUSIC_LIMIT` to D2's literals.
- ⚠ **SETTLED — MUSIC IS STRUCK, NEVER SWELLED** (Paul, 2026-09-16). ✅ The lab
  picks are ported for `title` and `pulse`; ⚠ `pulse` loops at 72 s, `drive` 62.6.
- ⛔ **Read GDD §6.5 before adding an entity.** NINE contract fields (plus
  `points()`), the wiring points, the Dive — and ⛔ **neither a token nor a RING
  is on that table**: no contract field, no `ENEMY_KINDS` row, its own home.
- `tools/music-lab.html` and `tools/sfx-lab.html` are the porting sources for
  `17-audio-tracks.js` and `C.SFX`, bound to the build by text identity;
  `well-lab` (⛔ the visual audition has not happened) and `feel-lab` complete
  the set; `reach-probe.js` and `perf-probe.js` are Node tools (data, never a
  gate; `run-all.js` runs neither). ⛔ **A new `SFX_KILL_PITCH` voice is a THREE-file edit**: `C`, BLOCK
  SFX and the `PITCH_*` tables. ⛔ **`C.SFX` is 28 events, `C.SFX_KILL_PITCH`
  11 voices**; a new event owes the lab a brief, an A label, 1–2 alternates and
  an in-context sequence, and is ONE line starting `    name:`. **Four** lab
  copies of `C` are pinned to the build (`test-cs010-p3.js`, `-cs014-p2.js`'s
  `LAB.dive`).

## Known issues

### What CS017 must act on

⛔ **CS017 is SHIP, PLANNED, and §0 is ANSWERED** (Paul, 2026-09-23, every recommendation; `PLANNED-FEATURES-CS017.md`). ⚠ **S10 is Paul's own action: make the GitHub repo private before the itch page goes live.**
Reasoning for everything CS016 built is in `log/CS016.md`; the rules are in
`CLAUDE.md`.

⚠ **Found by the planning session (2026-09-23), not worked around** — each
has a §0 call or a note in the plan:
- ⚠ **The GitHub repo is PUBLIC** and 8 live files name the original or its
  maker on 38 lines (GDD §18.1 says "docs"); the shipped package and `README.md`
  are clean (S10).
- ✅ Closed: the itch script's `zip` (P3, S12), the stray bench digit (P2,
  S7-B), §17's "full particles" and "no per-frame allocation" (P1, S2/S3).
- ⚠ **The soaks' MimicShot dodge — and `attractDrive()`'s port of it — measured
  WORSE than no dodge for a bot** (0.274 against 0.125 kills per reflection).
  No call; recorded.
- ⚠ **`package.json` says `"0.0.1"`** (P4, S8). ✅ §19's six rows carry an
  at-ship block (P3).
- ⚠ **Start Depth options are odd** (1, 3 … 29): a driver asking for an even
  depth silently gets row −1, i.e. level 1.

- ⛔ **A CLOSED FILE CAN GO VACUOUS WITHOUT GOING RED** (P3, `test-cs003-p2.js`):
  a change that unbinds, stubs or gates something a closed file DRIVES needs a
  grep for the file's presses, not only the red set.
- ⚠ **§19's gaps at ship (P3 sweep; each ◐ in its block, none fixed)**: §17
  item 7's loop stops at n = 199; no played session asserted on all sixteen
  wells (P4 takes it); `pulse`'s A→B→C never asserted; the stick's
  proportionality at one deflection; the shot's throat-zone fade (a known gap
  in `test-cs002-p3.js`); no `dist/`-vs-`src/` comparison; `TZ` unpinned for
  the week key; level 1's first seconds measured once (CS016); `INT_*` and
  `FILTER_*` values unpinned; CS016's Onboarding block says "fifteen enders"
  (twelve since P2) and "mutation-checked in both files" (p2 only) —
  corrected in the at-ship block, the CS016 block left as history.
- ⚠ **SETTLED — `w` IS BINDABLE IN THE SHIPPED BUILD** (Paul, S7 addendum,
  after P2): `reservedKey()` refuses every digit and every key in force, and
  `w` is neither. ⚠ A profile that binds `w` loads its KEYBOARD page as
  defaults in a flagged build (dev only).
- ⛔ **A FLAGGED BUILD IS A MUTATE** of `  DEBUG_KEYS:           false,`, by
  literal, in 14 files: re-spacing that line rewrites all fourteen in place.
- ⚠ **For the close's doc pass**: GDD §15.6 still says the `t`/`e` keys "stay
  until CS017's debug-key decision" (decided: they stay bound).
- ✅ **The Mimic is KEPT** (P3, S6; GDD §14.6's table, §21 #6): the row,
  `C.MIMIC_*`, prompt row 11 and `mimic_kill` stand. ⚠ Three suite comments
  still say "on probation" (`test-cs012-p4.js:50`, `-p6.js:61`,
  `test-registry.js:20`) — the close's doc pass. ✅ The budget's three
  measurements (P1) and the bench (P2, `C.DEBUG_KEYS` ships false) are closed.
- ✅ **S3's "no allocating expression on the draw path" IS MET WHOLE** (P1 and
  both addenda): budget board **6,001 / 4,979** B, empty **1,228 / 131**
  (perf-probe, after 300 draws / steady).
- ⛔ **`test-cs017-p1.js` PINS S2's BOARD AT 156 STROKES AND 8 TEXT CALLS A
  `draw()`**: a renderer change that draws more on that board rewrites
  `BUDGET_*` in place with the cause named — a counter, never a clock. Its NINE
  mutants pin `const out = points._screen;` …, `drawPoly(ctx, _shotPair,
  false);`, `ctx.font = textFont(size);`, ` : promptFadeColor(a);`,
  `wellBandColor()`'s indexed `for` line, the three HUD cache lines and game
  over's `if (screen === SCREENS.gameover &&` guard.
- ⚠ **`tools/perf-probe.js`'s bytes are the JIT's as much as the source's**
  (8,196 B after 300 draws, 7,076 after 5,000, same board): quote a reading
  with its warm-up; each board needs its own page. ⚠ GDD §16.4's `tools/` line
  does not list it yet (the close's doc pass).
- ⛔ **`C.GAME_VERSION` is `"0.0.13"` AND IS PINNED BY LITERAL in
  `test-cs016-p1.js:120`**: a bump rewrites that assertion in place (MEASURED red
  at the CS016 close). Ship's number is Paul's.
- ⛔ **AFTER SHIP THE ACHIEVEMENT IDS AND THE POOL'S LENGTH ARE FROZEN**: 23
  lifetime and 19 weekly ids, never renamed; the rotation walks the pool by
  length, so a row added or cut reshuffles every week's five against live saves.
  A threshold, a `name` and a `note` are not save data. ⚠ Real totals stay
  possible as an ADDITIVE v2, new ids beside these.
- ⛔ **`REACH` IS MEASURED BY `tools/reach-probe.js`** (~30 s, four passes):
  replace `REACH` WHOLE from its output; an unreached row is reported to Paul,
  never lowered.
- ⛔ **A TEST THAT IDLES 20 s AT REST ON THE TITLE ENTERS THE DEMO** (`"play"`
  under a `Game`-closure flag): a soak that parks on the title presses something
  inside 20 s or expects `"play"`. ⛔ `Date.now` / `performance.now` keep their
  four readers (the time seed, `nowMs()`, a score row's `ts`, the achievements
  clock).
- ⛔ **THE PROMPT BAND IS ARITHMETIC** (plan K2): `PROMPT_Y` 636 sits 6 px under
  the lowest rim and above the mirrored lives rectangle; a change to
  `HUD_MARGIN`, `HUD_ICON_SIZE`, `WELL_RADIUS`, `PROMPT_SIZE` or a prompt's length
  re-derives it, and `test-cs016-p1.js` asserts the clearance.
- ⛔ **`attractDrive()` IS A PORT OF THE SOAKS' HUNTER** (plan K5): a roster or
  rule change the soaks' driver learns must be taught to it too.
- ⚠ **SETTLED — A PROMPT CAN BE MARKED SEEN AND NEVER DRAWN, AND THAT IS
  ACCEPTED** (Paul, 2026-09-23, after the CS016 close): a row is marked on its
  trigger step and `startGame()` empties the queue (N4), so a row still queued
  at a RESTART or a QUIT is lost for that profile. Do not "fix" it.
- ⚠ **A mouse move during a demo DEATH FREEZE is drained and lost** (`frame()`'s
  freeze branch samples without `update()`); a held button or a named action
  still ends it. The shipped demo dies nowhere (MEASURED, ten seeds).
- ⛔ **`SEAT_EDGE` (`test-cs015-p2.js`) MATCHES BY INDENTATION LUCK**:
  `Meta.clearEdge();` sits at eight spaces inside the demo's `if (!attract)`
  block and the pinned six-space string is its suffix, once. A phase that
  re-indents that block moves the pin.
- ⛔ **A PHASE THAT EDITS EITHER ACHIEVEMENT SEAT MOVES SEVEN MUTATE STRINGS**:
  `test-cs015-p2.js`'s `SEAT_EDGE` / `SEAT_END`, `-p3.js`'s `SEAT_CLEAR` /
  `SEAT_END`, `-p4.js`'s `SEATS_OUT` (two) and `test-cs016-p3.js`'s
  `SEAT_CLEAR`, each in the build exactly once — ⚠ and `tools/reach-probe.js`
  mutates the line above each seat (`const win = wellWindow();`,
  `const ok = eligible();`).
- ⛔ **The per-well window and the clean streak move ABOVE the eligibility
  gate**, or a bench run hands the next well a doubled delta; both are re-minted
  by `runStarted()`. ⚠ **Five facts have no row** — `deaths`, `divesCompleted`,
  `purgesSpent`, `shotsFired`, `thornDeaths` — and `test-cs015-p3.js` pins that
  exact set.
- ⛔ **THE MENU SHAPES A SCREEN HAS TO FIT INTO**: the title has **five rows**
  (PLAY, OPTIONS, SCORES, PROFILE, ACHIEVEMENTS), game over **three lines**, and
  ⛔ **OPTIONS HAS ELEVEN ROWS WITH A SEVEN-ROW WINDOW** — a row goes before
  BACK, never above TELEMETRY. ⛔ **A LABEL IS ≤ 20 CHARACTERS BESIDE A
  FOUR-CHARACTER DETAIL AND A SCREEN GETS TWO INFO LINES** (MEASURED, CS015 P3).
  ⛔ **SCORES' rows are MODE, VIEW (module only), the entries, BACK**, rebuilt on
  entry / MODE / VIEW / a board's answer and never in `draw()`; ⛔ **the entry
  mode is the last run STARTED this session** (`lastRunMode`, which a demo never
  writes). ⛔ **NAME steps in place of the menu model**; ⚠ there Space, Z and X
  type. ⛔ **The ACHIEVEMENTS screen is SCORES' shape**, BACK returns to
  whichever door, and ⛔ **no seventh HUD rectangle** — centre-top is
  Overdrive's combo readout, centre-bottom the prompt band.
- ⛔ **NINE TEXTS IN `22-meta.js`, `23-main.js` AND `lib/kit-leaderboard/` ARE
  PINNED BY A CLOSED CS011 `mutate`** — grep `test-cs011-*.js` for `mutate:`
  before editing those files. ⛔ **kit-leaderboard cannot be inlined.**
- ⛔ **`Game.reset()` restores the controls and the sound rows, writes nothing,
  and leaves the screen on play**; a RELOAD test builds again over the same `Map`.
  ⛔ **A chosen level is reached from START DEPTH**, never `w`.
- ⛔ **`progress` IS v2 AND PER MODE**: `noteCleared()` writes the pair and
  `levelRecord(mode)` defaults to `state.mode`; the mode list is
  `Object.keys(C.MODE_FLAGS)`, so ⛔ **a feature is a FIELD IN THE ROWS, never a
  new top-level mode key.** ⛔ **A row-shape change bumps that key's version and
  supplies a `migrate`** — `migrateProgress` is the worked example.
- ⛔ **THE BOOT BLOCK RUNS INSIDE THE HARNESS**: `Meta.boot()` writes `profiles`
  in every build. ⚠ **A `Store.set` spy sees only `p0`'s writes**, and ⛔ **`p0`'s
  scope IS the root store**, so a per-profile `remove(key)` there must never name
  a root key. ⛔ **NO TELEMETRY WRITE FROM A PLAY STEP**; a settings save runs
  in `update()` on a menu step, and the clear edge writes `progress`,
  `achievements` and `onboarding` on a play step, legally.

### The entities and the Dive, as shipped (compressed; full text `log/CS015.md`)

- ⛔ **FOUR KILL SITES AND FIVE KILL LINES, ALL IN `09-collision.js`**
  (`:160`, `:286`, `:498`, `:512`, `:565`). ⛔ **A phase that edits a kill line
  repairs `test-cs012-p4.js`'s six `COMBO_OUT` strings, its two `mutantRed`
  shot-kill strings, `-p6.js`'s, and `test-cs014-p1.js`'s count of four.**
- ⛔ **`startDive()` DOES FIVE THINGS** — `resetDive()` (which empties the ring
  set), the `anchored` filter, `resetJump()`, `resetTokens()` and `layRings()`
  last; ⛔ **a repeated dive re-lays the set**, and a Warden and a `MimicShot`
  never survive into one. ⛔ **A dive pays exactly one thing, a ring**, spends
  zero draws, and `diveDrawDepth()` is the renderer's whole knowledge of it.
- ⛔ **AN `aloft` ENTITY IS INVISIBLE TO THE SHOT PASS AND VISIBLE TO EVERYTHING
  ELSE**; ⛔ a new reader that excludes one says so itself.
- ⛔ **THE REAVER IS A `Vaulter` SUBCLASS AND THE `MimicShot` A `WeaverBolt`
  ONE**; order a price table's branches subclass-first. ⚠ **THE MIMIC IS KEPT
  (CS017 S6) AND STILL CUTS IN ONE ROW** (GDD §21 #6). ⛔ `test-registry.js`: `enemies` 9,
  `enemyKinds` 13, `state` **28** keys, `tally` **23** counters inside one.
- ⚠ **`RING_POINTS`, `RING_ARC_LANES`, `RING_LANE_STEP` and the five visual
  values are PROVISIONAL**; ⛔ **`DIVE_TIME_OD` 4.0 and `DIVE_RINGS_MAX` 6 are
  NOT** (a ceiling may only come DOWN).
- ⚠ **Unowned, none reachable by the suite's drivers:** a second Purge preferring
  a bolt or a reflection above 0.95; a run STARTING past 99; a rim Vaulter and an
  aloft Warden hunting the CONTINUOUS lane; a biggest-Purge of six.

### Test hazards

- ⛔ **A CLOSED OVERDRIVE PLAY AT L11+ OWES ITS DRIVER FOUR THINGS**: jump at
  anything `aloft`; **never target** a `MimicShot` and steer away within a lane;
  **go to** a hovering token; ⛔ **steer to a ring in reach during a dive** —
  "in reach" from the depth model and `C.KEY_SPEED_MAX`, never a constant. ⛔ Each
  is a no-op where the thing does not exist. ⛔ **Repairs are the DRIVER or the
  FIXTURE (playing longer), never the build, a lowered level or a relaxed
  assertion.** ⚠ **That driver holds fire and spends the Purge**, so it cannot
  reach `week_purge_held` or `purge_saver`'s upper tiers.
- ⛔ **A CLOCK READ IS A MOVER.** Every closed front-door soak but the last two
  fakes `Date.now` per CALL and RESTARTS on a time seed, so a phase that adds a
  clock reader plays those sessions differently (CS015 P2 repaired
  `test-cs013-p5.js`'s driver for exactly this). ⛔ **`test-cs015-p4.js` and
  `-cs016-p4.js` fake WALL time** (the frame clock plus a base) — MEASURED, under
  a per-call fake a stubbed twin diverges at the first RESTART for no reason the
  board owns.
- ⛔ **A STAGED RECORD CAN MASK A WRITE** (CS016 P4, MEASURED by its own
  mutant): a zero-bytes claim over a demo is vacuous if the profile's Start
  Depth record already sits above the demo's clears, because `noteCleared()`
  then writes nothing. Stage records AFTER a no-write window, and assert the
  precondition. ⛔ **"Fires nothing" is PROBED off the real `promptScan()`** with
  one row unseen and a scratch queue — never a copy of a trigger.
- ⛔ **A DIVE'S LENGTH IS A PROPERTY, NEVER A STEP COUNT** (`1/60` is not
  binary). ⛔ **A run that ends inside a dive leaves `dive.active` TRUE until the
  RESTART press.** ⛔ **`state.tally`, the per-well window and the clean streak
  are re-minted by `startGame()`.** ⛔ **A kill site's reading and `state.dive`
  are read AT THE CALL**: the dive's end is `nextWell()` → `enterWell()` →
  `resetDive()` on ONE step.
- ⛔ **THREE OVERDRIVE FIXTURES ARE SEED-FRAGILE**: `-cs012-p2`'s `SOAK_SEED`,
  `-cs012-p4`'s seed **59** and `-cs012-p6`'s `OD_CLOCK` 7927. ⛔ **Anything that
  changes how long an Overdrive BEAT lasts is as much a mover as a draw.**
- ⛔ **`gddPoints()` HAS SEVEN COPIES AND THE COUNT IS NOT WHAT DECIDES** —
  whether that copy's board can KILL the entity does; ⛔ `instanceof` is PER
  BUILD. ⛔ **ITEM 8 PRICES A RING AND A BOUNTY PER STEP** in `-cs012-p4`, `-p6`,
  `-cs013-p2`, `-p5`, `-cs014-p3`: a new payout owes all five a term.
- ⛔ **A MUTATION RUN THAT THROWS IS A DEFECT IN THE TEST**: guard the reads,
  report a COUNT and one index, ⛔ assert the string is in the build exactly once
  BEFORE asserting red. ⚠ **A closed test may pin the literal text of a line a
  later phase changes** — ⛔ pin only the ARGUMENT the claim is about.
- ⛔ **A TEST THAT BOUNDS `state.shots` READS THE CAP IN FORCE off
  `state.powers`**; MEASURED, 24 IS reached. ⛔ **A STAGED DEATH IN AN OVERDRIVE
  FIXTURE MUST CLEAR `state.powers.ward` OR SET UP TWO HITS.** ⛔
  **`state.shots.push` CANNOT BE INTERCEPTED**; `tally.shotsFired` is honest.
- ⛔ **MODE IS `OVERDRIVE`, THEN `CLASSIC`, BOTH ENABLED** — ⛔ a driver wanting
  a Classic run steps ONE row down; ⛔ **START DEPTH is built for `pendingMode`.**
  ⛔ **A REPLAY THAT OUTLIVES ITS GAME OVER MEETS A LIVE MENU** — stop pressing
  at the stop; ⛔ a driver starting at the title spends two live steps first;
  ⛔ **a fire-holder has no death path on levels 1–4 and never spends a Ward**;
  ⛔ **a menu press is an EDGE** — release every held key first.
- ⛔ **A `Profiles.select()` STAGED ON A PLAY SCREEN LOOKS LIKE A PLAY-STEP WRITE**
  to a `Store.set` spy (`beforeChange` writes `telemetry` and now `onboarding`).
  In the game a switch is a menu action: stage it off play (`test-cs016-p1.js`).
  ⛔ **A played session now stores `onboarding`** (per profile, at the clear
  edge or the run's end); a new "every stored key" assertion must allow it.
- ⛔ **`test-cs008-p4.js` scans the WHOLE built file, comments included**: no
  `fillRect`/`strokeRect`, one `.fillText(`/`.strokeText(` site;
  `test-cs008-p6.js`'s vocabulary scan is a SUBSTRING scan (only `webkit` is
  excepted from "web"). `test-cs002-p1.js` bans `e.key`, `.touches`,
  `getGamepads`, `clientX` and `addEventListener` outside `04-input.js` — ⚠ and
  `20-achievements.js`'s banner slice holds the three inlined kit bodies.
  ⚠ **Strip comments before grepping a function's source.**
- ⛔ **CS009 TRAPS IN AUDIO CODE**: no "atari" (write "the original's"); never
  `.key` after an identifier ending in `e`; no `21-`/`22-` banner at a comment
  line's start; no platform RNG; never `audioFrame()` inside `frame()`.
  ⛔ **THE DIRECTOR runs BEFORE `setState()`**; a new `heat(` call turns
  `test-cs007-p2.js` red (use `heatT()`); `19-sfx.js`'s code may not name
  `state`; ⛔ **`setState()` before the first gesture is DROPPED.**
- ⛔ **AN EDIT TO `16-audio-engine.js` OR `17-audio-tracks.js` IS A THREE-FILE
  EDIT** (both labs), as is one to `C.MUSIC_LIMIT`, `C.LAYER_THRESHOLD`,
  `C.LAYER_CROSSFADE` or `C.FILTER_*`. ⚠ **EVERY KIT `VERSION` BUMP IS A
  CLOSED-FILE EDIT** — four files pin `AUDIO_VERSION` by literal.
- ⚠ **`_harness.js`**: `{ stub }` rebinds a named top-level function to a no-op;
  `{ spy }` counts calls with optional `.before`/`.after`; `{ mutate }` throws
  unless its string is in the build exactly once. ⛔ **A function inside `Game`'s
  closure cannot be reached this way**, `Game.update` and `Meta`'s seats
  included. ⚠ **An added `EXPORTS` row is a design decision a plan's §11 owes a
  line** (**218** at the CS016 close).
- ⚠ **TWO TESTS READ coinless-kit's `registry.js`** and each SKIPS LOUDLY without
  it: `-cs011-p5` (at `f0b0eb2`), `-cs012-p3` (at `e2efed5`, ⛔ never `f8d34f3`).
  ⛔ **No close can carry a skip.**

## Open questions (blocking)

- None.

## Carried tasks

- ⛔ **ONE LAB SESSION COVERS ALL OF IT, when Paul wants it**: `drive`'s tiers,
  gains and tempo (PASS marks) **and the FIVE unauditioned cues** — `ringTake`,
  `ringMiss`, the Surger tone, the Warden's fuse and `unlock` (candidate A;
  alternates "square bell pair", "triangle step up"). Its own commit; it
  rewrites `test-cs012-p1.js`'s "no tier" and "no audition mark" assertions in
  place. ⛔ **Nothing waits on it**; ⛔ **a re-pick is ported verbatim from the
  lab, never hand-tuned in `C`.**
- ✅ **`C.CREDITS_LINES` stay the current two** (Paul, S9; P3 moved nothing).
- ✅ **Both boards are registered and deployed** (coinless-kit `e2efed5`), same
  seven `statsFields`. ⛔ **Read the Worker's registered `statsFields` before
  sending a stats key**; `C.GAME_ID` is the SAVE keyspace.
- Backport kit-input (0.8.0), kit-menu (0.1.0), kit-fx, kit-audio (**0.4.0**) and
  kit-leaderboard (0.2.1, `lib/`) — each a separate manual step. `createScores`
  is kit-scores' draft, `createAchievements` kit-achievements' (⚠ its screen is
  the game's wrapper, not the module's).
- ⚠ **`CLAUDE.md` carries no telemetry rule**; whether it earns one is Paul's.
  ⛔ **`TELEMETRY_FIELDS` is frozen at 29 and `telemetry` at v1**, and it,
  `TELEMETRY_KINDS`, `telemetryRow()` and the version move together.
- ⚠ **Unowned:** the pad-only silence, the VOICE bus, the Surger tone's 1.106
  sample peak, the enemy / menu palette and the HUD sizes,
  `tools/glow-lab.html`'s visual audition, F1 and F2, and whether the headroom
  gate should bound the limiter's INPUT (Paul's call).
- ✅ **A close indexes its changeset's calls in `DECISIONS.md`**, a pointer and
  never the writeup; ✅ **the index is complete through CS016.**
- ✅ **`CLAUDE.md` is 38,741 bytes** against its 50 KB ceiling. ⚠ The valve and
  the ban on standing sweeps stand.

## Next up — CS017 P4 (the fifteenth soak, the version, the review, the close)

Paste `IMPLEMENTATION-PHASES-CS017.md`'s P4 prompt. ⛔ **Paul's standing rule
(2026-09-23): a call that carries a recommendation is TAKEN and recorded for
him to reverse; only a call with none stops** (`CLAUDE.md` rule 3). ✅ **Taken
at P3 for P4**: the fifteenth soak's non-vacuity also asserts all sixteen
wells played (plan §6, P3 addendum). ⛔ When it lands, GDD §19 Quality's ✗
(§17 item 12) and Core's ◐ ("render and play") move — P4 edits those two
at-ship lines in place. ⚠ **S10 is Paul's action, outside the repo**: the
GitHub repository goes private before the itch page goes live; the zip is
`dist/vector-vortex-itch.zip` (untracked, rebuilt by `package-for-itch.sh`).
⛔ `../coinless-kit` must be present for the close (zero skips);
`tools/perf-probe.js` needs Chromium (Playwright's cache holds one here).
```

---

## Version history

| Version | Date | What landed |
|---|---|---|
| **1.0.0** | 2026-09-23 | CS017 P1–P4: **ship.** GDD §17's budget restated to the worst board the build can produce (16 enemies, 24 shots, 2 tokens, the readout, a fading prompt, a death's fragments) and MEASURED by a new `tools/perf-probe.js` (headless Chromium over `file://`: 6,001 B a `draw()` after 300 draws, 4.3 ms p50 at 1× CPU, ~18 ms at 4×) — data, never a gate; the canvas WORK gated by counter at 156 strokes and 8 text calls (`test-cs017-p1.js`); ⛔ no allocating expression left on the draw path (`projectPoly`, `drawShot`, `drawText`, `drawPrompt`, `wellBandColor()` by index, the HUD and game-over strings cached per value). **The debug bench behind `C.DEBUG_KEYS`**, `false` shipped — no key a player can press voids a run — with V3's seventeen closed repairs, and traverse-and-stop asserted on five device paths (`test-cs017-p2.js`). **The verdicts**: the Mimic KEPT on a measurement (S6), the credits kept (S9), the repository to go private (S10, Paul's), §19 Audio's sweep clause restated (S11), `package-for-itch.sh`'s `python3` zip fallback (S12); four `SKIPPED-PLAYTESTS.md` entries for the hardware halves; **GDD §19's six rows each given an at-ship verdict block — 45 clauses, 32 ✅, 13 ◐, no ✗**; `test-cs003-p2.js`'s vacuous pass repaired. **The fifteenth soak** (`test-cs017-p4.js`): §17 item 12's hundred time-seeded front-door runs in one file, 50 per mode, 391,046 played steps, all sixteen wells, ~35 s. `C.GAME_VERSION` "0.0.13" → **"1.0.0"**, `package.json` aligned. ⛔ No simulation baseline moved in any phase. **CS017 closes the ROADMAP** (S14). |

## Carried forward (not CS017's to fix)

The post-ship `STATUS.md` carries each with its reason: Paul's actions outside
the repo (the repository private, the itch page's copy, the upload); Paul's one
lab session (`drive` and five cues); the kit backports; the unowned list (plan
§0.2, S13 — the pad-only silence, the VOICE bus, the Surger tone's 1.106 peak,
the palette and HUD sizes, `glow-lab`, F1, F2, the four unreachable entity
cases); the ten coverage gaps §19's sweep named; whether `CLAUDE.md` earns a
telemetry rule; and the headroom gate's input question, unanswered at ship.
