# log/CS014.md — the ring-flight Dive

Per-changeset narrative and version history. ⛔ **Not session context.** Pull it
in only when a question genuinely needs CS014's history, and say that you did.

Hazards, moved baselines, fixture preconditions and anything the NEXT phase must
act on live in `STATUS.md`. What is here is **how a call was reasoned**, what was
**measured against what was predicted**, and the **mutation records**.

---

## P1 — the flight: the gate, the set, the take pass, the score path

Commit: `CS014 P1: the ring flight — the gate, the set, the take, and what it pays`.
Built `PLANNED-FEATURES-CS014.md` §0's answers to RF1–RF6 and RF9, all of which
Paul answered on 2026-09-20 by taking every recommendation. No design was
invented and none was left open.

### What shipped

`C.MODE_FLAGS` gains `rings` in both rows (RF6, R2). `00-config.js`'s Dive group
gains `RING_POINTS` 100, `RING_ARC_LANES` 1.5 and `RING_LANE_STEP` 0.25, each
⚠ provisional, and `C.DIVE_TIME_OD` and `C.DIVE_RINGS_MAX` get their first
readers. `state.dive` gains `rings`. `11-dive.js` gains three top-level
functions — `diveTime()`, `layRings()`, `takeRings()` — and `updateDive()` reads
`diveTime()` once per step into `total`, for the beat and the completion check
together.

### The three readings the plan delegated, and how they were derived

⛔ **The plan named the mechanism, the count, the depths, the payout and the
constraints, and left the ARITHMETIC of the lattice to the build.** §3's own
wording is "the arc width under RF2, and whatever RF3's lattice needs". These are
derivations inside stated constraints, not design calls — but they are written
down here because a future session will ask where the numbers came from.

**1. The crossing order and the depths.** RF3's answer is "six, evenly spaced at
`(i + 0.5) / 6`", and it lists the depths rim-first: 0.9167 … 0.0833. Laid as
`(n − i − 0.5) / n` so the array index IS the crossing order, which is the order
the take pass resolves them and the order P2 will draw them. Same six numbers.
⛔ A consequence worth its own line: every depth is strictly below 1, so the
grace beat (where `dive.depth` holds at 1) can never resolve one — the take pass
needs no phase guard, and that is arithmetic rather than luck.

**2. The arc centres.** The constraints fix almost everything. R3 forbids a
draw; R6 forbids `heat()` and the level; RF2's own argument forbids reading the
craft's lane (a set measured from where the player is standing makes its first
ring free on every dive, which is the lane-less ring again). What is left is the
WELL and constants.

`C.RING_LANE_STEP` is a **fraction of the well** rather than a lane count so the
walk asks for the same share of the rim on every well. An absolute step
degenerates on a narrow closed well: on a 5-lane Ring a step of 4 is a
`laneDelta` of −1. MEASURED off the constants at the shipped 0.25 — on the
widest well (16 lanes) the walk is 4.0 lanes per ring, and a ring's near edge is
4.0 − 1.5 = 2.5 lanes away across the 0.6083 s between crossings, or 4.1 lane/s
against `C.KEY_SPEED_MAX` 14. An attentive diver clears that; an ill-placed one
does not. ⚠ 0.5 was considered and rejected: on the widest well it demands a
half-well traverse six times in 3.65 s, which is GDD §14.5's own "turning the
breath into more work".

The walk goes through **`laneHop()`** and writes back its `dir`, as every hopper
in the roster does. `laneNormalize()` was the obvious alternative and is wrong:
it CLAMPS on an open well, and MEASURED on the 7-lane Fan at 0.25 it piles rings
4 and 5 both onto lane 6. `laneHop()` mirror-folds instead. ⚠ A fold turns the
walk around, so two rings near a wall can sit close together — that is the fold
behaving, and `test-cs014-p1.js` asserts the fold rather than the clamp by
counting how many of the six land on a wall.

**3. `C.RING_POINTS` = 100.** This is the plan's own worked figure, not a new
one: §8's sizing paragraph computes "a hypothetical 100 a ring: 600 unmultiplied
is 8.3 % of a median well". Against §1.6's MEASURED 108 wells (median 7,200,
clear bonuses mean 2,951) a full set is 8.3 % of a median well and about a fifth
of what the clear bonuses pay — near `PTS_NO_DEATH_WELL` 1,000 in weight, which
is the right size for a breath's bonus. ⚠ Provisional, owned by a tuning pass.

### Why the set is a field on `state.dive` and not a third array

RF1-A, and the reasoning is in `RATIONALE.md#ring-flight` and `02-state.js`'s
own comment block. The short form for this log: CS013's token answered EIGHT
live readers, and a ring has to answer TWO — but both are wrong by default, and
that is a stronger argument than the count. `startDive()` derives the well from
`WELLS[state.wellIndex]` rather than taking it as a parameter, because four
closed files call `startDive(state)` by name and a signature change is a
closed-file edit §11 did not predict.

### The ordering call, and it is the strike's

`takeRings()` sits above the strike test and above the completion check. Below
the completion check it would never resolve the deepest ring (the last step of a
descent is at depth 0, at or past every ring). Below the strike it would unpay a
diver who crossed a ring on the step a Thorn killed them. `11-dive.js:266`'s
comment is the precedent and its reason is the same sentence.

### MEASURED against PREDICTED

| Claim | Predicted (plan) | Measured (P1) |
|---|---|---|
| `test-cs006-p3.js` "still unread" ×2 | red, repaired in place | ✅ red, repaired |
| `test-cs012-p2.js:54`, `test-cs013-p1.js:58`/`:59` | red, literals repaired | ✅ red, repaired |
| The four item-8 decoders' dive branch | red, repaired in place | ✅ red in all four, repaired |
| `test-cs012-p4.js:1114` (seed 17) | red — seed-fragile fixture | ✅ red. Seed 17 now ends at ×1 |
| `test-cs012-p4.js:628` (the mutation control) | red — seed-fragile fixture | ⚠ **GREEN.** The prediction over-called it |
| `test-cs012-p2.js:204`, `test-registry.js` | none | ✅ none |
| `P1_DETERMINISM_HASH`, `GOLDEN_LANES` | unmoved | ✅ unmoved |
| `test-cs013-p3.js:737`, `test-cs011-p3.js` | none | ✅ none |
| `test-cs008-p2.js`, `-p8.js`, `test-cs006-p5.js`, `test-cs010-p2.js` | none | ✅ none |
| Files red | 7 | **6** — one fewer than the stand-in measured |
| An edit outside §11's table | a finding | **none** |

⚠ **The one divergence is in the safe direction and it is worth naming.** The
plan's stand-in build V1 reddened `test-cs012-p4.js:628` ("the mutation window
is green unmutated"); the real P1 did not. The stand-in and the shipped feature
spend the same draws and differ only in the arithmetic of the lattice, so the
2,500-tick mutation window at seed 11 happened to land differently. ⛔ The
lesson is the one `STATUS.md` already carries: a seed-fragile fixture is a
fixture, and which of them moves is not predictable from a stand-in — only
*that* some will.

### The seed repair, and why it is the fixture

`test-cs012-p4.js`'s `drawRun()` needs a run that ENDS at a live multiplier, so
the claim "the two runs hold the same board" is made about a board where the
multiplier was doing something. ⛔ The cause is not the ring score: it is
`C.DIVE_TIME_OD`. A 4.0 s dive means the same draws land on later STEPS, so
3,000 ticks reach a different board. Seed 17 now ends at ×1. MEASURED across
twenty seeds at level 13 with the file's own `drive()`: 5 → ×2.5, 29 → ×1.5,
43 → ×2, **59 → ×3**, 61/67 → ×1.5. Seed 59 was taken as the strongest restore.
⛔ The fixture moved; the claim did not.

### Mutation records

Every string was asserted to be in the build exactly once BEFORE the mutant was
asserted red (`STATUS.md`'s K10 rule), and each probe is the same function for
the real build and the mutant.

| Mutation | What it proves | Result |
|---|---|---|
| `if (!modeHas("rings", state.mode))` → `if (modeHas(...))` | the gate is what lays the set | red |
| `modeHas("rings") ? C.DIVE_TIME_OD : C.DIVE_TIME` → `C.DIVE_TIME` | `C.DIVE_TIME_OD` has a live reader | red |
| `const n = C.DIVE_RINGS_MAX;` → `- 1` | `C.DIVE_RINGS_MAX` has a live reader | red |
| `addScore(C.RING_POINTS)` → `× comboMult()` | RF4: the payout is unmultiplied | red |
| `rings: true` → `rings: false` in the Overdrive row | ⚠ **THE CUT** — zero rings, no dive payout, `C.DIVE_TIME` long, and the jump / combo / tokens untouched | proved |

⛔ **The Classic no-op is proved by a HASH, not by reading**: 4,000 steps of a
Classic session hash identically, step by step, against a build with
`layRings()` and `takeRings()` stubbed out — and the SAME pair of builds
DIVERGES in Overdrive, which is what makes the Classic identity a statement
about the gate rather than about the stubs doing nothing.

### The `CLAUDE.md` valve

⛔ **It fired, on the section this phase wrote.** `CLAUDE.md` was at 45,797
bytes (91.6 % of its ceiling) and P1 owed it a Dive section, a Scoring rule and
a code-map row. Written in full the file reached **49,953 bytes** — 47 bytes of
headroom, which is no headroom. The reasoning moved to
`RATIONALE.md#ring-flight` and the rules stayed, naming the anchor; the file
closed at **48,897**.

⚠ **Two notes on the valve's own wording.** (1) The new section is the one that
fired it, which is the mechanism working at the earliest possible moment rather
than a sweep. (2) `## Code map` is 4.3 KB and P1 edited it, so a strict reading
of the phase prompt says fire the valve there too — but the Code map has **no
reasoning to move**: it is entirely rule, and relocating it to an on-demand
document would delete the read-order skeleton every session needs. It was
trimmed instead. ⚠ `### Math and lifecycle` is **7.0 KB** and is the real
candidate; P1 did not edit it, and `CLAUDE.md`'s own ⚠ SETTLED rule says the
valve fires when an over-size section is next edited and **never as a standing
cleanup sweep**. It is left for the phase that next edits it.

### What P1 did NOT do

No visual and no sound — both are P2's (RF7, RF8), so a shipped Overdrive dive
is 4.0 s of a still board with six rings nobody can see. No telemetry column, no
`tally` field, no HUD item, no spawn row, no heat accessor, no kit edit, no
`ENEMY_KINDS` row, no kill site and no `sfxVoice`. `09-collision.js` was not
edited. GDD §19's Overdrive ✗ is P3's to clear at the close.

### Hazards parked out of `STATUS.md` at P1's compression

⛔ **NOTHING WAS DELETED.** `STATUS.md` was at 401 lines before P1 and its
ceiling is ~400, so P1 had to compress before it added (plan K1). Most of it was
tightening; what follows was **moved here rather than rewritten**, because every
one of these is a hazard about a system CS014 does not touch — the menus, the
store, the boards, the audio duck-typing, `onShot`, the bolt's speed reader, the
HUD's rectangles, the kit `VERSION` pins. ⛔ **A CS015+ phase that touches one
of those systems pulls this section in**; the closing phase decides whether any
of it goes back.

- ⛔ **`test-cs012-p5.js` PINS TWO TEXTS BY `mutate`** — `  if (jumpAirborne(state)) return;`
  and `state.input.fire && jumpCanFire(state) &&` — and RUNS `test-cs009-p5.js`
  in a child process, so a change that reddens that file reddens this one too.
- ⚠ **`test-cs009-p5.js`'s headroom sort compares exact floats**: a non-binary
  `stepDur` reads a back-to-back pair as an overlap, repaired in place by a
  future tempo port. ⛔ A test counting scheduled steps counts NOTES.
- ⛔ **The store rules are `CLAUDE.md`'s.** The hazards: ⛔ **`OWN_KEYS` is the
  declared per-profile list** (CS015 adds `achievements` there and to `Store`;
  never `scores` or `profiles`); ⛔ **the boot block RUNS INSIDE THE HARNESS**
  (`Meta.boot()` writes `profiles` in every build); ⛔ **kit-leaderboard cannot be
  inlined** — `test-cs009-p1.js:508` bans its `setTimeout`, `test-cs002-p1.js`
  its `addEventListener`.
- ⛔ **`progress` IS v2 AND PER MODE**: `readProgress()` returns both modes so
  `noteCleared()` writes the pair, and `levelRecord(mode)` defaults to
  `state.mode`.
- ⛔ **SCORES' ROWS ARE MODE, VIEW (module only), THE ENTRIES, BACK** — VIEW is
  row 1, rebuilt on entry / MODE / VIEW / a board's answer, never in `draw()`.
  ⛔ **The entry mode is the last run STARTED this session** (`lastRunMode`).
- ⛔ **Nine texts in `22-meta.js`, `23-main.js` and `lib/kit-leaderboard/` are
  pinned by a closed CS011 `mutate`, each exactly once in the build** — grep
  `test-cs011-*.js` for `mutate:` before editing a line in those three files.
- ⛔ **The title has four rows**, game over three lines, and ⛔ **OPTIONS HAS TEN
  ROWS with a SEVEN-row window** — rows added before BACK, never above TELEMETRY.
  ⛔ **NAME steps in place of the menu model** (`stepName()`); ⚠ there Space, Z
  and X type.
- ⛔ **NO TELEMETRY WRITE FROM A PLAY STEP**; a settings save runs inside
  `update()` on a menu step, and `levelRecord(mode)` reads storage every call.
- ⚠ **A `Store.set` spy sees only `p0`'s writes**, and the stored `lastUsed`
  stays `""` after a first boot (accepted by Paul; do not rename
  `legacyProfileId` to "fix" it).
- ⛔ **`hudLayout()` HAS TWO OVERDRIVE RECTANGLES, BOTH TIGHT, AND CS013 ADDED
  NONE** (T10: no HUD item for any token — each effect shows where it acts). The
  centre-top band is 76.29 px (the Fan well) and `HUD_COMBO_Y` 6 + `SIZE` 56 +
  2 × `PAD` 4 clears it by 6.29 px, so `HUD_COMBO_SIZE` cannot pass ~62 (§10.4).
- ⚠ **A wall-pinned, fire-holding driver parks an Overdrive L7 board with no
  Reaver released** (`pinWall()` alternates the replay with a released pin), and
  ⚠ **Carrier, Weaver and Surger park rather than hunt.** ⛔ **The repair is the
  driver, never the build.**
- ⛔ **ATTRIBUTE A KILL TO THE PHASE THE STEP ENDED IN** (`test-cs005-p3.js`'s
  form). The entity pass runs before the collision pass, so a step that ENTERS a
  discharge is lethal on that step.
- ⚠ **EVERY KIT `VERSION` BUMP IS A CLOSED-FILE EDIT.** FOUR files pin
  `AUDIO_VERSION` by literal and two pin the signal path node by node.
  ⛔ A plan that bumps a kit owes §11 a row per pinning file.
- ⚠ **DELETING A CONFIG OBJECT IS A DIFFERENT GREP FROM RE-SOURCING A KEY**:
  ⛔ a plan that deletes one owes §11 a grep for `in <OBJECT>` too.
- ⛔ **A chosen level is reached from START DEPTH**, never `w`; a console unlock
  is `levelRecord("classic").noteCleared(81)`.
- ⛔ **`onShot` IS ASKED BY TWO PATHS AND A NEW ONE MUST DECIDE WHAT `null`
  MEANS** (a shot passes itself, the rim sweep passes `null`). `Thorn.onShot()`
  is the ONE reader of the argument, and `    addScore(C.PTS_THORN);` is still
  in the build exactly once (`test-cs012-p4.js:607`).
- ⛔ **THE BOLT'S SPEED IS AN OVERRIDABLE READER**, named ONCE in the build
  inside `WeaverBolt.speed()`, and ⛔ **a speed refactor is proved by a HASH** —
  a Classic run bit-identical against a build carrying the old line.
- ⛔ **THE SURGER TONE IS DECIDED AT FRAME END** (frozen or off play stops every
  voice), and ⛔ **`reconcileSurgeTones()` is DUCK-TYPED** — `phase ===
  "telegraph"` plus a `chargeTip()`, which is how the Warden borrows it with
  `19-sfx.js` unedited. ⛔ **An entity that does NOT want the voice must not
  match**; the Mimic's two phases and missing `chargeTip()` are why it does not.
- ⛔ **`Game.reset()` restores the controls and the sound rows, writes nothing,
  and leaves the screen on play**; a test claiming a setting survives a RELOAD
  builds again over the same `Map`.
- ⚠ `drawFragments`, `drawHud` and `drawMenu` read `C`, so kit-fx and kit-menu
  extraction each owe an options argument. ⚠ The touch buttons and the
  top-centre pause target are live but undrawn, and the combo readout overlaps
  that target and covers 10 rim lane-centres (F1, accepted, GDD §10.4).

---

## Version history

*(appended by the closing phase)*
