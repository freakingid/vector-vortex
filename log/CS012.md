# log/CS012.md — Overdrive core: `drive`, the Reaver, the combo, Jump, Overdrive's board

Narrative build log for CS012, **closed 2026-09-17**: `drive` (P1), the mode
flags, the Reaver and Overdrive's schedule (P2), Overdrive at the front door and
on its own board (P3), the Jump (P5), the combo (P4) and the tenth soak plus the
close (P6). Planned at `de53c0d` (`9228249`); Paul took every recommendation in
`PLANNED-FEATURES-CS012.md` §0, O1–O16 (`6fdf4ee`). Each phase appended its
reasoning here as it went. ⚠ **P5 ran before P4** at Paul's request, and the
entries below are in BUILD order for that reason. At the close the phases were
read together; two GDD paragraphs, `DIFFICULTY-NOTES.md` and seven closed
assertions were reconciled (P6, review notes).

---

## Phase ledger (moved from `STATUS.md` at the close)

| Phase | Commit | One line |
|---|---|---|
| Planning | `9228249`, `6fdf4ee` | Measured at `de53c0d`. O1–O16 answered by Paul, every one as recommended; R1–R16 flagged; every claim MEASURED or PREDICTED |
| P1 | `79a4840` | `drive` (138 BPM, 36 bars A→B→C, six layers, untiered, unmarked; kick `beat: true`) in `17-audio-tracks.js` and both labs; `C.MODE_TRACK.overdrive`, DRIVE on MUSIC TRACK; `tracks` 3. `test-cs012-p1.js` (55). Headroom 1.0345 → 0.3505; worst 13 nodes. Four closed files in place (three outside plan §11). 9 of 9 red |
| P2 | `7fdbf43` | `C.MODE_FLAGS` + `modeHas()`; `C.SPAWN_SCHEDULE_OVERDRIVE` merged by `eligibleKinds(level, mode)`; `07-enemies-overdrive.js`, `Reaver extends Vaulter` (hop ÷ 1.6, hunts mid-climb); `REAVER_POLY`; kill pitch 1.15 via sfx-lab's new KILL PITCH table. `COUNTS` 7 / 10. `test-cs012-p2.js` (158). One closed edit (`test-cs009-p4.js` voices). 12 of 12 red |
| P3 | `4a4a81c` | MODE: OVERDRIVE enabled and FIRST (the row order IS GDD §13's highlight), carried as `pendingMode`; `C.LEADERBOARD_GAME_IDS` and one kit client per mode, routed by `state.mode`, `load(mode, done)`, `queueLength()` summing both; SCORES' MODE row, per-mode LOCAL and ONLINE, entry on the last run's mode; `progress` **v2** `{ classic, overdrive }` with a `migrate`, `levelRecord(mode)` / `startDepthOptions(mode)`. `test-cs012-p3.js` (93). 13 closed files repaired in place, every one in plan §11. 10 of 10 red |
| P5 | `10c11a6` | THE JUMP: `state.jump` + `updateJump()` (a TOTAL no-op outside `modeHas("jump")`), O6's phases with the cooldown from LANDING; ONE skip in `collideSkimmer()` that takes the rim sweep with it; no fire airborne OR recovering; `resetJump()` at `enterWell()`, the respawn and `startDive()`; the draw-only lift + rim shadow; kit-audio **0.4.0**'s optional `highpass` group, `setHighpass()` from `audioFrame()`, both labs' BLOCK A; the Overdrive-only HUD glyph. `STATE_FIELDS.CS012: ["jump"]`. `test-cs012-p5.js` (172). ⚠ Six closed assertions in three files repaired that plan §11 did NOT predict. 15 of 15 red |
| P4 | `660e51a` | THE COMBO: `state.combo` `{ mult, kills, since, peak }` and five functions in `12-scoring.js`, every one a no-op outside `modeHas("combo")`; O3's build (+0.5 per 4), lapse (one step per window, ⛔ the kill count KEPT) and death (×1 at once); O5's Dive hold, in the function AND at the call site; ⛔ **the multiplier at the four kill lines, never in `addScore()`** (O4); `dangerInputs()`'s real combo input (R5); the centre-top readout and its ELLIPSE ring; `C.SFX.comboLost` at ONE seat, `comboDrop()`; `max_combo` / `maxCombo` off `state.combo.peak`, `C.TELEMETRY_PLACEHOLDER` deleted whole (R7). `STATE_FIELDS.CS012` += `"combo"`. `test-cs012-p4.js` (350). ⚠ Three closed assertions plan §11 did NOT predict. 5 of 5 red. MEASURED: Overdrive director max **0.6519** |
| P6 | this commit | The tenth soak (`test-cs012-p6.js`, 114 assertions: two paired sessions per mode, one hash per frame; Overdrive's invariants per step), the review, the ledgers, `C.GAME_VERSION` **0.0.9**, the docs and the close. 13 of 13 red |

---

## P1 — `drive`

**What shipped.** `buildDriveTrack()` and `MUSIC_TRACKS.drive` in
`17-audio-tracks.js`, copied whole into both labs' BLOCK B; music-lab's
`LAB_TRACKS.drive`. `C.MODE_TRACK.overdrive = "drive"` and `"drive"` appended to
`C.MUSIC_TRACK_CHOICES` (R10). `COUNTS.tracks` 3. `test-cs012-p1.js` (55
assertions). Four closed files edited in place (below). Build 586.6 → 594.7 KB.

**The composition (O13: 138 BPM, 36 bars A→B→C, untiered, unmarked).** GDD
§11.7 asks for the flagship, driving. `pulse` is sparse and tonal in E minor, so
`drive` goes the other way: A minor, four on the floor, and a pumping bass, in
the same 12 + 12 + 12 form `pulse` uses, so the lab's section marks and Paul's
reading of the two tracks line up. Six layers, each written to stand alone:
- `lead` carries the tune, and nothing else does. A 2-bar hook (A A E . D C . D)
  over Am F C G is stated twice in A, answered over Dm Am F G, climbs an octave
  through B, and is stated an octave up again at the top of C. At bar 32 it
  returns in its opening register, and an E major bar (G♯ as the leading tone)
  pulls back to bar 0, as `pulse`'s B major bar does. A detuned saw with a
  closing low-pass. GATE 3.
- `arp` is the riff: three chord tones struck 3 + 3 + 2 twice a bar, one tone
  order per section (up-down in A, falling in B, rising with an octave leap on
  the last strike in C).
- `bass` gives each section its own figure: octave eighths (A), a gallop (B),
  sixteenths (C). A resonant saw whose filter closes in 0.1 s. That figure is
  also what the test reads to find the three sections.
- `kick` plays four on the floor, a sine dropping two octaves. It is the one
  `beat: true` layer, so the rim pulses on every beat.
- `snare` hits two and four, with a ghost note in B and C, and a fill in each
  section's last bar.
- `hat` plays offbeat eighths from bar 4, filling in to every non-kick sixteenth
  in C.

Struck on every layer: `atk` ≤ 0.003, and every `rel` covers its GATE at 138
(0.5625 on `lead`, 0.1875 on the rest). A longer `rel` sounds the same, since the
engine clamps it to the note. It also moves music-lab's slowest ladder step to
76 BPM, so Paul can try tempos well below 138. One fill hit moved from step 8 to
step 9 after the first count: on the kick, the arp and the lead it made the worst
step 15 nodes. After the move, the worst is 13.

⚠ **The gains are the composer's, not rendered and not heard.** The CS010 render
rig (chrome-headless-shell) is not on this machine, and no mix was measured.
They follow `pulse`'s shipped balance, which Paul approved: the struck rhythm
layers loud (kick 0.450, bass and snare 0.300, hat 0.250) and the tonal layers
low (lead 0.070, arp 0.060). Paul's lab session replaces them.

**Measured** (recording fake; scratch scripts in the session directory):

| | `title` | `pulse` | `drive` |
|---|---|---|---|
| Loop | 16.0 s | 72.0 s | **62.6 s** (36 bars) |
| Notes | 85 | 861 | 1,319 |
| Worst nodes / step | 9 | 14 | **13** (bar 4, step 14) |
| Headroom in → out (D16) | 0.5904 → 0.3408 | 1.0771 → 0.3512 | **1.0345 → 0.3505** (2.17 dB under 0.450) |

The exact compare and the 1e-9 s tolerance give the same loudest value on all
three. On `drive`, 417 back-to-back pairs round to an end after the next start,
but none is at the loudest moment. The tolerance stays, as plan §1.8 requires.

**Finding 1 — the prompt's mutation cannot be met (MEASURED).** "Doubling one
layer's gain turns [the headroom gate] red" was a prediction. Under D16's curve
(threshold −24, ratio 20, makeup +13.68 dB), a gain change moves the modeled
output by a twentieth of its dB. Doubled one at a time on `drive`: kick → 0.3564,
snare 0.3550, bass 0.3546, lead 0.3515; arp and hat do not move the loudest
moment. To reach red, the input must be **152.8** (×148). So the gate cannot catch
a louder `drive`. It guards the limiter's premise (knee 0), the curve and the
buses. The test has two mutations instead:
(1) a doubled kick raises the gate's input, and the output rises by exactly 1/20
of that change in dB, which proves the gate reads the gains through the curve;
(2) the build with its `limiter:` line mutated out is red (1.0345 against 0.450).
Recorded in `STATUS.md`.

**Finding 2 — three closed edits outside plan §11's table (MEASURED red before
repair).** The plan predicted only `test-cs009-p3.js:38–39`. Running the suite
after the port turned three more red, and each was rewritten in place with its
cause stated at the assertion:
- `test-cs009-p3.js:307`, "clamped at PULSE": the row now clamps at DRIVE.
- `test-cs010-p4.js:43`, "exactly one layer is marked beat: pulse's heart": now
  read over `title` and `pulse`. `drive`'s kick is asserted in P1's own file.
- `test-cs011-p2.js:191` planted `"drive"` as "not a choice". It now plants
  `"title"`, which names a track but will never be a choice.

**Mutations, scripted, each file restored (9 of 9 red on `test-cs012-p1.js`).**
`C.MODE_TRACK` without `overdrive` (6: AUTO, pause and the R11 run); the choices
without `drive` (6: the row, the save, the reload); the kick without `beat` (2);
tempo 120 (2); a `tier` on `arp` (1); an `audition` on `lead` (1); one bass
figure for every section (1); a kick on every eighth (1); music-lab without its
`drive` entry (5). The first draft crashed on two of them rather than failing
loudly, and it compared a float exactly. All three were fixed before the record
above.

**Baselines.** `P1_DETERMINISM_HASH` 1229033515 and `GOLDEN_LANES` unmoved (the
suite green). `COUNTS.tracks` 2 → 3 (plan §10). The suite has 61 files.

## P2 — the mode flags, the Reaver, Overdrive's schedule

**What shipped.** `C.MODE_FLAGS` and `modeHas(name, mode)` (R1), read by tests
only. `C.SPAWN_SCHEDULE_OVERDRIVE` (one row, `reaver` at 6) merged by
`eligibleKinds(level, mode)`; `pickSpawnKind(state)` passes `state.mode` (O2).
`src/07-enemies-overdrive.js` (O15) with `class Reaver extends Vaulter` (R9);
the Vaulter gained `hopDuration()`, `midClimbDir()` and `hopRate = 1`.
`REAVER_POLY` (derived from `VAULTER_POLY`, twelve points) and `drawReaver()`.
`C.SFX_KILL_PITCH.reaver` 1.15, with sfx-lab's new KILL PITCH table (A 1.15, B
1.06, C 1.33; COPY OUT rewrites the voice's number). `COUNTS` 7 / 10. Build 594.7
→ 620.9 KB, 25 modules. `CLAUDE.md` 34,068 → 34,933 bytes.

**Readings taken in the build.**
- `hopDuration()` stays `C.VAULT_HOP_TIME` on the Vaulter and the Reaver overrides
  it as `/ this.hopRate`; the intervals are `(accessor) / this.hopRate` in the
  Vaulter's own `update()`. `x / 1 === x`, so no Vaulter number moved.
- `REAVER_COLOR` is its own key with `VAULTER_COLOR`'s value: O16 says shared,
  plan §4 names the key. The test pins them equal.
- `modeHas()` reads an unknown mode or flag as `false` rather than throwing.

**Measured against predicted.**

| Claim | Predicted | Measured |
|---|---|---|
| `P1_DETERMINISM_HASH`, `GOLDEN_LANES`, nine soaks | unmoved | **unmoved** (61 files green before the new file) |
| `climbMult()` call sites | 5 | **5** (`test-cs007-p2.js` green, no edit) |
| Closed edits | `test-cs009-p4.js:115` only | **that one** (the voice list, and its message's "seven" → "eight") |
| `test-cs007-p3.js` | no edit | **no edit** |

`_harness.js`'s `EXPORTS` gained `modeHas`, `Reaver`, `REAVER_POLY`, `drawReaver`
(infrastructure, not an assertion).

**Finding — CS008 P1's rim mutations no longer bite alone (MEASURED).** P1b's rim
sweep kills a touching rim enemy under held fire, so removing ε, or leaving
`atRim()` at `>= 1`, leaves every Reaver arrival case 24/24. The sweep removed
alone: also 24/24. **Sweep and ε removed together: hopping in is 18/24 on the
Ring and the Vee at L6** (climbing in stays 24/24). That pair is the test's
in-file mutation. `atRim()` cannot redden a Reaver case even without the sweep,
because a Reaver hunts mid-climb and never waits for the rim.

**Finding — the soak board parks with a wall-pinned mouse.** At Overdrive L7, a
Skimmer pinned to a wall with fire held released no Reaver in 5,000 steps on any
open well: parked Weavers and Carriers filled the budget of 4. The driver
alternates 300 steps of the sweeping, firing replay with 300 pinned to a wall with
fire released: 4–8 Reavers per well, 34–135 wall-lane steps.

**Mutation record** (each applied to `src/`, rebuilt, `test-cs012-p2.js` run):

| Mutation | Result |
|---|---|
| Reaver's `midClimbDir` → the Vaulter's | RED, 7 (the hunt) |
| `hopDuration` unscaled / heat-scaled | RED, 11 / 8 |
| intervals not ÷ `hopRate` | RED, 10 |
| climb × `hopRate` | RED, 9 (climb equality, respawn push) |
| `eligibleKinds` ignores mode | RED, 5 (incl. the Classic hash, first differs at step 82) |
| `pickSpawnKind` drops `state.mode` | RED, 9 |
| Overdrive row first at an equal level | green, then RED 1 once a staged-data tie case was added (no shipped row ties) |
| one-entry set draws | RED, 2 |
| Reaver `anchored` / not `purgeable` | RED, 8 / 4 |
| `modeHas` default ignores `state.mode` | RED, 1 |

---

## P3 — Overdrive at the front door, and its own board

Built §5 against §0's four answered calls (O9 MODE's order, O10 SCORES, O11 the
id, O12 the record). Nothing was invented; nothing was re-opened.

**O9 — the highlight IS the row order.** GDD §13 asks for "Overdrive is the
default highlight". The menu model already puts the cursor on the first enabled
row, so the whole feature is `SCREENS.mode.items` reordered and `enabled: true`:
no detail string, no colour, no flag, nothing for a later phase to unify away.
The `LOCKED` detail and M1's comment went with it. `pickClassic` and
`pickOverdrive` share one branch that writes `pendingMode`; they stay two action
names because `test-cs008-p5.js` and §1.6's measured variants name them.

**Why `pendingMode` and not `state.mode` for START DEPTH.** At MODE no run
exists, so `state.mode` still names the LAST run's mode — on a fresh boot,
`newState()`'s `"classic"`. With the record per mode (O12) that is a live bug,
not a tidiness point: choosing OVERDRIVE would have listed Classic's unlocked
depths. `buildDepthRows()` passes `pendingMode` explicitly, and the GDD §4.6
entry now says so.

**O12 — `progress` v2.** The mode argument follows the heat accessors' level
argument: optional, defaulting to `state.mode`. `readProgress()` is the one
reader and returns BOTH modes as whole numbers, so `noteCleared()` writes the
pair and one mode's clear can never drop the other's. `migrateProgress` is a
plain function declaration (hoisted above `boot()`), pure, and returns
`undefined` for any origin version but 1 — kit-storage then leaves the bytes
alone rather than destroying a record this build cannot read. v1's value goes to
`classic` because Overdrive was not choosable before this phase, so no Overdrive
clear can predate the bump. MEASURED: kit-storage writes the migrated value back
at v2 under the same key (`test-cs012-p3.js`).

**O11 / R8 — two clients.** `C.LEADERBOARD_GAME_IDS` is keyed by mode and its
KEY ORDER is load-bearing: it is the order `make()` creates in, which is what
keeps `test-cs011-p5.js`'s "the first client is Classic's with `C.GAME_ID`"
true. `make()` builds every client that is neither made nor broken, so one
board's throwing `create()` cannot stop the other's, and neither is retried.
`present()` is true as soon as any client exists — the question SCORES asks is
"did the module load", not "is this mode's board reachable". The stale-answer
token stayed ONE counter across both boards: only one load is ever shown, and a
MODE step on ONLINE is a newer load, so the outgoing mode's answer is dropped by
exactly the mechanism that already dropped an older one. That is why P3 needed
no second token and no mode field on the answer.

**O10 — SCORES' MODE row.** `SCORES_MODES` is read OFF `SCREENS.mode.items`
rather than written twice, so the SCORES row and the MODE screen cannot disagree
about the order or the count. O10's "else MODE's first row" is read as the MODE
SCREEN's first row: reading it as "the SCORES MODE row's first value" is
circular, since that row's value IS the entry mode. "The last run started this
session" is a module-level `lastRunMode` in `23-main.js`, written by
`startGame()` — not `state`, which `quitToTitle()` resets to `"classic"`, and
not `Meta`, which must write nothing. `loadScoresView()` is the one place a menu
step begins a load, so MODE and VIEW cannot answer the ONLINE question
differently.

**Closed-file edits — exactly §11's table, and no more.** Ten files went red on
the first build, all predicted: `test-cs008-p5.js` (O9's 15 assertions, MEASURED
variant A), the eight O9-A front-door drivers, and `test-cs011-p2.js` (O12).
The three §11 listed as green-but-playing-Overdrive (`test-cs010-p2.js:277`,
`test-cs011-p1.js:172`, `test-cs011-p2.js:164`) were repaired the same way, and
`test-cs011-p4.js`'s two planted `progress` values (`:447`, `:506`) were moved to
v2's shape — green either way, but a v1-shaped object under a v2 declaration is
a fixture that no longer says what it means. ⛔ **No unlisted file went red, so
there is no finding to record.**

Each repair is one step to CLASSIC per driver, which is the whole cost O9
predicted. Three claims were rewritten rather than repaired, because their
mechanism was replaced and not merely relocated:
- "rotating toward OVERDRIVE leaves CLASSIC highlighted" → "OVERDRIVE is the
  default highlight, and one row down is CLASSIC". The old claim was about a
  DISABLED row never taking the cursor; the pure-model check beside it
  (`createMenu` over a two-row screen with a disabled last row) still holds that,
  so nothing was lost.
- "OVERDRIVE is drawn, in the locked colour" → drawn and NOT in the locked
  colour, plus the clamp at both ends proving MODE has exactly two rows.
- `test-cs011-p5.js`'s "the client is made once" → made once each, one per mode,
  Classic first.

**A fixture repair worth naming.** `test-cs011-p5.js`'s fake had ONE `queue`
counter, which two clients summed into a doubled title line. The queue moved to
one counter per `gameId`, matching the kit's own `coinless.lb.<gameId>.v1`
keying. That restores what CS011 P5 was asserting — the LINE's wording, singular
and plural — and leaves the summing claim to CS012 P3's own file, where it
belongs.

**`test-cs012-p3.js` (93 assertions, no skips).** OVERDRIVE chosen on keyboard,
mouse, pad and touch with `state.mode === "overdrive"`; the two `create()`s with
the two ids, C's endpoint and version; an Overdrive run submitting once to
Overdrive's board and never Classic's, a Classic run the reverse, RESTART
keeping the board, a bench run posting to neither; the queued line summing 2 + 3
= 5; both modes' stats keys read from coinless-kit's `registry.js` at Paul's O11
commit `e2efed5` (skipping loudly if that repo or entry is absent); SCORES' MODE
row cycling with its table, ONLINE per mode, the outgoing mode's late answer
dropped, and its mutation (`answer` without the token) painting the Overdrive
board under `CLASSIC · ONLINE`; the v1 → v2 migration and its write-back; a clear
in one mode extending only that mode's list, through the real clear edge; START
DEPTH built for the chosen mode; a reload keeping both; and `state` hashed around
every `Meta.*` and `Leaderboard.*` call.

**Mutation record — 10 of 10 red** (each applied to `src/`, rebuilt,
`test-cs012-p3.js` run, `src/` restored):

| Mutation | Result |
|---|---|
| `beginRun()` / `submit()` always use Classic's client | RED, 9/93 |
| `queueLength()` returns the first board's count instead of summing | RED, 1/93 |
| `make()` creates only `state.mode`'s client | RED, 11/94 |
| `buildDepthRows()` reads `state.mode`, not `pendingMode` | RED, 2/93 |
| `noteCleared()` writes `{ [mode]: level }` alone | RED, 4/93 |
| `migrateProgress` returns v1's data unchanged | RED, 3/93 |
| `migrateProgress` sends v1's value to `overdrive` | RED, 4/93 |
| SCORES' `Meta.scores(scoresMode)` pinned to `"classic"` | RED, 1/93 |
| SCORES' entry mode ignores `lastRunMode` | RED, 2/93 |
| MODE's rows swapped back (CLASSIC first) | RED, 39/93 |

The eleventh, `answer` without the stale token, is the in-file `mutate` above.

⚠ **Three of those ten first came back as a TypeError, not a count** — the file
threw at `ob.submits[0].result` or `rec.pending[0].res` and abandoned its
remaining claims. A throw is red, but it is a worse answer than a count: it
hides which claims the mutant actually breaks. The reads are now guarded
(`settle()`, `|| {}`), and the same three mutants report 9, 11 and 39 failed
assertions. ⛔ **A mutation run that throws is a defect in the test, not a
result** — worth remembering for CS013's item-13 test.

---

## P5 — Jump

⚠ **RUN OUT OF ORDER.** `IMPLEMENTATION-PHASES-CS012.md` and `STATUS.md` both
had P4 (the combo) next; Paul's prompt asked for P5. Nothing in P5 reads the
combo, so the two are independent, and the only visible consequence is that
`STATE_FIELDS.CS012` is created here as `["jump"]` and P4 appends `"combo"`
rather than the other way round (plan §10 ledger: `+ CS012: ["combo", "jump"]`).
⛔ **P4 still owns everything plan §11 lists for it**, including the five
`C.TELEMETRY_PLACEHOLDER` edits and `test-cs009-p4.js`'s `comboLost` rows — none
of which P5 touched. P4 also places its rectangle into a HUD that now has a
jump glyph beside the Purge glyph; the combo's is centre-top (O8), so they do
not meet.

**What shipped.** `state.jump` and five top-level functions in `05-skimmer.js`
(`updateJump`, `resetJump`, `jumpAirborne`, `jumpCanFire`, `jumpLift`); one gate
in `updateShots()`; one skip in `collideSkimmer()` and one forced latch in
`killSkimmer()`; `resetJump()` at `enterWell()`, `respawnSkimmer()` and
`startDive()`; the lift and shadow in `Skimmer.draw()` and `skimmerPoints()`'s
fourth argument; kit-audio **0.4.0**'s `highpass` group and `setHighpass()`, in
`16-audio-engine.js` and both labs' BLOCK A; `MusicSys.setHighpass()` in
`audioFrame()`; the jump glyph in `15-render-hud.js`. Eleven `C` knobs.
`test-cs012-p5.js` (172 assertions). Build 638.8 KB. Suite 64 files, zero skips.

### O6, and why the cooldown counting from landing is the whole design

O6's table was the phase's one real arithmetic. The three candidate cooldown
origins give 64.3 % / **39.1 %** / 36.0 % of the cycle airborne, against a
Surger cycle of 2.78–3.08 s. Counting from landing is what makes §14.2's
"clearing one leaves you grounded for the next" literally true: 2.30 s of cycle
against a Surger's 2.78 s worst case leaves the player grounded for the shorter
one and airborne for none of two. Counting from takeoff would let a player be
airborne for 64 % of the time and dodge two discharges in a row, which is the
trivialisation §14.2 names. The implementation is one line — `j.cool = 0` at the
landing, not at the takeoff — and it is the line the mutation record below
checks.

`JUMP_RECOVERY` is expressed as **the cooldown's first beat**, not as a fourth
timer: `recover` ends when `cool` passes `C.JUMP_RECOVERY`, so the two can never
disagree and there is nothing to keep in step. That also makes the lethal window
exactly the first 0.20 s of the 1.40 s, which is what O6 says.

### Why the immunity is a `return` and not a `killDepth`

The alternative the codebase had been predicting for three changesets was to
give the Skimmer a `depth` and let `collideSkimmer()` compare two depths — four
shipped comments said so in as many words (`07-enemies.js`'s base class and the
Drifter's constructor, `09-collision.js`'s header, `02-state.js`'s dive note),
and GDD §4.5 and §6.5 said it twice more. Every one of them is corrected in this
commit, because the skip is strictly better on three counts:

1. **One line covers the whole roster.** Seven contact killers, three of which
   (armoured Drifter, Weaver bolt, Surger discharging below the rim) the rim
   sweep deliberately does not cover, are all handled by the same `return`. A
   depth term would need `killDepth` to mean something new on each of them.
2. **It takes the rim sweep with it.** R3's wording — "the rim sweep never runs
   there, because fire is refused" — is not quite right as mechanism:
   `input.fire` is a level and stays true while airborne, so a fire-gated sweep
   WOULD run. Skipping the pass is what actually delivers R3's claim, and
   `test-cs012-p5.js` asserts it directly (a firing airborne craft kills nothing
   it flies over). ⛔ **That is a correction of the plan's reasoning, not of its
   answer.**
3. **The build keeps exactly one two-depth comparison**, the dive strike, which
   is the property `11-dive.js` and `09-collision.js` both lean on.

### What did NOT need a decision, and why that is the point

⛔ No contract field moved (GDD §6.5), no enemy class changed, `killSkimmer()`
is unchanged but for the latch, and the Dive is untouched but for one
`resetJump()`. R4's "a jump in flight when the well clears lands on the clear
step" went into `startDive()` beside `state.shots = []`, for the same reason:
what belongs to the well being left does not cross the throat. Without it an
airborne craft carried into a dive would hold its immunity, its lift and its
high-pass for the whole descent with no way to come down — the dive
short-circuits `update()`, so `updateJump()` never runs there.

### The high-pass: three choices inside O7's one sentence

O7 fixed the group's shape (`{ hz, tc }`, `setTargetAtTime`, never `.value`) and
left three things to the build.

- **Where in the chain.** "After the sweep and before the limiter" is O7's, and
  the reason is worth writing down: the duck and the dip sit AFTER the limiter
  because CS010 MEASURED a 0.5 duck coming out at −1.9 dB in front of one. A
  tone control is the opposite case — it should be inside the limiter's view, so
  what the limiter holds is the sound the game asked for. `ensureGraph()` wires
  downstream first, so the node is built between the limiter and the sweep and
  the signal runs sweep → high-pass → limiter.
- **What "off" is.** A bypass switch would mean connecting and disconnecting a
  node, which cannot be ramped. "Off" is instead a resting **0 Hz** cutoff — a
  pass-through — so the node stays in the path permanently and both directions
  are one `setTargetAtTime`. `AUDIO_HP_OFF_HZ` names it once.
- **Q.** Fixed at the Butterworth `-3.0103` dB and ⛔ **not a tunable**, which is
  the decision that keeps `test-cs009-p5.js` unedited: a flat high-pass is at or
  under unity at every frequency, so D16's headroom model needs no term for it.
  A peaking high-pass would be a different effect rather than a louder one, and
  a host that wanted one would be asking for a filter this group does not
  describe. `test-cs012-p5.js` asserts `Q <= C.FILTER_Q` and then RUNS
  `test-cs009-p5.js` in a child process rather than copying its model.

### MEASURED — closed-file edits the plan did not predict (a finding)

⛔ **Plan §11 predicted NO closed-file edits for P5.** Six assertions in three
closed files went red on the version bump and the new node, and each was
rewritten in place to the replacement behaviour:

| File | Assertion | Now |
|---|---|---|
| `test-cs009-p1.js:100` | `AUDIO_VERSION` "0.3.0" | "0.4.0" |
| `test-cs009-p1.js:335` | `gameRoute`: gain → sweep → limiter → … | … → sweep → high-pass → limiter → … |
| `test-cs009-p4.js:98` | `AUDIO_VERSION` "0.3.0" | "0.4.0" |
| `test-cs010-p1.js:38` | `AUDIO_VERSION` "0.3.0" | "0.4.0" |
| `test-cs010-p1.js:210` | "only the sweep feeds the limiter" | "only the high-pass feeds the limiter", plus a new line for the sweep feeding it |
| `test-cs010-p1.js:221` | the path walk's expected tail | the high-pass inserted |

⚠ **This is a gap in the plan's method rather than a surprise in the code.**
Plan §10's ledger DID predict "kit-audio 0.3.0 → 0.4.0 at P5"; §11's table of
closed-file edits simply did not carry the consequence across. Every version
bump in this project is a closed-file edit, because three closed files pin
`AUDIO_VERSION` by literal, and a signal-path change is another because two pin
the path node by node. ⛔ **A future plan that names a kit `VERSION` bump owes
§11 a row for every file that pins that string**, and one for the path if the
bump moves a node. None of the six deletes or weakens a claim: the claim in each
case is "the module is at the version this changeset shipped" and "the music
reaches the bus down one named chain and no other".

### Mutation record — 15 of 15 red

Each applied to `src/`, rebuilt, `test-cs012-p5.js` run, `src/` restored. Two of
the fifteen are in-file `mutate` cases (the collision skip, the fire gate) and
are part of the test; the rest were run by hand.

| Mutation | Result |
|---|---|
| `collideSkimmer()`'s airborne skip removed | RED — all 7 killers kill again (the in-file `mutate`) |
| `updateShots()`'s `jumpCanFire(state)` removed | RED — both phases fire again (the in-file `mutate`) |
| `killSkimmer()`'s `state.jump.latched = true` removed | RED, "FORCES the jump latch true" |
| `startDive()`'s `resetJump()` removed | RED, "the jump LANDED on the clear step" |
| `enterWell()`'s `resetJump()` removed | RED, "enterWell(): grounded" |
| the respawn's `resetJump()` removed | RED, "the respawn put the jump back on the GROUND" |
| `updateJump()`'s `modeHas("jump", …)` gate removed | RED — the Classic hash pair diverges |
| the cooldown reset moved from landing to takeoff | RED, "THE COOLDOWN STARTS FROM LANDING, at exactly 0" |
| takeoff on the LEVEL instead of the rising edge | RED — 162 airborne steps instead of 54 |
| the skip widened to cover recovery too | RED (the anchor is gone, so the in-file `mutate` refuses loudly) |
| `setHighpass()` by a bare `.value` | RED — no `setTargetAtTime` on any flip frame |
| `setHighpass()` made non-idempotent | RED — it automates on all 900 frames |
| the high-pass group not built | RED, "WITH the group, one high-pass node is built" |
| the shadow stroke dropped | RED — one stroke airborne instead of two |
| the glyph drawn in Classic too | RED, "a CLASSIC run hands drawHud a null jump view" |
| the glyph not absent while airborne | RED, "ABSENT while airborne" |
| `C.HUD_JUMP_GAP` pushed under the touch button | RED — 31 rectangle points on a button |

⚠ **Two mutants first came back as a THROW rather than a count** (the shadow
dropped, and the high-pass not built), because the file read `strokes[1].alpha`
and `withIt.highpass.Q.value` without a guard. CS012 P3's log says a mutation
run that throws is a defect in the test; both reads are now guarded and both
report a count. ⛔ The same rule caught it twice in one changeset — worth
carrying into CS013's item-13 test.

### MEASURED — the phase-length assertion, and a float trap

⛔ **`Math.ceil(limit / C.FIXED_DT)` is NOT the number of steps a count-up timer
spends in a phase**, and the two disagree in BOTH directions. `C.FIXED_DT` is
`1/60`, which is not a binary fraction, so `t` accumulated by repeated addition
drifts: `C.JUMP_TIME` 0.90 s takes **54** steps where `ceil` says 54, and
`C.JUMP_RECOVERY` 0.20 s takes **13** where `ceil` says 12. The first draft of
this file asserted the step counts and went red on the second one. It now
asserts the property instead — *the first step at or past the limit is the last
one in the phase* — plus the duration to within one step. ⛔ **A future test that
pins a phase length by `ceil(limit / DT)` is asserting a float accident.**

### The lift's geometry

O7 says "raised outward from the well's centroid by up to `C.JUMP_LIFT` rim
radii". That is applied AFTER the projection, in screen space, and the reason is
`perspective()`: depth is capped at 1, so a point "outside the rim" expressed as
a depth above 1 would silently collapse back onto the rim and the silhouette
would lose a vertex on every well — the same trap `SKIMMER_POLY`'s header warns
about for its own `d` offsets. Each projected point is pushed along its own
outward direction from `wellCentroid()` mapped to screen space, by
`lift × C.WELL_RADIUS`, which is what makes "rim radii" a unit rather than a
figure of speech. The parabola is `4u(1 − u)`: 0 at takeoff, `C.JUMP_LIFT` at
half time, 0 at landing, so the craft touches down exactly where it left.

⛔ **The shadow is the UNLIFTED outline of the same craft**, stroked first so the
lifted one reads over it. `skimmerPoints()` returns the one shared scratch
array, so the two paths have to be built and stroked in turn — which is exactly
what `drawPoly` then `glowStroke` do, and why the shadow cannot be hoisted.

### The HUD glyph

⛔ **The layout rectangle is the RING's extent, not the craft's.** The craft is
drawn at `C.HUD_JUMP_CRAFT` (0.60) of the box, but the readiness ring runs at
the box's full radius, and `hudLayout()`'s rectangle is what
`test-cs012-p5.js` and (for its own four) `test-cs008-p4.js` measure against the
throat zone and the touch buttons. Reporting the craft's box would leave a third
of the ring unasserted. The ring itself is a **polyline** of
`C.HUD_JUMP_RING_SEG` segments to a full turn — `drawPoly` + `glowStroke` is the
one path (GDD §10.2), and `ctx.arc` would be a second.

MEASURED at the shipped sizes, non-mirrored: the glyph's rectangle is
x 1040–1070, y 666–696; the nearest touch button (Jump, bottom-right) starts at
x 1140. Mirrored it is x 1180–1210 against a button ending at x 140. Both clear
every well's throat-zone bounding box by the full height of the well.

---

## P4 — the combo

**What shipped.** `state.combo` `{ mult, kills, since, peak }` in `02-state.js`;
`comboMult()`, `comboKill()`, `comboDeath()`, `updateCombo()` and the private
`comboDrop()` at the foot of `12-scoring.js`; the four kill lines in
`09-collision.js` and `comboDeath()` in `killSkimmer()`; `updateCombo()` in the
play step and `dangerInputs()`'s real combo reading in `23-main.js`; the
centre-top readout and its ring in `15-render-hud.js`; `C.SFX.comboLost` (and
two candidates in `tools/sfx-lab.html`); `max_combo` and the `maxCombo` column
off `state.combo.peak`, with `C.TELEMETRY_PLACEHOLDER` deleted whole.
`STATE_FIELDS.CS012` gains `"combo"`. `test-cs012-p4.js` (350 assertions).
Build 638.8 → 655.0 KB. Five closed files edited in place. Suite 65 files green,
zero skips.

### The one thing this phase had to decide, and it was arithmetic

⛔ **O8 says the combo is centre-top at `C.HUD_COMBO_SIZE` 56 px with a ring
around it. O16's measurements for O8 were the top pause target (640, 84, r 56)
and "the top of the well's radius is at y 30" — neither of which is the binding
constraint.** MEASURED here, with `test-cs008-p4.js`'s own bounding-box
arithmetic: the **Fan** well (index 14) is an arc across the top of the screen,
so its throat zone reaches **y 76.29**, and its x span 504.6–775.4 straddles the
centre. Every other well's throat zone starts at y 154 or lower. So the whole
readout has to live in a band **76.29 px tall**, and at `C.HUD_MARGIN` 24 the
56 px em box alone (24 → 80) breaches it before a ring is drawn.

Two numbers came out of that, and both are new constants rather than a change to
O16's answer:
- **`C.HUD_COMBO_Y` = 6**, not `HUD_MARGIN` 24. The readout is the one HUD item
  that is not in a corner and the one that sits over the throat, so it gets its
  own top edge.
- **The ring is an ELLIPSE.** The widest reading is `C.HUD_COMBO_CHARS` (4)
  characters — "×3.5"; "×8" is two — which at 56 px and `C.TEXT_CHAR_W` 0.62 is
  138.88 px wide against a 56 px em box. A *circle* around that is 148 px tall
  and cannot fit in 76.29 px at any y. An ellipse at `rx = 73.44`, `ry = 32` is
  64 px tall. It is still one `drawPoly` + `glowStroke` polyline, so GDD §10.2's
  one path is untouched.

Shipped sum: 6 + 56 + 2 × 4 = **70**, clearing 76.29 by **6.29 px**. The test
asserts the clearance is real *and* under 40 px, so a future edit that quietly
made it comfortable by shrinking the text would be as visible as one that
breached it. ⛔ **This is the finding the next phase needs: `C.HUD_COMBO_SIZE`
cannot rise past ~62 without moving the Fan or relaxing §10.3.**

⚠ **The readout does overlap the undrawn top-centre pause target**, which O8
measured and accepted ("a centre-top combo sits inside it"). The two corner
touch buttons are clear, and the test probes those through the real hit test.

### Why the view carries `mode` when `combo > 1` would do

Plan §6 lists three view fields, `combo`, `comboFill` and `mode`, and
`hudComboAlpha()` reads both `mode` and `combo`. `combo > 1` alone would already
hide the readout in Classic, because nothing there ever writes `state.combo`.
The second condition is kept because O8's rule is *two* rules — "Overdrive only"
and "absent at ×1" — and a reader who deletes one of them should have to notice
that they are not the same claim. CS012 P5's `view.jump = null` is the other
shape; both are on the view, neither reads game state.

### O3's lapse, read literally

O3: "+0.5 every `C.COMBO_KILLS_PER_STEP` kills. A window with no kill costs 0.5,
and each further window costs another 0.5, down to ×1. **A death is ×1 at once,
with the kill count emptied.** The window restarts on every kill."

⛔ **The kill count is emptied on a DEATH and on a step up, and NOT on a lapse** —
O3 names it for the death alone, and this phase took that literally rather than
tidying it. So a player who stops firing for three seconds loses a half step but
keeps their progress toward the next one. `test-cs012-p4.js` asserts it as its
own line, so a later session that "fixes" it has to argue with the plan rather
than with a silence.

`since` wraps at the window (`since -= C.COMBO_WINDOW` in a `while`) rather than
being a pure time-since-last-kill, and that is what makes the HUD ring refill and
empty again for each further half step — the loss the player is being shown. The
`while` never runs twice at `C.FIXED_DT`, but it is a `while` so a larger `dt`
cannot silently skip a step.

### O5's hold, in two places on purpose

`updateCombo()` carries `if (state.dive.active) return;` **and** sits below
`Game.update()`'s dive short-circuit, which already means it cannot run in one.
The guard is duplicated so the rule belongs to the function rather than to the
call site's position: the test drives `updateCombo()` directly through 600 dive
steps, which is a claim about the function, and separately drives the real dive
branch, which is a claim about the wiring. Plan §6 asked for the rule; §8 asked
for it "staged step by step", and a rule that only exists as an ordering cannot
be staged.

### Item 8 in Overdrive is asserted PER CALL, not per step

`test-cs008-p2.js` compares a step's score delta against the step's events summed
at ×1. That form does not survive a multiplier, because **a step can span a step
up**: four kills in one Purge with three banked means the first three pay at ×m
and the fourth at ×m + 0.5, and "that step's multiplier" is then not a number.
Reconstructing the pairing would mean reconstructing the order the Purge, the
shot pass and the rim sweep resolve in — a re-implementation of the thing under
test.

So the soak spies `addScore` and records `state.combo.mult` live at every call,
and classifies each call by whether `state.tally.kills` has just risen (the kill
sites increment it immediately before they score). Each **kill** call must equal
a GDD §7 price off the board times the multiplier recorded at that call, and the
multiset of board prices must be empty at the step's end; each **other** call
must equal an unmultiplied literal — the chip's 5, or one of the clear bonuses.
That is order-free, stronger than a per-step total, and it is what makes
"multiplying a chip is red" a direct consequence rather than a hope.

⚠ **One fixture subtlety.** The board's price multiset holds prices **> 0** only.
A zero-price death is either a kill site paying `e.points()` of 0 (a Thorn taken
by its last chip, whose worth is its chips) or no kill site at all (a Weaver
bolt self-terminating at depth 1), and the two are the same number; a kill call
of 0 is accepted as itself. The first draft matched zeros against the board and
went red on a bolt at tick 6341.

⚠ **And one that cost a full run.** `gddPoints()` began as a copy of
`test-cs008-p2.js`'s, closed over the outer build's classes. This file builds
**seven** games (the base, five mutants and the Classic pair), and `instanceof`
is per build: every price came back 0 and every non-vacuity check passed on an
empty count. It now takes the build as its first argument.

**MEASURED, 72,000 played Overdrive steps** at levels 1, 6, 13 and 23, two seeds
each: 783 kill calls, 38 zero-price kills, 192 chips, 92 bonus calls, 42 clears,
57 deaths; every one of the fifteen lattice values from ×1 to ×8 was in force at
some point; Vaulter 325, Carrier 141, Drifter 88, Reaver 72, Surger 61, Weaver
58. Zero mismatches.

**Mutations, 5 of 5 red** (and a green control over the same 2,500-tick window):
multiplying a Thorn chip; multiplying the well-clear bonus; multiplying the
no-death bonus; a shot kill paid without the multiplier; a shot kill that does
not build. ⛔ `mutantRed()` asserts the mutation string was found **exactly
once** before asserting red — a `buildGame` throw on a stale string would
otherwise read as a pass, which is the failure mode this kind of check has.

### Classic, and what "bit-identical" is asserted against

Six mutation strings take the combo out of the build: one per kill line, one for
`comboDeath()` in `killSkimmer()`, one for `updateCombo()` in the play step.
The three `addScore(e.points() * comboMult()); comboKill(state); sfx(...)` lines
are textually identical, so each string carries the line that follows it
(`break;`, `continue;`, or the Purge's own `if`) — and because `buildGame`
refuses a string that is not in the build exactly once, the list is its own
uniqueness proof. A 6,000-step Classic session hashes identically, step for
step, with all six applied, and `state.combo` comes out of it at its shipped
default. A 3,000-step **Overdrive** session spends the same draws and holds the
same board with the same six applied, which is the other half: the multiplier
feeds back nothing but the score.

### MEASURED — the Overdrive intensity re-measure (O14, plan §1.4)

On the recording fake with the real `audioFrame()`, 16,000 frames over levels 1,
6, 13 and 23 with the Reaver, the Jump and the combo all live and the multiplier
reaching ×8:

| | Classic (CS010 P5, fire-holder) | Classic (plan §1.4, sharp bot) | plan §1.4's model, +0.5 per 4 | **Overdrive, MEASURED here** |
|---|---|---|---|---|
| director max | 0.668 | 0.381 | 0.521 | **0.6519** |
| sweep max | 0.668 | — | — | **0.6519** |

The model under-read by 0.13, and the reason is the two things it did not have:
the Reaver adds to `count` and `proximity`, and the Jump changes how long a
board stays busy. ⛔ **Every `setIntensity` / `setSweep` argument stayed in
[0, 1]** over all 16,000 frames (GDD §17 item 9). Per O14 the number is recorded
and GDD §19 keeps its ✗: 0.6519 against the 1.0 the sweep needs to open end to
end. No weight was rescaled (D6).

### Closed-file edits — 5 files, and TWO the plan did not predict

Plan §11 predicted `test-cs007-p4.js:154`/`:157`, `test-cs008-p3.js:155–156`,
`test-cs008-p2.js:387–388`, `test-cs011-p5.js:239` (P3 moved it; found by text)
and `test-cs009-p4.js:32`/`:109`/`:142`. All were rewritten in place.

⚠ **FINDING: `test-cs007-p4.js` has TWO MORE references the table missed**, both
`in` checks against the object rather than reads of its value — `:153`
(`!("score" in C.TELEMETRY_PLACEHOLDER)`) and `:160` (the same for `mode` and
`startDepth`). Deleting the object **whole** turns a key-absence check into a
`TypeError`, where a key-value read only changes its answer. Both were rewritten
to the replacement claim (`!("TELEMETRY_PLACEHOLDER" in C)`), keeping what the
assertion was always about: `score`, `mode` and `startDepth` are not placeholder
keys — and now nothing is. ⛔ **A future plan that deletes a config object owes
§11 a grep for `in <OBJECT>` as well as for `<OBJECT>.`**

`test-cs009-p4.js` also needed a fourth edit the table did not list: the lab's
`CONTEXT` table gains a `comboLost` row, because `:155` asserts every event in
`EVENTS` has an in-context sequence. That is the same edit as `:32` in spirit —
the event list drives four assertions, not three.

### What P4 did NOT do

- ⛔ **No RNG draw and no `heat()` call**, asserted off the built file's function
  bodies. The determinism hash (`1229033515`) and `GOLDEN_LANES` are unmoved.
- ⛔ **`TELEMETRY_FIELDS` did not move**, so `telemetry` stays v1: `maxCombo`
  kept its place (index 20) and only its source changed.
- ⛔ **`addScore()` is unchanged**, `19-sfx.js` is unchanged, and the Purge still
  never calls `onShot()`.

---

## P6 — the tenth soak, the review, and the close

### `scratchpad/test-cs012-p6.js` (114 assertions)

⛔ **A tenth file; the nine closed soaks are untouched.** It takes
`test-cs009-p6.js`'s drive, DOM target and whole-board hasher unchanged, and
`test-cs012-p3.js`'s recording fake leaderboard with **one record per board**,
set on `X._env.win` before the first frame. One `session()` drives either mode
from the boot title through `Game.frame()` in half-frames: PLAY → the mode row →
a Start Depth → a run to game over → RESTART → a second run, one of them paused
and resumed → QUIT TO TITLE, three times.

**FOUR sessions, two pairs.**

| Pair | A | B | The claim |
|---|---|---|---|
| Classic | as is | the jump key pressed every 97th FRAME **and** `{ stub: ["updateCombo", "updateJump", "comboKill", "comboDeath"] }` | the hash on every frame |
| Overdrive | the recording fake, instrumented | no audio API | the hash on every frame |

⛔ **The two pairs share a store on purpose.** The Classic session boots an empty
`Map`; the Overdrive pair boots over what it saved (its B side over a copy taken
before A ran), so the reload at the end can hold BOTH modes' tables and records
and can say that an Overdrive session never moved the Classic one. Each pair's
two sessions therefore start from equal stores, which is what makes their hashes
comparable at all.

**Readings the file had to take, and what each cost.**

- ⛔ **THE LANDING STEP IS NOT AN AIRBORNE STEP** (MEASURED). `updateJump()` runs
  at the top of `update()`, above `updateShots()` and the collision pass, so a
  step that BEGINS airborne can end in `recover` — which is contact-lethal and
  cannot fire, by design (O6). The first draft read the phase before the step
  alone and called **two legal landing deaths** a breach of "no contact death
  while airborne". The invariant is about a step that is airborne at BOTH ends.
- ⛔ **`state.shots.push` CANNOT BE INTERCEPTED for "no shot airborne"**, because
  `updateShots()` filters `state.shots` into a NEW array before it fires
  (`06-shots.js`), so a hook on the pre-step array never sees the shot. The
  MEASURED symptom was a green mutant: the `jumpCanFire()` gate removed, and
  nothing red. `state.tally.shotsFired` rises inside the fire branch itself and
  is the honest counter. ⚠ **The enemy array is the opposite case** — it is
  filtered *after* the entity and collision passes, so a Carrier's split does
  land on the pre-step array and that interception is sound.
- ⛔ **A SCHEDULE LONGER THAN THE JUMP CYCLE NEVER EXERCISES THE COOLDOWN.** At
  one press per 170 steps against a 2.30 s (~138 step) cycle, every press was
  ready and "a takeoff without the readiness gate" came back GREEN. The shipped
  schedule is one press per 43 steps: MEASURED **274 takeoffs against 509
  refusals**, and the mutant is red at 471 early takeoffs.
- ⛔ **`peak` IS 0 UNTIL THE RUN'S FIRST KILL** (GDD §14.4: a Classic run reports
  0), so "never below the live multiplier" starts there and not at `newState()`.
  And ⛔ **`startGame()` is not the only boundary**: `quitToTitle()` re-mints
  `state` too, so the one honest marker is a spy on `newState()` itself. Both
  were MEASURED as false failures first.
- ⛔ **A Reaver count kept only in the instrumented session cannot say "a Classic
  run never sees one."** MEASURED: `eligibleKinds` ignoring the mode came back
  green until the count moved into every session's frame loop.
- ⛔ **ITEM 8 HERE IS THE O4 CLAIM, NOT `test-cs008-p2.js`'s TOTAL.** Each
  `addScore` call is classified by whether `state.tally.kills` has just risen
  (trap 5): a kill call must equal a GDD §7 price of an entity that died on that
  step times the multiplier recorded live AT THAT CALL, with the board's price
  multiset empty at the step's end; every other call must equal an
  **unmultiplied** literal from `{ chip, the three clear bonuses, the Start Depth
  bonus }`. `test-cs012-p4.js` owns the per-well bonus reconstruction and its
  five mutations; this file owns the claim on a front-door session, where the
  bonuses fire against a `diedThisWell` the driver does not control.

**MEASURED (seed 20260925).**

| | Classic | Overdrive (audio) |
|---|---|---|
| Frames / play steps | 110,174 / 52,687 | 121,840 / 58,985 |
| Frames whose hash differs from its pair | **0** of 110,174 | **0** of 121,840 |
| Levels played | 1–8, 13–19, 23–28 | 1–16 |
| Start Depth record at the end | classic **27** (staged 23) | overdrive **15** (staged 13) |
| Run ends, all eligible, all `died` | 6 | 6 |
| Submits | 6 on `vector-vortex`, 0 on the other | 6 on `vector-vortex-overdrive`, 0 on the other |
| Reaver entity-steps | **0** | 6,619 |

**The Overdrive session, per step (77,958 steps checked).** Takeoffs **274**,
presses refused by the cooldown **509**, wholly airborne steps **14,600**
(18.8 %), `comboLost` **98**, dives run to their end **27**. 451 kill calls (26
at a price of 0), 157 chips, 54 bonus calls, **169 unmultiplied calls made while
the multiplier was above ×1**, 451 builds. Kills by class: Vaulter 220, Carrier
75, Reaver **48**, Weaver 44, Drifter 25, Surger 13. All nine lattice values ×1
to ×5 were in force. Zero NaN, zero array over `C.ENEMY_CAP` / `C.SHOT_MAX`,
zero item-8 mismatches.

**The audio side.** `drive` scheduled on **9,276** steps and `pulse` on none
(R10); `title` 14. Worst step **13 nodes** against `C.MUSIC_STEP_NODE_MAX` 16.
Every director level, music intensity, sweep level and setter argument in [0, 1]
on all 121,840 frames. The high-pass automated on **548 frames, exactly the 548
frames `jumpAirborne()` flipped**, by `setTargetAtTime` alone, between 0 and
`C.JUMP_HP_HZ` 700 and nothing between.

### ⛔ MEASURED — O14's re-measure on front-door boards

| | Classic (CS010 P5) | Classic (plan §1.4, sharp bot) | Overdrive (CS012 P4, staged) | **Overdrive (P6, front door)** |
|---|---|---|---|---|
| Director maximum | 0.668 | 0.381 | 0.6519 | **0.6448** |
| Sweep there | 5,801 Hz | 2,190 Hz | 5,457 Hz | **5,377 Hz** |

Two independent Overdrive measurements now agree to within 0.007: P4's on staged
boards at levels 1, 6, 13 and 23, and P6's over 121,840 front-door frames at
levels 1–16 with the menus, pauses and dives included. ⛔ **Per O14 the number is
recorded and GDD §19 keeps its ✗** — 0.6448 against the 1.0 the sweep needs to
open end to end. No weight was rescaled (Paul's D6). The soak asserts the
maximum is at or past tier 3's threshold (non-vacuity) **and** below 1, so the
day a retune does open it, the assertion says so rather than sitting silent.

### Mutation record — 13 of 13 red

Each applied to `src/`, rebuilt, `test-cs012-p6.js` run alone, `src/` restored.

| # | Mutation | Red |
|---|---|---|
| M1 | `collideSkimmer()`'s airborne skip removed | 4 contact deaths on airborne steps |
| M2 | `updateShots()`'s `jumpCanFire()` gate removed | 3,543 shots off the rim airborne |
| M3 | a takeoff without the readiness gate | 471 takeoffs inside a cooldown (first at `cool` 0.5167), and the refusal count collapses to 6 |
| M4 | `updateJump()`'s mode gate removed | the Classic pair diverges at frame 12,154 |
| M5 | `eligibleKinds()` ignores the mode | 19,998 Reaver entity-steps in a Classic session |
| M6 | a Thorn chip multiplied | 127 unmultiplied calls paying 17.5 at ×3.5 |
| M7 | a shot kill paid without the multiplier | 435 kills with no matching board price |
| M8 | a shot kill that does not build | 351 steps of builds ≠ scored kills |
| M9 | `submit()` always uses Classic's client | all six Overdrive ends post to `vector-vortex` |
| M10 | `noteCleared()` writes BOTH modes | the two modes offer the same Start Depth list |
| M11 | `setHighpass()` made non-idempotent | 121,835 automations against 548 flips |
| M12 | `audioFrame()` writes `state` | the Overdrive pair diverges at frame 15 |
| M13 | `comboDrop()` without its `sfx` seat | 0 `comboLost` |

⛔ **M11 is also a lesson, and it is CS012's third in the same shape.** Its first
draft compared `autoFrames.join(",")` against `flips.join(",")`, so the mutant's
answer was a **159,000-entry string**. P3 recorded that a mutation run which
*throws* is a defect in the test rather than a result, and P5 hit it again; an
answer nobody can read is the same failure wearing a different coat. The
assertion now reports two counts and the first mismatching index.

### ⛔ The baseline ledger — plan §10, measured at the close

`git diff --stat de53c0d..HEAD -- scratchpad/test-cs006-p2.js scratchpad/test-cs004-p1.js`
is **empty**.

| Baseline | Plan §10 predicted | MEASURED at the close |
|---|---|---|
| `test-cs006-p2.js:522` `P1_DETERMINISM_HASH` | unmoved P1–P6 | **1229033515**, unmoved, file untouched |
| `test-cs004-p1.js:301` `GOLDEN_LANES` | unmoved | `[10,10,12,0,8,14,12,12,8,14,10,0,7,7,12,3,2,5]`, file untouched |
| The nine closed soaks' paired hashes | unmoved | green, all nine |
| `COUNTS.tracks` | 3 at P1 | **3** |
| `COUNTS.enemies` | 7 at P2 | **7** |
| `COUNTS.enemyKinds` | 10 at P2 | **10** |
| `COUNTS.wells` / `openWells` | unmoved | **16 / 6** |
| `STATE_FIELDS` | `+ CS012: ["combo", "jump"]` | **`["jump", "combo"]`** — ⚠ P5 shipped before P4, so the order is the build's; the guard is the SUM, **26**, and it equals `newState()`'s key count |
| `C.SPAWN_SCHEDULE` | unmoved, `level` + `kind` | **7 rows**, fields `level`, `kind` only |
| `C.SPAWN_SCHEDULE_OVERDRIVE` | 1 row at P2 | **`[{ level: 6, kind: "reaver" }]`** |
| `C.SFX` / `C.SFX_KILL_PITCH` | 22 / 8 | **22 / 8** |
| `C.MODE_TRACK` / `C.MUSIC_TRACK_CHOICES` | + `overdrive` / + `"drive"` | **`{ classic: "pulse", overdrive: "drive" }` / `["auto", "pulse", "drive"]`** |
| `C.TELEMETRY_PLACEHOLDER` | deleted at P4 | **absent from `C`** |
| `TELEMETRY_FIELDS` / `telemetry` version | unmoved | **29 / 1** |
| `progress` version | 2 at P3 | **2**, with `migrateProgress` |
| `settings`, `scores` versions | unmoved | **1, 1** |
| `climbMult()` call sites | 5 | **5** (`test-cs007-p2.js` green, unedited) |
| kit-audio | 0.4.0 at P5 | **0.4.0** |
| kit-input / kit-menu / kit-leaderboard | unmoved | **0.8.0 / 0.1.0 / 0.2.1** |
| `MANIFEST` | 25 modules at P2 | **25 + 3 inlined**, 655.0 KB |
| The suite | 66 files, 0 skips | **66 files, 0 skips, 118 s wall** (60 files / 85.3 s at `de53c0d`) |
| `CLAUDE.md` | < 50 KB | **39,650 bytes** (34,068 at `de53c0d`) |

⛔ **No baseline moved in any phase, exactly as the ledger predicted.**
`C.GAME_VERSION` is **0.0.9** at this commit, and CREDITS reads it.

### ⛔ Acceptance criteria — plan §12, item by item

1. ✅ **Overdrive — Jump with cooldown and an unmistakable airborne state on
   three channels.** The rules, the immunity (all seven contact killers,
   mutation-checked), the lift, the shadow and the high-pass are asserted
   headless (`test-cs012-p5.js`) and played (`-p6.js`: 274 takeoffs, 509
   refusals, zero airborne deaths, zero airborne shots, 548 high-pass
   automations on exactly 548 flips). ⚠ "Unmistakable" is a skipped playtest.
2. ✅ **Overdrive — combo builds, decays, displays, feeds the director.** All
   four asserted at P4 and played at P6: nine lattice values in force, a peak
   that never falls, 98 falls sounded, and the director's combo input measured.
3. ✅ **Overdrive — Reaver correct.** The contract, the schedule, the hop, the
   hunt, the respawn guarantee, the wall bound and rim arrival at P2; played at
   P6 — 48 killed, 6,619 entity-steps, never below level 6, never in Classic,
   never outside `[0, lanes−1]` on an open well.
4. ✅ **Audio — flagship tracks ≥ 36 bars (A→B→C).** ◐ → ✅: `drive` runs 36 bars
   in three sections, 62.6 s at 138 BPM (P1).
5. ✅ **Audio — filter sweep audible end to end: RE-MEASURED and recorded, ✗
   kept** (O14). 0.6519 at P4, **0.6448** at P6. No weight rescaled.
6. ✅ **Audio — every gated layer passes the solo audition.** Unchanged: `drive`
   ships untiered (O13), so no gate was added.
7. ◐ **Audio — the Surger tone over music at every tier.** The proxy extends to
   `drive` (0.3505 against 0.450) and survived kit-audio 0.4.0 unedited. "By ear"
   is a skipped playtest.
8. ✅ **Meta — local top 10 per mode.** ◐ → ✅ at P3; played at P6, six rows in
   each mode's table across a reload.
9. ✅ **Meta — separate online boards.** ✗ → ✅ at P3, on Paul's registered
   `vector-vortex-overdrive`; played at P6, six submits per mode and none on the
   other board, mutation-checked.
10. ✅ **Quality.** No banned vocabulary (`test-cs008-p6.js` scans the whole built
    file); plays from `file://` (one HTML, no `fetch`/`import` in the game
    script); nothing opaque below depth 0.25 — the combo readout clears the Fan's
    throat zone by 6.29 px and the jump glyph clears every well by the well's
    own height; the concat build is the oracle (every test loads `dist/`);
    ⛔ **zero skips**, the P3 registry read included (`../coinless-kit` at
    `e2efed5` is present and both entries were read).

### Plan §13 and §14 at the close

- **§13 held.** No tokens, no Warden, no Mimic; `C.MAX_TOKENS`, `TOKEN_*`,
  `PTS_WARDEN` and `PTS_MIMIC` are still unread. No ring-flight Dive;
  `C.DIVE_TIME_OD` and `DIVE_RINGS_MAX` unread. No achievements, no onboarding.
  No `drive` tier, gain or tempo touched. No intensity weight rescaled. No heat,
  concurrency-ladder or Classic constant changed for Overdrive. No telemetry
  column, no bench key, no ONLINE time-window switch. kit-input, kit-menu and
  kit-leaderboard unedited. Nothing deployed and no Worker edit. No backport.
- **§14's risks.** K1 answered by O1-A and re-proved at P2 and P6. K2 mitigated
  by P1 landing first. K3 closed by Paul's `e2efed5`. K4 is unmeasurable here and
  is a skipped-playtest entry. K5 cost exactly what it predicted — one press per
  driver, thirteen files, no unlisted red. K6 held: the Butterworth needs no
  headroom term, ⚠ but the VERSION bump itself cost six closed edits §11 had not
  predicted (P5's finding). K7 accepted as provisional art. K8 held: no text trap
  was tripped in any phase. K9 is what this close fixes. K10 is Paul's and is
  closed by `e2efed5`.
- **§15's assumptions.** A1–A4 held. ⚠ **A5 under-read the soak**: it predicted
  ~100,000 frames from the ninth's, and the tenth runs 110,174 + 121,840 over
  four sessions in 32 s, well inside `run-all.js`'s 120 s per-file timeout — but
  only because the Overdrive script stops jumping when the fire hold ends.
  Airborne immunity is 39 % of the cycle (O6), and a jumping passive board
  outlives its segment cap.

### Review notes — where two phases said different things

⛔ **Read together, the six entries above disagree in five places, and all five
were live documents rather than the build.**

- **`eligibleKinds` has two signatures in the GDD, in the same section.**
  §8.1's CS007 P3 paragraph carried ⛔ *"the eligible set is a function of
  `state.level` and nothing else"* and named `eligibleKinds(level)`; P2 added a
  CS012 paragraph below it saying ⛔ *"a function of the level and the run's
  mode"*. Two ⛔ invariants, one section, contradicting each other. The CS007
  paragraph now carries the two-argument signature, points at the paragraph
  below, and says its table is a Classic run's. §6.1's shipped note took the same
  repair.
- **`DIFFICULTY-NOTES.md` was never updated for Overdrive's schedule.** It is on
  the "touching difficulty" read contract and still said ⛔ *"a function of the
  level and nothing else"*, with the one-argument call and no second table. P2's
  doc list named GDD §8.1 and `CLAUDE.md` and missed it. It now carries the
  second table, the Reaver's share of releases per band, the fact that its tables
  are a Classic run's, and that CS013's two rows move those shares. ⛔ **That is
  the file CS013 will read.**
- **§11.8's node enumeration predates the high-pass.** "Every tier is covered
  because a gate targets only 0 or 1, the sweep has no peak, and the duck and dip
  never exceed 1" was written at CS010 P1 and P5 added a node to the chain. P5
  recorded the reasoning in §11.1; §11.8's own list now names it, so a reader of
  the headroom model alone sees a complete chain.
- **⛔ Seven closed assertions read a BOARD id off the SAVE keyspace.** P3
  established that `C.GAME_ID` is kit-storage's keyspace and
  `C.LEADERBOARD_GAME_IDS` is the board table — `CLAUDE.md` now says ⛔ *"Never
  reach for `C.GAME_ID` when what is wanted is a board"* — but `test-cs011-p5.js`
  kept naming the Classic board with `C.GAME_ID` at six sites (`:179` the
  `create()` config, `:193` the registry lookup, and four queue keys) and
  `test-cs001-p0.js:19` still called it "the Worker registry name". They pass
  only because the two constants hold the same string. All seven were rewritten
  in place to the replacement claim; none was deleted or weakened, and the
  `test-cs001-p0.js` assertion keeps its value and gains the reason the value can
  never move.
- **"A mutation run that throws is a defect in the test" is recorded three times
  in this file and nowhere a session loads.** P3 wrote it, P5 hit it again, P6
  hit a third shape of it (an answer 159,000 entries long). ⛔ A hazard that
  lives only in `log/` is a hazard nobody reads, so it is now a `STATUS.md` line
  for CS013's item-13 test rather than a third log entry.

Two more differences were checked and needed no reconciliation. **P5 ran before
P4**, so `STATE_FIELDS.CS012` is `["jump", "combo"]` where plan §10 wrote
`["combo", "jump"]`; the guard is the sum and `test-registry.js`'s comment
already says why. And **P5's correction of plan R3's mechanism** ("the rim sweep
never runs there, because fire is refused" → the pass is skipped, which is what
delivers it) is already the wording in GDD §4.5, §14.2 and `CLAUDE.md`.

---

## Version history

| Version | Date | What landed |
|---|---|---|
| 0.0.9 | 2026-09-17 | CS012 P1–P6: **Overdrive's core.** `drive`, the flagship track (138 BPM, 36 bars A→B→C, six struck layers, untiered until Paul's lab pass) and Overdrive's AUTO; `C.MODE_FLAGS` and `modeHas()`, the one reader of both; the **Reaver**, `Vaulter`-derived at 1.6× the hop and hunting mid-climb, in a new `07-enemies-overdrive.js` behind a second schedule table; **OVERDRIVE on MODE, first and highlighted**, with its own online board `vector-vortex-overdrive`, SCORES per mode, and a per-mode Start Depth record (`progress` v2, migrated); the **Jump** — airborne as a PHASE, immune to every contact killer, a draw-only lift, a stroked rim shadow, kit-audio **0.4.0**'s high-pass and an Overdrive-only HUD glyph; the **combo multiplier**, applied at the four kill lines and never in `addScore()`, with its centre-top readout, `comboLost`, the director's fifth input and `max_combo`'s real source. The tenth soak: two paired sessions per mode, one hash per frame, and Overdrive's invariants on every step of a played board. ⛔ No baseline moved. |

## Carried forward (not CS012's to fix)

- ⛔ **Paul, when he wants it:** `drive`'s lab session (PASS marks, tiers, gains,
  tempo). It ports as its own commit and rewrites `test-cs012-p1.js`'s "no tier"
  and "no audition mark" assertions in place. Nothing waits on it (A3).
- ⛔ **CS013:** the five tokens (a warm palette no enemy uses — ⚠ the Reaver
  shares `VAULTER_COLOR`, so the warm range is still free), the Warden and the
  Mimic in `07-enemies-overdrive.js`, their `C.SPAWN_SCHEDULE_OVERDRIVE` rows
  (11 and 16), `PTS_WARDEN` / `PTS_MIMIC`, and their rectangles beside the combo
  readout and the jump glyph in `hudLayout()`.
- ⛔ **CS014:** the ring-flight Dive; `C.DIVE_TIME_OD` and `DIVE_RINGS_MAX` are
  still unread, and `C.DIVE_TIME` is the WHOLE dive, grace included.
- ⛔ **CS015:** achievements, and `achievements` joining `Store` and
  `Profiles.remove()`'s `OWN_KEYS`.
- ⚠ **Unowned, and none of them moved in CS012:** the pad-only silence (R2), the
  VOICE bus, the Dive's visual, the Surger tone's 1.106 sample peak, the
  enemy/menu palette and HUD sizes, `tools/glow-lab.html`, GDD §12's four-second
  promise (CS016's), and whether the headroom gate should also bound the
  limiter's INPUT (P1's finding — Paul's call, not a phase's).
- ⚠ **Backport** kit-input 0.8.0, kit-menu 0.1.0, kit-fx, kit-audio **0.4.0** and
  kit-leaderboard 0.2.1 to coinless-kit — each a separate manual step, verified
  against that repo's own suite. `createScores` is kit-scores' draft.
