# CS015 — achievements: the id table, written once

Per-changeset narrative log and version history. ⛔ Not session context: a later
phase pulls this file in only when a question genuinely needs project history.

`STATUS.md` carries what the NEXT phase must act on; this file carries how a call
was reasoned, what was measured against what was predicted, and mutation records.

---

## P1 — the module, the store, the week (2026-09-20)

### §0.1's two corrections were already in place

The prompt's step 0 asked for `PLANNED-FEATURES-CS015.md` §0.1's two `STATUS.md`
corrections before anything else. **Both were already recorded**, by the planning
session itself — §0.1's own text says the one file that session edits outside its
two documents is `STATUS.md`, for exactly these. MEASURED, `grep` at `47bd435`:

- `STATUS.md:112` — *"⛔ A DEFECT IN GDD §0: THERE IS NO ROW FOR AN ACHIEVEMENTS
  SCREEN"*, under "What CS015 must act on", with the measurement and the
  instruction not to invent the row.
- `STATUS.md:199` — *"⚠ `C.LIVES_MAX` LEAVES THIS LIST — it IS reachable"*,
  corrected in place inside the "Unowned, none reachable by the suite's drivers"
  bullet, with the five-of-seven probe reading and the reason (run LENGTH, not
  the driver).

Nothing was rewritten. Verifying and moving on is the cheaper answer, and
re-recording either would have duplicated a line in a file with under ten lines
of headroom.

### The `CLAUDE.md` valve did NOT fire, and that was measured, not assumed

The prompt named **Save data** and **Leaderboard** as the sections this phase
edits and said to fire the valve first if one is over ~4 KB. MEASURED, both
sections' byte counts at `47bd435`:

| Section | Bytes |
|---|---|
| `### Save data` | 2,728 |
| `### Leaderboard` | 1,323 |
| `### Math and lifecycle` | 5,464 (already rule-only; not this phase's) |
| `## Code map` | 4,487 (no reasoning to move; not this phase's) |

Neither section this phase edits is over ~4 KB, so the valve's condition is not
met and it stays unfired — the ⚠ SETTLED rule forbids a standing sweep, and
firing it on an under-size section would be exactly that. The ceiling is still
respected by keeping the edits small: the Save-data table gains one row and one
clause, not a paragraph.

### A12's collision, resolved by `createScores`' seam — and what `defs` is

⛔ The config invariant ("every tunable in `C`") and the boundary contract ("a kit
module names no `C`") point in opposite directions over a table whose tier
thresholds are tunables. §1.9 MEASURED the shipped resolution: `createScores`
takes `perMode` from the config as an **option**, so the number is where a tuning
pass finds it and the module still reads nothing. `createAchievements` takes the
whole definition table the same way.

**Plan §7 asks for TWO tables kept apart; the prompt asks for FOUR options.** Both
hold: the definition table is one option whose value carries two arrays —

```js
ACHIEVEMENTS: { perWeek: 5, lifetime: [ … ], weekly: [ … ] }
```

`lifetime` is what exists and what each row reads; `weekly` is the pool that
rotates. Neither array names the other's numbers, the rotation reads
`weekly.length` and `perWeek` and nothing else, and nothing derives a pool index
from a lifetime row's tier — which is the whole of §7. The alternative, a fifth
option `pool`, was rejected because the prompt and plan §3 and §8 all name
exactly `{ defs, load, save, now }`, and a build phase invents no interface the
plan did not price.

### The row shape, and why an untiered row is a second key rather than a flag

A row is `{ id, mode, fact }` plus **exactly one of** `tiers: [t1, t2, t3]` or
`at: n`. A11 makes the payload's `tier` the 1-based index reached and **0 for an
untiered row**, so the two kinds are genuinely different answers and not one
answer with a null in it. Making them two keys means the factory can REFUSE a
row that carries both or neither, at construction, with a named message —
`createScores`' shape — rather than at evaluation, which is where A12-B's
rejected variant would have failed.

`mode` is `"classic"`, `"overdrive"` or `null` (A5): the tag is a property of the
achievement, so a third mode is a table edit and `C.MODE_FLAGS` gains nothing.

### The placeholder ids, and why they are prefixed

⛔ An id that reaches a player's store is save data from that moment on. P3
replaces the rows' contents whole, so P1's rows must be ids **nothing will ever
store under**. Two guards, not one: every placeholder id starts `_`, which no
shipped row in §9 does, **and** P1 builds no evaluation seat at all, so the
shipped build has no caller of `evaluate()` and cannot write the key. A stored
placeholder id would in any case be dropped on read — unknown ids are dropped,
which is A6's known-value-else-default rule doing its job.

### The ISO week, MEASURED

The function is `isoWeekKey(ms)`: the UTC calendar day, shifted to that ISO
week's Thursday, whose UTC year is the ISO year; the week number is counted from
the Monday of the week holding 4 January. Its constants — a day in ms, 7 days, the
3-day shift to Thursday, 4 January, the 53-week ordinal base — are **ISO-8601's
definition, not tunables**, and they stay local: a kit module may not read the
game's config, and the config invariant is about knobs a tuning pass turns.
Recorded here because it is the one place in this phase where the two rules were
weighed and the answer was "neither applies".

MEASURED, all eight cases green before the module was written (a scratch probe,
then re-asserted off the build in `test-cs015-p1.js`):

| Instant (UTC) | Key | Why it is a boundary |
|---|---|---|
| 2020-01-01 | `2020-W01` | Wednesday; week 1 straddles the year |
| 2021-01-01 | `2020-W53` | ⛔ the 53-week year naive code gets wrong |
| 2025-12-29 | `2026-W01` | Monday of the NEXT ISO year, in December |
| 2026-01-01 | `2026-W01` | Thursday; the anchor day |
| 2027-01-03 | `2026-W53` | ⛔ Sunday; the last day of the previous ISO year |
| 2027-01-04 | `2027-W01` | the Monday after it — the roll |
| 2026-09-20 23:30 | `2026-W38` | ⛔ Sunday late: UTC reads the EARLIER week |
| 2026-09-21 00:00 | `2026-W39` | the roll, 30 minutes later |

The Sunday 23:30 pair is the one the UTC rule exists for: any local reading east
of UTC already says W39 at that instant, so two devices would disagree about
which five are live.

### The rotation, MEASURED

A stride walk, pure integer arithmetic on the key: `ord = year * 53 + week`,
`start = ord % n`, `stride = 1 + (ord / n | 0) % (n - 1)`, then `perWeek` picks at
`(start + i * stride) % n`, stepping to the next free index on a collision.
⛔ It spends no draw, calls no clock beyond the key it is given, and reads no
board — which is why a dive, a spawn or a soak's hash cannot move under it.

MEASURED over a pool of 20 and 104 weeks (2026-W01 … 2027-W52): **68 distinct
sets, all 20 entries touched, 24–27 appearances each**, and one key returns the
same five on every call. Plan §1.6's probe measured the same walk over a pool of
29 and reported 98 distinct sets over 104 weeks; the drop to 68 is the smaller
pool, not a different function — with `n` 20 the start cycles every 20 weeks and
the stride every 400.

### What is NOT here

⛔ No evaluation seat, no `tally` field, no real id, no screen, no sound, no kill
line, no draw call. `evaluate()` exists — it is the module, and "tiers only rise"
cannot be proved without it — but the shipped build has no caller: P2 seats it at
the clear edge and at `Meta.runEnded()`.

### Mutation records

Each is a `buildGame({ mutate })` over the real build, and each string was
asserted present in the build **exactly once** before the red was asserted
(`_harness.js` throws otherwise, which is the defect the rule guards against).

| Mutation | What it proves |
|---|---|
| `if (reached > (held.lifetimeTiers[row.id] \|\| 0)) {` → `reached !== …` | ⛔ tiers only RISE. The mutant reaches tier 3, then a run at 15 LOWERS it to 1 |
| `OWN_KEYS`' four-name literal → the old three | ⛔ a profile delete removes `achievements` BY NAME. The mutant leaves `p1.achievements` in storage and still removes `p1.settings` |

**Two, not three.** A third was drafted — deleting the finite-fact guard, to prove
a missing fact is a skip rather than a comparison against `undefined` — and was
dropped because ⛔ **its string is in the build TWICE**, once in the lifetime loop
and once in the weekly one, so `_harness.js`'s `mutate` refuses it by design. The
claim is asserted positively instead (a missing, a `NaN` and a string fact each
unlock nothing), and a future phase that wants the mutation owes the two loops a
shared reader first, not a hand-picked unique substring.

### The closed-file edits, and one honest note about the first

Exactly the two the prompt predicted, and no third.

- **`test-cs011-p6.js:326`** — the `declared` regular expression gains
  `achievements`. ⚠ **MEASURED green WITHOUT the edit** (the file run alone with
  the name removed from the expression again: 50 assertions, exit 0), and that is
  worth saying:
  plan §1.2's V2 stand-in declared the key *and wrote it at the run's end*, so it
  went red; P1 declares it and writes nothing, because there is no seat yet. The
  edit is still right — the assertion's claim is "every stored key is declared",
  and `achievements` is now declared — and P2, which lands the writer, would
  otherwise have had to make a repair that belongs to the phase that declared the
  key. The line is repaired in place and `:330`'s message is untouched.
- **`_harness.js`'s `EXPORTS`** — `+createAchievements`, `+Achievements`. Two,
  not three: the week key and the rotation are reachable through the factory's
  own instance (`weekKey()`, `weeklyFor(key)`), so a test drives the real code
  without a third top-level name. ⚠ CS014 added ten `EXPORTS` rows across two
  phases and predicted neither; this one was predicted, in plan §11, and landed
  at the predicted count's floor.

### Two documents that did NOT move, and why

- **GDD §16.4's repository layout.** `20-achievements.js` was already in `src/`
  and in `MANIFEST`; it gained content, not a place. Its new sibling
  `src/20-achievements.NOTES.md` is not listed either, because `src/22-meta.NOTES.md`
  is not — §15.7 states the rule ("each carrying a `.NOTES.md` from its first
  commit") and the tree does not enumerate them.
- **GDD §19's Meta row.** Still ✗. P1 meets part of plan §12's list (declared and
  versioned, per profile, removed with the profile, never enumerated; monotonic
  tiers with the mutation; the UTC week on six boundaries; the rotation's purity
  and its no-draw) and cannot meet the rest — "every id in the shipped table
  unlocks from a played or staged board" is P3's and P4's. A row is marked met
  when it is met, not when it is nearly met.

### One correction made in `STATUS.md` while editing the region

Its GDD §0-defect bullet read *"§0's call A1, **unanswered**"*. A1 has been
answered since — a screen off the title, no toast — so the bullet was rewritten
to point P3 at the three sections it edits together. ⚠ The bullet was written
before Paul answered and nothing had reread it; the lesson is rule 3b's again,
one file older.

### Baselines

`P1_DETERMINISM_HASH` 1229033515, `GOLDEN_LANES`, `COUNTS`, `STATE_FIELDS` and
all twelve soaks' paired hashes: ⛔ **unmoved.** P1 spends no draw, adds no
`state` field, edits no kill line and draws nothing. `MANIFEST` is unmoved at
25 + 3 — `20-achievements.js` was already listed and `KIT_INLINE_AFTER` is its
name, so the module becoming real added nothing to the build's shape.

### After P1 — the vocabulary scan becomes a substring scan (Paul's call)

Plan §0's finding: `test-cs008-p6.js` tested `\bword\b`, so a banned word
glued on by `_` or camelCase passed. Paul chose the substring scan over an
underscore-aware one and over checking by eye. MEASURED against the build at
`cc30cc3` first: a substring scan hits nothing but `webkitAudioContext` (two
lines, the browser's API), so "web" alone reads `web(?!kit)`. The closed
assertion is rewritten in place — same claim, wider sight — with probes built
from the word list so the test file names no banned word beyond it. Its own
commit.

### Version history

| Version | What landed |
|---|---|
| 0.0.11 | CS015 P1 — `createAchievements()`, `C.ACHIEVEMENTS`, the `achievements` key declared v1 and in `OWN_KEYS`, the UTC ISO week and the rotation |
