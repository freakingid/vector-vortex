{
  "name": "vector-vortex",
  "version": "0.0.1",
  "private": true,
  "description": "Vector Vortex - browser tube shooter. Zero runtime dependencies.",
  "license": "GPL-3.0",
  "scripts": {
    "build": "node build.js",
    "test": "node build.js && node scratchpad/run-all.js",
    "test:only": "node build.js && node scratchpad/run-all.js --only",
    "watch": "node build.js --watch",
    "serve": "node tools/serve-lan.js"
  },
  "devDependencies": {}
}
